# CLAUDE.md — 행운상자

전 세션 공통 규칙. 실행법은 각 디렉토리 `README.md`, 설계·계약·절차는 `docs/wiki/` 에 있다.
작업은 한국어를 사용한다.

## 규칙 우선순위

충돌하면 위가 이긴다.

1. 현재 대화에서 사용자가 직접 지시한 내용
2. 작업 대상 폴더의 `CLAUDE.md`
3. 상위 폴더의 `CLAUDE.md`
4. 루트 `CLAUDE.md`
5. 기존 코드 스타일과 프로젝트 관례

작업 전에 루트뿐 아니라 **작업 대상 폴더의 `CLAUDE.md` 도 반드시 확인한다.**

## 공통 규칙

1. **한국어로 쓴다.** 응답·문서·주석·커밋 메시지·서브에이전트 프롬프트 전부. 코드 식별자와 명령어는 원문.

2. **`.env` 로 시작하는 파일을 Claude 가 직접 읽지 않는다.** Read·cat·grep·head 전부 금지. (프로그램은 당연히 읽는다 — 사람이 채운 시크릿이 대화 기록에 남지 않게 하려는 규칙이다.)
   환경변수 정보가 필요하면 **`env.sample`** 을 읽는다. 새 키가 필요하면 `env.sample` 에 키와 주석만 추가하고 사용자에게 갱신을 요청한다. 값을 추측해 채우지 않는다.

   | 컴포넌트 | 템플릿 (읽어도 됨) | 실제 파일 (읽지 않음) |
   |---------|------------------|---------------------|
   | worker | `worker/env.sample` | `worker/.env_worker` |
   | backend | `backend/env.sample` | `backend/.env_backend` |
   | frontend | `frontend/env.sample` | `frontend/.env_frontend` |

3. **일이 주어지면 분석 → 계획 → 검토 → 실행 순으로 한다.** 계획 없이 코드를 쓰지 않는다.

4. **실행하기 전, 다른 세션의 작업이 필요하면 위키부터 쓴다.** 세 세션(`worker` `backend` `frontend`)이 **동시에** 돌기 때문이다. 계약(`docs/wiki/10-contracts/`)이나 결정(`docs/wiki/00-decisions/`)을 먼저 기록하지 않고 착수하면 다른 세션이 낡은 전제 위에서 개발한다. 위키가 세 세션의 **유일한 통신 수단**이다. 코드를 먼저 쓰고 위키를 나중에 맞추지 않는다.

5. **자기 디렉토리 밖의 코드를 고치지 않는다.** 다른 컴포넌트가 필요하면 그 코드가 아니라 `docs/wiki/10-contracts/` 의 계약을 읽는다.

6. **설계·절차·결정은 `docs/wiki/` 에서 찾는다.** 출발점은 `docs/wiki/index.md`. 위키에 없으면 지어내지 말고, 위키에 먼저 쓴 뒤 개발한다.

7. **금지 표현을 쓰지 않는다.** 당첨 확률·고확률·예상 적중률·1등 예측·당첨 보장 등. 코드·주석·UI 문구·API 필드명 어디에도. 전체 목록은 `docs/wiki/40-domain/forbidden-expressions.md`.

8. **커밋 메시지는 한국어.** `feat:` `fix:` `docs:` `chore:` 접두어를 쓴다.

## 주석 규칙

코드를 작성하거나 수정할 때 주석을 충분하고 풍부하게 쓴다. 다음은 반드시 설명한다.

- 이 코드가 **필요한 이유**
- 주요 처리 흐름
- 조건문·분기문의 **의도**
- 예외 처리를 그렇게 한 **이유**
- 추후 유지보수 시 주의할 점

**무엇을 하는지가 아니라 왜 그렇게 처리했는지**를 중심으로 쓴다. 당연한 문법 설명은 쓰지 않는다. 디버깅과 유지보수에 도움이 되는 주석을 우선한다.

## CLAUDE.md 관리 원칙

- **항상 가볍게 유지한다.** 핵심 규칙만 둔다. 매 턴 컨텍스트에 강제 주입되기 때문이다.
- 루트에는 **프로젝트 전체 공통 규칙만** 둔다. 특정 기능·도메인·모듈에만 해당하는 내용은 그 폴더의 `CLAUDE.md` 로 분리한다.
- 임시 메모, 일회성 작업 기록, 세부 구현 설명을 루트에 누적하지 않는다.
- 설명이 길어지면 별도 문서(`README.md` 또는 `docs/wiki/`)로 빼고 요약이나 링크만 남긴다.
- 새 주요 폴더·모듈을 만들 때 그 영역만의 규칙이 있다면 그 폴더에 `CLAUDE.md` 를 만든다.
- 폴더 `CLAUDE.md` 에는 **제약**만 둔다 — 역할, 소유/비소유 경계, 코딩 규칙, 수정 시 주의사항, 참조할 위키 경로.
  **파일 구조, 실행·테스트 방법, 관련 API·DB·화면·배치 정보는 같은 폴더 `README.md` 에 둔다.**
  판정 기준: *어기면 안 되는 것은 `CLAUDE.md`, 찾아보면 되는 것은 `README.md` 와 위키.*
- 작업 중 반복적으로 필요한 규칙을 발견하면 적절한 위치의 `CLAUDE.md` 에 정리할 것을 **제안한다.** 일회성 참고사항은 넣지 않는다.

## 계약을 바꿀 때

사용자 승인 없이 `docs/wiki/10-contracts/` 를 고치지 않는다. **영향도 조사 → 승인 → 수정 → `docs/wiki/log.md` 기록** 순서를 지킨다. 계약과 코드가 다르면 코드가 틀린 것이다.

## 디렉토리

| 경로 | 세션 | 책임 |
|------|------|------|
| `worker/` | A | 외부 수집 → Postgres 쓰기. 배치·스케줄 |
| `backend/` | B | Postgres 읽기 → REST API. 예측·꿈해몽 |
| `frontend/` | C | Next.js 렌더링·SEO·애널리틱스 |
| `docs/wiki/` | 공유 | 지식체계 |
| `docs/raw/` | — | 원본 기획·시안. **읽기 전용** |
| `work_order/` | — | 아카이브. **수정 금지** |
