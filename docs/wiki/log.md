# 위키 변경 로그

append-only. 새 항목을 **아래에** 추가한다. 과거 항목을 고치지 않는다.

기록 대상:
- 페이지 생성·삭제
- **계약 변경** (`10-contracts/`) — 반드시. 무엇을 왜 바꿨고 어느 세션이 영향받는지
- ADR 추가 또는 `deprecated` 전환
- lint 결과 중 사람이 결정한 사항
- 코드와 위키가 어긋난 것을 발견하고 고친 일

형식: `YYYY-MM-DD | 작업 | 대상 | 한 줄 설명`

---

2026-07-09 | 초기화 | `SCHEMA.md` | 위키 규약 확정. 그래프 레이어 미사용, 계약 페이지의 특별 지위 명시
2026-07-09 | 생성 | `00-decisions/0001~0009` | Phase 0 결정 9건 기록
2026-07-09 | 생성 | `10-contracts/*` | DB 스키마·API·잡·환경변수 계약 4건 확정. **이 시점부터 세션 분기 가능**
2026-07-09 | 생성 | `20-design/*` | 시안(`메인샘플.png`) 기반 토큰·반응형·컴포넌트·a11y 4건
2026-07-09 | 생성 | `30-seo/*` | 메타데이터·구조화데이터·애널리틱스·애드센스·검색엔진등록 5건
2026-07-09 | 생성 | `40-domain/*` | 기존 코드에서 추출한 도메인 지식 4건
2026-07-09 | 생성 | `50-ops/*`, `90-external/*` | 운영 3건, 외부 의존 3건
2026-07-09 | 발견 | `40-domain/prediction-algorithm.md` | 기존 `CLAUDE.md` 가 `confidence` 필드를 정의했으나 **코드에 존재하지 않음**. 실제로는 `avg_number_hits`. 문서가 없는 확률 개념을 발명했고 그것이 금지 표현. 신규 API 에서 미노출로 확정
2026-07-09 | 발견 | `40-domain/prediction-algorithm.md` | 기존 `CLAUDE.md` 가 `pattern.py` 에 "조합 레벨 필터" 가 있다고 적었으나 없음. 필터는 `montecarlo.py` 인라인 구현
2026-07-09 | 발견 | `40-domain/dream-pipeline.md` | 기존 `CLAUDE.md` 의 "3-세트 조합 생성" 은 실제로 3 tier × tier당 10세트
2026-07-09 | 발견 | `10-contracts/db-schema.md` | `lotto_results` 실측 1,231행(1~1231 연속). `CLAUDE.md` 의 "1,204회차" 는 낡은 값
2026-07-09 | 발견 | `00-decisions/0008-python-313-torch-pin.md` | torch 2.13.0 이 Python 3.13/3.14 를 공식 지원. 과거 3.11 핀의 원인은 torch 2.11 이었음. 3.13 승격 가능
2026-07-09 | 폐기 | `00-decisions/0005-postgres-migration.md` | SQLite `journal_mode=DELETE` 결정(WSL/NTFS 락 회피)을 `deprecated` 로 기록. Postgres 전환으로 무효
2026-07-09 | 수정 | `SCHEMA.md` | 태그 분류 체계에 `ops` 추가 (누락 발견)
2026-07-09 | 생성 | `50-ops/wsl-drvfs-pitfall.md` | `/mnt/d`(drvfs) 에서 디렉토리 rename 시 dentry 캐시가 오염돼 stat=ENOENT / mkdir=EEXIST 모순이 발생. 두 이름이 별칭이 되어 한쪽 `rm -rf` 가 둘 다 지운다. 실제 겪고 기록
2026-07-09 | 재편 | 저장소 | 구 Vite 프론트를 `frontend_old/` 로 이동(git rename 23파일). `worker/` `frontend/` 골격 생성. `frontend_bak` 이름은 WSL 캐시 오염으로 사용 불가하여 `frontend_old` 채택
2026-07-09 | 생성 | `CLAUDE.md` ×4, `README.md` ×4, `env.sample` ×4 | 문서 계층 규약 적용. 루트 36줄/세션 54~58줄로 상한 준수. 구 루트 `CLAUDE.md`(약 200줄)를 README 와 위키로 해체
2026-07-09 | 결정 | 루트 `CLAUDE.md` 규칙 4 | "실행 전, 다른 세션의 작업이 필요하면 위키부터 쓴다" 를 명문화. 세 세션이 동시에 돌기 때문
2026-07-09 | 개명 | 전역 | **`collector` → `worker`**, `연계수집기`/`수집기` → `워커`. 디렉토리·환경변수(`COLLECTOR_*`→`WORKER_*`)·위키 페이지 3건(`worker-jobs`, `0003-worker-writes-backend-reads`, `0004-manual-trigger-on-worker`)·frontmatter `owner` 포함. `items_collected`/`collected_at` 식별자는 불변
2026-07-09 | 정정 | `00-decisions/0006-keep-chromadb-not-pgvector.md` | **사실 오류 수정.** "chroma_words 는 git 에 포함된다" 고 썼으나 실제로는 36MB 이고 `.gitignore` 에 등록되어 git 에 없다. 백업 반론이 유효함을 인정하고, 보관 방법 결정을 `tbd` 로 남김. pgvector 도 이 문제를 풀지 못한다는 점은 그대로
2026-07-09 | 확정 | `10-contracts/env-vars.md` | env 파일명 규약: worker=`.env_worker`, backend=`.env_backend`, **frontend=`.env.local`**. Next.js 는 임의 파일명을 읽지 않아 `.env_frontend` 를 만들면 전부 `undefined` 가 되고 에러도 안 난다
2026-07-09 | 확정 | `10-contracts/env-vars.md` | 가상환경은 uv 가 관리한다(`uv sync`/`uv run`/`uv add`, `uv.lock` 커밋, sudo 금지). 세션 CLAUDE.md 3건에 반영
2026-07-09 | 결정 | `50-ops/deployment.md` | **worker 와 backend 는 별개 컨테이너로 배포한다.** 같은 호스트여도 서비스를 나눈다. backend 컨테이너에 `app_writer` 자격증명이 없어야 권한 분리가 침해 시나리오까지 방어한다. 합칠 경우 ADR 필수
2026-07-09 | 보강 | `00-decisions/0003-worker-writes-backend-reads.md` | "백엔드에 쓰기 요구가 생기면" 절 추가. **워커에 HTTP 로 던지는 구조는 안티패턴** — 워커는 배치 잡 러너지 쓰기 서비스가 아니다. 권한은 컴포넌트가 아니라 데이터 소유권을 따라 나눈다(`app_user` 롤 + 자기 스키마). 워커에게 작업을 지시해야 하면 Postgres outbox 테이블
2026-07-09 | 보강 | `10-contracts/db-schema.md` | 롤 초기화 SQL 을 실제 실행 가능한 형태로 교체. `REVOKE ... FROM PUBLIC`, `ALTER DEFAULT PRIVILEGES FOR ROLE` 이 빠져 있었다. 스크립트: `worker/scripts/init_roles.sql` (비밀번호는 psql 변수로 주입)
2026-07-09 | 확정 | `10-contracts/db-schema.md` · `env-vars.md` | **개발 DB 는 신규 생성이 아니라 기존 `prod_db` 를 쓴다.** WSL Docker `bskim-dev-pg18`(postgres:18.4), 호스트 포트 **5179**. DB·`public` 스키마 소유자는 `prod_user`(슈퍼유저 아님, DBeaver 접속용). `CREATE DATABASE` 하지 않고 스키마 소유권도 뺏지 않는다. `prod_db`/`prod_user` 라는 이름은 로컬 개발용인데 운영으로 오인될 수 있어 **사용자가 인지하고 감수한 위험**
2026-07-09 | 정정 | `10-contracts/env-vars.md` | **frontend env 파일명을 `.env.local` → `.env_frontend` 로 되돌림.** 사용자 규약(`.env_<컴포넌트>`)을 따른다. Next.js 가 이 이름을 자동 로드하지 않는 것은 사실이므로, `next.config.ts` 최상단에서 `dotenv` 로 명시 로드하는 방법을 함정과 함께 기록. 로더를 빼면 전부 `undefined` 가 되고 에러도 안 난다
2026-07-09 | 확장 | 루트 `CLAUDE.md` | **규칙 우선순위**(대화 > 폴더 > 상위폴더 > 루트 > 관례), **주석 규칙**(무엇이 아니라 왜), **CLAUDE.md 관리 원칙** 추가. 분량 상한을 40줄 → 약 80줄로 조정. 세션 `CLAUDE.md` 는 60줄 유지. `.env` 열람 금지는 **Claude 에게만** 적용되고 프로그램은 읽는다는 점을 명시
2026-07-09 | 조정 | 문서 계층 | 사용자가 제시한 "폴더 `CLAUDE.md` 에 파일 구조·실행/테스트 방법·API/DB 정보를 쓴다" 는 "CLAUDE.md 를 가볍게 유지한다" 와 충돌. **제약은 `CLAUDE.md`, 실행법·구조는 `README.md`** 로 정리해 반영
2026-07-09 | 완료 | Postgres | 사용자가 `worker/scripts/init_roles.sql` 실행. `app_writer`/`app_reader` 롤 생성 및 권한 분리 테스트 통과
2026-07-09 | 변경 | 포트 | **backend 포트 8002 → 8005.** `frontend/env.sample`, `backend/run.py`, README 3건, `local-setup.md`, `deployment.md`, `env-vars.md`, `index.md` 반영. `docs/raw/` 는 읽기 전용이라 손대지 않음(위키가 정본)
2026-07-09 | 완료 | `10-contracts/env-vars.md` | 사용자가 `.env_worker`/`.env_backend`/`.env_frontend` 를 만들고 실제값을 채움. **Phase 1·2·3 을 막는 항목이 사라짐.** `news` 잡만 네이버 API 키 대기(`lotto` 잡은 키 없이 동작)
2026-07-09 | 수정 | `.claude/agents/postgres-db-expert.md` | **위험한 결함 제거.** 에이전트가 자신을 "`gtob` 프로젝트" 로 소개했고(같은 서버에 실존하는 타 프로젝트 DB), `.env_worker` 를 읽으라고 지시했으며(루트 규칙 2 위반), 키 이름이 `DB_*` 로 달랐다. 접속을 `docker exec -u postgres` 소켓(비밀번호 불필요)으로 교체하고 프로젝트 제약 절을 추가
2026-07-09 | **계약 변경** | `10-contracts/db-schema.md` | **DB 명명 전면 재설계** (사용자 승인). SQLite 이름을 그대로 옮기지 않는다. 테이블 4개 전부 개명: `lotto_draws`→`lotto_draw`, `lotto_prize_tiers`→`lotto_prize`, `news_articles`→`lotto_news`, `job_runs`→`collect_job_log`. 예약어 3개 제거(`trigger`→`exec_type_cd`, `rank`→`prize_grade_no`, `source`→`provider_nm`). 전 컬럼 `COMMENT ON`, `pk_/fk_/uk_/ck_/ix_` 명명, `BIGSERIAL`→`GENERATED ALWAYS AS IDENTITY`. DDL 원본: `worker/scripts/ddl/001_initial_schema.sql`
2026-07-09 | 생성 | `10-contracts/db-naming-standard.md` · `00-decisions/0010-db-naming-standard.md` | 표준단어·도메인 접미어 사전과 ADR. **API 응답 필드는 바꾸지 않는다** — 공개 계약을 DB 스키마에 결합시키지 않는다. 백엔드가 매핑하며 매핑표는 `db-schema.md` 에
2026-07-09 | 결정 | `10-contracts/db-schema.md` | 접미어 `_ymd` 의 타입은 **`date`** 다(공공표준의 `CHAR(8)` 아님). 문자열이면 날짜 연산마다 캐스팅이 필요해 인덱스가 죽는다. 이 서비스는 최근 N회차·D-day·sitemap lastmod 로 날짜를 계속 계산한다
2026-07-09 | 추가 | `10-contracts/db-schema.md` | 신규 제약 `uk_lotto_draw_draw_ymd`(UNIQUE). 주 1회 추첨이라 일자↔회차 1:1. **실데이터 1,231행에서 중복 0건 확인 후 추가.** 날짜 조회 인덱스를 겸하므로 별도 인덱스를 두지 않는다
2026-07-09 | 정정 | `10-contracts/db-naming-standard.md` | 에이전트 산출물의 내부 모순 수정. 도메인사전은 `_no`(업무상 번호)를 정의하는데 DDL 은 사전에 없는 `_num`(`winning_num1`, `bonus_num`)을 썼다. 같은 개념에 이름이 둘이면 표준이 아니다 → `winning_no1..6`, `bonus_no` 로 통일하고 표준단어 `번호=num` 을 삭제
2026-07-09 | 검증 | `worker/scripts/ddl/001_initial_schema.sql` | `BEGIN; ... ROLLBACK;` 으로 문법 검증. 4테이블·2인덱스·36컬럼 전부 생성 성공, **컬럼 코멘트 36/36**, 롤백 후 DB 무변경 확인. **DDL 을 실제 적용하지 않았다** — 적용은 워커의 Alembic
2026-07-09 | 생성 | `10-contracts/component-boundaries.md` | **세션이 가장 먼저 읽는 경계 페이지.** 세 세션의 소유/금지/접점을 한 장에. 세션 `CLAUDE.md` 3건의 "먼저 읽을 위키" 표 최상단에 추가. 세션 기동 프롬프트에서 이 경로를 가리킨다
2026-07-09 | 함정 | `10-contracts/db-schema.md` | **DBeaver 에서 테이블이 안 보이는 함정.** `app_writer` 가 만든 테이블의 소유자는 `app_writer` 이고, `prod_user` 는 DB 소유자일 뿐 슈퍼유저가 아니라 남의 테이블에 권한이 없다. DB 를 소유한다고 그 안의 객체를 소유하는 게 아니다. 해법은 `GRANT app_writer TO prod_user` (롤 기본이 INHERIT). **운영에서는 이 멤버십을 주지 않는다**
2026-07-09 | **계약 보강** | `10-contracts/api-contract.md` | 백엔드 세션이 구현에 착수하며 응답 스키마가 비어 있던 **여섯 곳**을 확정. 엔드포인트·필드명·표현 규약은 불변이므로 기존 계약과 충돌 없음. ① 목록 응답 봉투 `{total,page,size,items}` 통일(`rounds` 는 `round_no` DESC, 범위 밖 page 는 빈 배열 200) ② 회차 상세 `traits` 6필드 확정 — **`hot_count`/`cold_count` 제외**(기준 시점이 모호해 과거 회차에 붙이면 오독된다) ③ `stats/*` 에 `window`·`rounds_analyzed` 공통 필드, `overdue` 는 window 가 아니라 역대 전체에서 계산, pattern 응답 필드 확정(`tail_diversity_avg`→API 는 `tail_variety_avg`) ④ `recommend` 의 `traits` 는 6필드+`hot_count`/`cold_count`, 기준을 `hot_window` 로 응답에 노출 ⑤ **`pure_random` 은 회차 50개 미만에서도 200** — DB 를 읽지 않는 통제군이라 막을 근거가 없다. `hot_count`/`cold_count` 는 `null` ⑥ `dream/keywords` 슬러그는 **한글 그대로**(검색어와 URL 일치, 로마자 규칙의 정본 부재), `dream/recommend` 응답에서 `score`·`distance` 제거(`score` 는 금지 필드명). 영향: 프론트 세션은 이 문서만으로 전 엔드포인트를 타이핑할 수 있다
2026-07-09 | 구현 | `worker/**` | **Phase 1 워커 구현.** 잡 레지스트리·잡별 락·크론·수동 트리거·`collect_job_log`·Alembic·이관 스크립트. 코드를 먼저 쓰고 위키를 나중에 맞췄다 — 루트 규칙 4 위반. 사용자가 지적해 아래 계약 갱신을 수행. **순서를 지켰어야 했다**
2026-07-09 | **계약 변경** | `10-contracts/env-vars.md` | worker 환경변수 11개 추가(`LOTTO_RETRY_DELAY_MIN` `LOTTO_MAX_ATTEMPT` `CRAWL_*` 5개 `NEWS_MAX_RETRY` `NEWS_BACKOFF_BASE_SEC` `STALE_RUNNING_HOURS` `CATCH_UP_DAYS`). 전부 기본값이 있어 `.env_worker` 에 없어도 기동한다. 수집 딜레이·재시도 횟수를 코드에 박지 않기 위한 것. worker 세션만 영향
2026-07-09 | **계약 변경** | `10-contracts/env-vars.md` · `worker/CLAUDE.md` | **의존성에 `pydantic-settings` 추가.** 기존 목록(`httpx` `apscheduler` `psycopg` `alembic` `fastapi` `uvicorn` "뿐")과 같은 문서의 `SettingsConfigDict(env_file=".env_worker")` 요구가 서로 모순이었다. 목록의 취지는 ML 의존(`torch`·`transformers`) 금지이지 경량 설정 라이브러리 금지가 아님을 명문화
2026-07-09 | **계약 변경** | `10-contracts/worker-jobs.md` | `lotto` 재시도를 절대시각("22:00, 23:00")이 아니라 **상대시간**(`LOTTO_RETRY_DELAY_MIN` × `LOTTO_MAX_ATTEMPT`)으로 규정. `LOTTO_CRON` 을 바꾸면 재시도도 따라 움직여야 한다. 예약된 재시도는 워커 재시작 시 사라진다(메모리 잡스토어) — 수동 트리거로 복구
2026-07-09 | **계약 변경** | `10-contracts/worker-jobs.md` | **`/internal/health` 는 `X-Job-Key` 를 요구하지 않는다.** 기존 문서가 세 엔드포인트 아래 헤더를 뭉뚱그려 적어 모호했고, `local-setup.md` 의 스모크 테스트는 키 없이 호출한다. 나머지 둘은 키 필요
2026-07-09 | **계약 변경** | `10-contracts/worker-jobs.md` | **`401` 이 `404` 보다 먼저 판정된다.** 키를 모르는 호출자가 상태코드 차이만으로 유효한 잡 이름을 알아내지 못하게 한다. `404` 의 판정 기준은 DB 제약이 아니라 잡 레지스트리(`JOBS`)
2026-07-09 | **계약 변경** | `10-contracts/worker-jobs.md` | `news` 잡의 **키 부재 시 동작** 규정: 워커는 기동하고, 크론에 등록하지 않으며, 수동 트리거는 `202` 후 즉시 실패해 `collect_job_log` 에 사유를 남긴다. 조용히 성공하지 않는다. `collected_cnt` 는 조회 건수가 아니라 실제 INSERT 된 행 수(`rowcount`)
2026-07-09 | **계약 변경** | `10-contracts/worker-jobs.md` | **락 획득 원자성 함정.** `lock.locked()` 확인과 `await lock.acquire()` 사이에 어떤 `await` 도 넣지 않는다. `run_id` 를 먼저 얻으려 `job_log.start()` 를 끼우면 두 요청이 나란히 `409` 를 통과해 같은 잡을 두 번 실행한다. 부분 성공 기록(`JobProgress`)도 명문화
2026-07-09 | 함정 | `10-contracts/db-schema.md` | ★ **Alembic 이 `app_writer` 비밀번호를 평문으로 뱉었다. 실제로 겪었고 사용자가 비번을 교체했다.** `alembic.ini` 는 ConfigParser 라 URL 의 `%`(퍼센트 인코딩된 특수문자)를 보간으로 해석해 `ValueError` 를 내는데, **그 메시지가 URL 전체를 출력**한다. 해법: `set_main_option` 을 쓰지 않고 `env.py` 에서 `create_engine(URL)` 을 직접 만들고, 오프라인 모드에는 `dialect_name` 만 넘긴다. psycopg 접속문자열도 f-string 대신 `make_conninfo`
2026-07-09 | 추가 | `10-contracts/db-schema.md` | Alembic 은 **`--autogenerate` 를 쓰지 않는다**(`target_metadata = None`). DDL 정본은 `scripts/ddl/001_initial_schema.sql` 이고 마이그레이션은 `op.execute()` 로 그대로 옮겨 적는다. 모델을 두면 정본이 둘이 된다. `worker/README.md` 의 autogenerate 안내를 정정. `alembic_version` 테이블이 추가로 생기지만 계약 대상이 아니다
2026-07-09 | 정정 | `10-contracts/env-vars.md` · `90-external/naver-search-api.md` · `index.md` | **네이버 API 키가 이미 발급·기입되어 있었다.** 위키는 "미발급 → `news` 잡 차단" 으로 기록돼 있었으나 실제로는 채워져 있어 `news` 잡이 동작한다. 남은 미결은 **일일 쿼터**뿐이며 `naver-search-api` 는 `status: draft` 유지. 현재 하루 6회 호출(3회 × 검색어 2개)
2026-07-09 | 적용 | Postgres `prod_db` | 사용자 승인 후 `alembic upgrade head` 실행. 4테이블·2인덱스·**컬럼 코멘트 36/36** 생성. `ALTER DEFAULT PRIVILEGES FOR ROLE app_writer` 가 실제로 동작함을 확인 — 새 4테이블 전부 `app_reader` 가 `SELECT` 가능·`INSERT` 불가
2026-07-09 | 완료 | `50-ops/migration-sqlite-to-postgres.md` | 1회성 이관 실행. **1,231행 적재, 검증 5쿼리 전부 통과.** 재실행은 종료코드 1 로 거부(멱등 아님). `backend/data/lotto.db` 는 보관
2026-07-09 | 생성 | `frontend/**` | **Phase 3 프론트엔드 구현.** Next.js 15 App Router + React 19 + TS, Tailwind 없이 `tokens.css` 커스텀 프로퍼티. 페이지 22개(초안 5.1 URL 구조 전부), 정적 생성 25 라우트. **백엔드가 죽어 있어도 빌드·렌더가 통과한다** — 모든 조회가 실패 시 `null`/빈 봉투를 반환하고 화면이 폴백을 그린다. 세 세션이 병렬로 돌기 때문에 필요한 성질
2026-07-09 | 보강 | `10-contracts/env-vars.md` | 프론트에 `NEXT_PUBLIC_CONTACT_EMAIL` 추가. `/contact` 와 `/privacy` 에 노출된다. 비면 문의 페이지가 주소 대신 "준비 중" 안내를 보여준다 — 동작하지 않는 가짜 주소를 만들지 않는다. **애드센스 신청을 막는 값**(심사는 연락 수단의 존재를 본다)
2026-07-09 | 정합 | `frontend/src/lib/api-types.ts` | 백엔드 세션이 확정한 계약에 프론트 타입을 맞춤. 페이지네이션 봉투(`{total,page,size,items}`), `traits` 6필드(회차·꿈해몽) vs 8필드(`CombinationTraits`, 추천), `pattern` 응답(비율맵·`sum_range`·`tail_counts`), `dream` tier 구조, 한글 슬러그. **계약과 어긋난 것은 코드였다** — 관대 파서(`unwrapList`)를 지우고 봉투를 그대로 받는다
2026-07-09 | 결정 | `30-seo/metadata-strategy.md` | **통계 페이지의 `window` 를 쿼리스트링으로 받지 않는다.** 서버 컴포넌트가 `searchParams` 를 읽으면 라우트가 dynamic 이 되어 ISR 이 깨지고 `?window=50` 이 중복 색인된다. 서버가 네 구간을 모두 렌더링해 클라이언트 탭에 넘기고 전환은 상태로만 한다. canonical 은 쿼리 없는 URL 하나
2026-07-09 | 함정 | `30-seo/metadata-strategy.md` | **서버 컴포넌트는 클라이언트 컴포넌트에 함수를 넘길 수 없다.** render prop 을 넘겨 `Functions cannot be passed directly to Client Components` 로 빌드가 죽었다. 이미 렌더링된 엘리먼트를 넘기면 그 내용은 서버 컴포넌트인 채로 전달된다
2026-07-09 | 함정 | `frontend/src/lib/api.ts` | `cache: 'no-store'` 를 쓰면 그 fetch 를 호출하는 라우트가 통째로 dynamic 이 되어 홈의 ISR 이 사라진다. POST 는 어차피 Next 데이터 캐시 대상이 아니므로 `next: { revalidate }` 만 남긴다
2026-07-09 | 결정 | `30-seo/metadata-strategy.md` | 꿈 키워드 표제어가 4,802개다. 전부 `generateStaticParams` 로 구우면 빌드가 감당하지 못한다. **앞쪽 200개만 사전 생성**하고 나머지는 `dynamicParams`. **사이트맵에는 전부 싣는다**(색인은 빌드와 무관). 무엇이 빠졌는지 빌드 로그에 남긴다 — 조용히 자르지 않는다
2026-07-09 | 결정 | `20-design/components.md` | **헤더 검색은 전체 검색이 아니라 회차 번호 이동**이다. `/search` 는 URL 구조에 없다. 동작하지 않는 검색창을 만들지 않으려는 선택이고, 그래서 JSON-LD `WebSite` 에 `potentialAction` 을 넣지 않는다 — 화면에 없는 기능을 마크업에 넣지 않는다
2026-07-09 | 결정 | `frontend/src/app/contact/page.tsx` | 문의 **폼을 만들지 않는다.** 폼은 서버에 이메일·내용을 받는다는 뜻이고, 그러면 개인정보처리방침에 수집 항목과 보유 기간을 적어야 한다. "이용자 식별 정보를 일절 저장하지 않는다" 가 방침의 핵심이므로 메일 링크만 둔다
2026-07-09 | 검증 | `frontend/` | 빌드 통과(백엔드 미기동 상태, 25 라우트 정적 생성). JS 없는 원본 HTML 에서 주요 15개 페이지의 본문·면책 고지 확인(`curl` + `grep`). 금지 표현 grep 결과 UI 노출 2건을 수정하고 주석 4건은 "금지 목록을 서술하는 곳"으로 유지. 광고·GA4·네이버 스크립트가 env 미설정 시 미렌더링됨을 확인. **375px 가로 스크롤과 Lighthouse 는 실측하지 못했다** — 헤드리스 브라우저 의존 라이브러리 설치에 sudo 가 필요. 대신 고정폭 CSS 규칙 3건이 전부 `overflow-x:auto` 컨테이너 안에 있음을 전수 확인
2026-07-09 | 확정 | `90-external/naver-search-api.md` | **일일 쿼터 25,000 회** (사용자 확인). `status: draft` → `stable`, `tbd` 태그 제거. 남은 미확인은 재배포 약관·출처 표기 의무뿐이며 잡을 막지 않는다
2026-07-09 | **계약 변경** | `10-contracts/worker-jobs.md` · `env-vars.md` | **`news` 잡에 당일 필터 추가** (`NEWS_SAME_DAY_ONLY=true`). 발행일자(KST)가 수집 실행일자와 다른 기사는 저장하지 않는다. `sort=date` 여도 네이버는 과거 기사를 함께 준다 — 실측 94건 중 **34건이 3일치 과거 기사**였다. 날짜는 반드시 KST 로 변환 후 뗀다(UTC 비교 시 오전 9시 이전 발행분이 전날로 밀린다). 실행일은 잡 시작 시 한 번 고정한다(자정 넘김 방어). 발행일 미상은 버린다. 필터는 INSERT 전에 적용 — `link_url` UNIQUE 때문에 한 번 들어가면 재판단 기회가 없다
2026-07-09 | **계약 변경** | `10-contracts/worker-jobs.md` · `env-vars.md` | **`NEWS_CRON` 기본값 `0 6,12,18 * * *` → `0 * * * *`(매시간).** 부하가 아니라 당일 필터 때문이다 — 당일 기사만 담으므로 **실행 간격이 곧 놓치는 시간 창**이 된다. 하루 3회면 18시~자정 6시간치가 통째로 사라진다. 매시간 × 검색어 2개 = 48회/일 = 쿼터의 0.2%. **감수한 대가**: 마지막 실행~자정 발행분(최대 1시간)은 영영 들어오지 않는다
2026-07-09 | 정리 | `lotto_news` | 당일 필터 규칙에 어긋나는 기존 34행 삭제(발행일 ≠ 수집일, 7/6~7/8분). 새 규칙상 재수집되지 않으므로 영구 삭제다. 60행 잔존. 검증: 규칙 위반 0건, `link_url` 중복 0건
2026-07-09 | 확인 | `lotto_news` | **중복 제거는 이미 3중으로 동작 중.** ① 같은 실행 내 검색어별 결과를 `link_url` 로 병합(키워드 합침) ② `uk_lotto_news_link_url` + `ON CONFLICT DO NOTHING` ③ `collected_cnt` 는 조회 수가 아닌 실제 INSERT 행 수. 실측 `link_url`·제목·`orig_link_url` 중복 전부 0건
2026-07-09 | 검증 | `frontend/` | **계약 스키마를 그대로 흉내낸 목 백엔드(:8005)로 재빌드·재검증.** 페이지네이션 봉투·8필드 `traits`+`hot_window`·`pattern` 비율맵·`tail_counts`·dream `tiers`·한글 슬러그(`/dream/돼지` SSG 생성 확인)가 모두 화면에 렌더됨을 `curl` 로 확인. 회차 3건 SSG, sitemap 26 URL. 이 과정에서 **조사 버그 발견·수정** — `traitSentence` 가 "은" 을 하드코딩해 "당첨번호은" 이 출력됐다. 받침 판별 유틸(`lib/korean.ts`)을 만들어 적용하고 7케이스 단위 검증
2026-07-09 | 구현 | `backend/**` | 백엔드를 Postgres 읽기 전용 API 로 재작성. SQLite·크롤러·스케줄러·`crawl_logs` 제거(워커 소유). `repository.py` 한 곳에서만 DB↔API 필드를 매핑한다. 예측·꿈해몽 모듈은 **재작성하지 않고** 데이터 주입부만 psycopg 로 교체 — `predictor.predict()` 가 `db_path` 대신 `Sequence[Draw]` 를 받는다. 라우터는 `asyncio.to_thread` 로 감싼다
2026-07-09 | 검증 | `00-decisions/0008-python-313-torch-pin.md` | ADR 대로 **Python 3.13.8 + torch 2.13.0+cu130 import 성공.** `requires-python = ">=3.13,<3.14"` 상한 적용. 3.12 로 내릴 필요 없음
2026-07-09 | 검증 | `10-contracts/db-schema.md` | 워커가 마이그레이션을 적용해 4테이블 생성(`lotto_draw` 1,231행, `lotto_news` 60행, `lotto_prize` 0행). backend 의 전 SELECT 를 `SET ROLE app_reader` 세션에서 실행해 통과. **INSERT 는 `permission denied for table lotto_draw` 로 거부됨** — 권한 분리가 실제로 동작한다. `ALTER DEFAULT PRIVILEGES` 함정도 재현되지 않음(app_writer 가 만든 테이블을 app_reader 가 읽는다)
2026-07-09 | 실측 | `40-domain/dream-pipeline.md` | `chroma_words` 의 `lotto_word` 컬렉션 **4,802 단어 확인**(문서의 주장이 맞았다). 메타데이터 `lotto_number` 는 `"[5, 33, 39]"` 형태의 문자열. 단어 목록만 필요하면 `get_collection().get()` 으로 임베딩 모델 없이 읽을 수 있어 `/api/dream/keywords` 는 20초 함정을 피한다
2026-07-09 | 함정 | `10-contracts/env-vars.md` | **pydantic 의 `ValidationError` 가 `.env` 내용을 스택트레이스에 통째로 출력한다.** 필수 필드 하나가 빠지자 `input_value` 에 나머지 키와 비밀번호가 실려 나왔다. Claude 가 `.env` 를 읽지 않아도 예외 처리가 규약을 무너뜨린다. 해법: 필수값을 `required` 로 두지 말고 빈 기본값 + `model_post_init` 에서 값 없는 예외를 던진다
2026-07-09 | 발견 | `10-contracts/env-vars.md` | **`backend/.env_backend` 에 `DATABASE_URL` 이 없고 `app_writer` 자격증명이 들어 있다.** 백엔드가 기동하지 않으며, 더 중요하게는 권한 분리(ADR 0003)가 무효화된다. 사용자 조치 필요: `DATABASE_URL`(app_reader)로 교체하고 `PG_*` 키 삭제, `app_writer` 비밀번호 교체 권장
2026-07-09 | 완료 | `10-contracts/env-vars.md` | 사용자가 `NEXT_PUBLIC_CONTACT_EMAIL` 을 채움. `/contact` 의 mailto 링크와 `/privacy` 10항 문의처가 폴백 문구 대신 실제 주소를 렌더링함을 확인(값은 출력하지 않고 링크 존재만 검사). **애드센스 필수 정책 페이지 4종이 모두 실제 콘텐츠로 채워졌다** — `/privacy`(제3자 광고 쿠키 안내 포함)·`/terms`·`/disclaimer`·`/contact`
2026-07-09 | 검증 | `backend/**` | **계약의 전 엔드포인트가 200 을 반환함을 실제로 확인.** `.env_backend` 가 깨져 개발 DB 에 붙을 수 없으므로, 일회용 `postgres:18` 컨테이너에 db-schema.md 의 DDL 을 올리고 개발 DB 의 `lotto_draw` 1,231행을 `pg_dump --data-only` 로 복제해 스모크했다(사용자 DB 는 읽기만 했고 무변경 확인). 결과: 단위 40 + 통합 27 통과, `sets=3&seed=1` 두 호출이 바이트 단위로 동일, 전략 6종 200, 없는전략/window=7/sets=99 → 422, 없는 회차 → 404. `lotto_prize` 의 SELECT 를 회수해 **`ALTER DEFAULT PRIVILEGES` 누락 상황을 재현**한 결과 사용자에게는 `{"detail":"일시적인 서버 오류입니다."}` 와 500 이, 서버 로그에는 `permission denied for table lotto_prize` 원문이 남았다 — 계약의 "403 을 흘리지 않는다" 가 실제로 동작한다
2026-07-09 | 정합 | `frontend/src/app/sitemap.ts` | **버그 수정.** 계약이 `sitemap-entries.news` 를 **id 오름차순**으로 확정하면서 `news[0]` 은 가장 **오래된** 기사가 됐는데, 프론트는 그것을 `/news` 의 `lastmod` 로 쓰고 있었다. 또 `lastmod` 가 `null` 일 수 있게 되어 `new Date(null)` → `1970-01-01` 이라는 거짓 신선도 신호가 나갈 수 있었다. null 을 거른 뒤 최댓값을 취한다(ISO 8601 은 사전순 = 시간순). 목 백엔드로 재현·검증
2026-07-09 | 정합 | `frontend/src/lib/api.ts` | `POST /api/dream/recommend` 에 `sets_per_tier=5` 를 **명시적으로** 보낸다. 계약 기본값은 10 이고 tier 가 셋이면 최대 30개 조합이 화면에 쏟아진다. 값을 넘기지 않으면 백엔드가 기본값을 바꿀 때 화면이 조용히 달라진다
2026-07-09 | 기록 | `index.md` | Phase 3(frontend) 구현 완료 기록. 375px 가로 스크롤·Lighthouse 는 미실측(헤드리스 브라우저 의존 라이브러리 설치에 sudo 필요)
2026-07-09 | 연결 | `10-contracts/env-vars.md` · `db-schema.md` | **시크릿 유출 함정이 하루에 두 번 나왔다.** 워커의 Alembic(예외에 접속 URL 평문)과 백엔드의 pydantic(`ValidationError` 가 `.env` dict 전체 출력). 공통 원인은 라이브러리가 실패를 설명하려고 입력값을 그대로 출력하는 것. 두 함정을 상호 링크하고 "새 설정 로더를 붙일 때마다 일부러 실패시켜 출력에 값이 섞이지 않는지 확인한다" 는 규칙을 남김
2026-07-09 | 갱신 | `index.md` | Phase 2(backend) 구현·검증 완료 기록. "`.env_*` 는 준비됐다" 를 정정 — `backend/.env_backend` 만 미비하며 그것이 유일한 잔여 차단 항목
2026-07-09 | **계약 변경** | `10-contracts/worker-jobs.md` · `env-vars.md` | **`news` 신선도 필터를 당일 → 최근 N일로 완화** (`NEWS_SAME_DAY_ONLY` 삭제, `NEWS_MAX_AGE_DAYS=1` 신설). 사용자 지시: "뉴스량도 많지 않으니 하루 전 것도 담자". 부수 효과로 당일 필터의 구멍이 닫혔다 — 마지막 실행~자정 발행분이 다음날 '어제 기사' 로 들어온다. 미래 날짜는 버린다(나이가 음수가 되어 통과해버린다)
2026-07-09 | **계약 변경** | `10-contracts/worker-jobs.md` · `env-vars.md` | **`lotto` 크론을 토 20:40·20:50·21:00 3회로.** 추첨 방송은 20:35 시작이고 번호 추첨이 끝나는 시각이 회차마다 다르다. 먼저 결과가 보이는 실행이 가져가고 나머지는 0건 성공으로 끝난다. **다중 크론 문법 신설** — `;` 로 이은 표현식을 `OrTrigger` 로 합친다. 표준 크론 한 줄로는 세 시각을 못 쓴다(`40,50,0 20,21 * * sat` 은 곱집합이라 여섯 번 돈다). 잡·락·`job_nm` 은 하나로 유지, 트리거만 여럿
2026-07-09 | 함정 | `10-contracts/worker-jobs.md` | ★ **APScheduler 의 요일 숫자는 표준 크론과 하루 어긋난다.** APScheduler 는 `0=월…6=일`, 표준 크론은 `0=일…6=토`. 계약이 `0 21 * * 6` 을 "토 21:00" 이라 적어 왔으나 실제로는 **일요일 21:00** 이다. 이식 원본(`backend/app/scheduler.py:71`)은 `CronTrigger(day_of_week="sat")` 로 옳았는데 `from_crontab` 문자열로 옮기며 어긋났다. 크론이 아직 발화한 적 없어 피해는 없다. → **요일은 이름(`sat`)으로 쓴다.** 워커가 숫자 요일에 경고 로그를 남긴다
2026-07-09 | 보강 | `10-contracts/worker-jobs.md` | 잡 성공 시 예약된 `lotto` 재시도를 취소한다(20:40 이 죽어 21:40 이 걸렸는데 20:50 이 성공한 경우). 0건 수집이 이어져 최신 회차가 **8일 이상 낡으면** 경고 로그. 7일이 아닌 이유는 토요일 밤 추첨 직전이 정상적으로 7일이기 때문. `OrTrigger` 로 합치면 어느 표현식이 발화했는지 알 수 없어 "21:00 에 0건이면 경고" 로는 판정할 수 없다 — 시각이 아니라 데이터의 낡음으로 판정한다
2026-07-09 | 수정 | `worker/worker/config.py` | 백엔드 세션이 기록한 [[env-vars]] 의 **pydantic ValidationError 가 `.env` 를 쏟는 함정**이 워커에도 있었다. `field_validator` → `model_post_init` + `RuntimeError` 로 교체. 가짜 env 로 재현해 트레이스에 시크릿이 없음을 확인. 워커는 같은 부류의 함정(Alembic 이 URL 을 평문 출력)을 이미 겪었다 — 라이브러리는 실패를 설명하려고 입력값을 그대로 출력한다
2026-07-09 | **계약 변경** | `10-contracts/env-vars.md` | **backend 의 DB 접속을 `DATABASE_URL` 한 줄에서 `PG_HOST`/`PG_PORT`/`PG_DB`/`PG_USER`/`PG_PASSWORD` 조각으로 바꿨다** (사용자 지시). 이유 셋: ① URL 이면 사람이 비밀번호를 퍼센트 인코딩해야 하고 잊으면 엉뚱한 이유로 실패한다 ② 비밀번호가 든 문자열이 통째로 돌아다녀 예외 메시지에 실린다(워커의 Alembic 이 그랬다) ③ 워커의 `.env_worker` 와 키 이름이 달라 대조가 안 된다. 조립은 `psycopg.conninfo.make_conninfo` 가 하므로 이스케이프는 라이브러리의 일이 된다. 영향: backend 만. `config.py`·`db.py`·테스트 3건·README 갱신, `env.sample` 은 사용자가 이미 수정
2026-07-09 | 추가 | `backend/app/db.py` | **기동 시 롤 자가검증.** `SELECT has_table_privilege(current_user,'lotto_draw','INSERT')` 가 true 면 기동을 거부한다. `.env_backend` 에 쓰기 자격증명이 들어가도 서버는 증상 없이 잘 뜨고, 그 순간 권한 경계(ADR 0003)가 조용히 사라지기 때문이다. 증상 없는 사고를 시끄러운 기동 실패로 바꾼다. 테이블이 없으면(마이그레이션 전) 판단을 보류하고 경고만 남긴다
2026-07-09 | **정정** | `10-contracts/env-vars.md` · `index.md` | **앞선 "`backend/.env_backend` 에 `app_writer` 자격증명이 들어 있다" 는 기록은 틀렸다.** pydantic 이 유출한 파편에 `app_writer` 라는 문자열이 보였으나 그것은 롤 이름이 아니라 `app_reader` 비밀번호의 부분 문자열이었다. 실접속 결과 `current_user = app_reader`, `lotto_draw` INSERT 권한 없음 — 권한 경계는 처음부터 온전했다. 진짜 문제는 계약이 `DATABASE_URL` 을 요구한 반면 파일에는 `PG_*` 가 있었던 **키 이름 불일치**였다. 교훈: 읽지 않기로 한 파일의 내용을 유출된 파편으로 추론하지 않는다. 확인할 방법을 코드로 만든다(위의 롤 자가검증). 노출된 값은 읽기 전용 롤의 비밀번호이므로 `ALTER ROLE app_reader PASSWORD ...` 교체를 권한다
2026-07-09 | 검증 | `backend/**` | 개발 DB(`prod_db`, 1,231회차·뉴스 75건)에 직접 붙어 **전 엔드포인트 200**. 테스트 67개(단위 40 + 통합 27) 전부 통과, skip 없음. 기동 로그: "접속 롤 'app_reader' 의 쓰기 권한 없음을 확인했습니다"
2026-07-09 | ★ **발견** | `90-external/dhlottery-blocked.md` | **`search.naver.com/robots.txt` 가 `User-agent: * / Disallow: /` 전면 금지다.** 현재의 위젯 파싱이 여기 해당한다. 역설적으로 정당한 원본 `dhlottery.co.kr` 의 robots.txt 는 `/resources/`·`/winImages/` 만 금지해 나머지를 허용하는데, 그 사이트는 여전히 접근을 차단한다(공식 API → 메인, `gameResult.do` → `/errorPage`. 재확인함). 페이지를 `status: draft` + `tbd` 로 강등
2026-07-09 | ★ **정정** | `90-external/dhlottery-blocked.md` · `10-contracts/db-schema.md` | **"위젯에 등위별 정보가 없다" 는 사실이 아니었다.** 1230회차 HTML 을 직접 파싱해 확인: 등위별 1~5등 총당첨금·당첨게임수·1개당 당첨금, 자동/수동/반자동 분포, **1·2등 배출점 상호·주소**까지 모두 제공한다(출처 표기 `제공 동행복권`). 따라서 `lotto_prize` 가 비어 있는 이유는 "소스가 없어서" 가 아니라 **"수집 정책이 미결이어서"** 로 바뀐다. 여전히 없는 것은 `total_sell_amt` 와 `first_accum_prize_amt` 뿐
2026-07-09 | **중단** | `lotto_draw` 백필 | **사용자 지시로 즉시 중단.** 253/1,231회차(908회까지) 채운 상태. 기술적 실패가 아니다 — `429`·`403`·연속실패·번호불일치 전부 0건이었고, robots.txt 를 확인한 뒤의 **정책 판단**이다. 작업은 멱등(`COALESCE`)이라 정책이 정해지면 이어서 돌릴 수 있다. 백필된 값은 1등 당첨금 7.4억~62.6억(평균 22.5억), 당첨자 3~24명으로 실제 분포와 부합
2026-07-09 | **보류** | 수집 범위 확장 | 등위별(`lotto_prize`)·판매점 수집을 **시작하지 않는다.** 소스에 데이터가 있다는 것과 담아도 된다는 것은 다르다. 사람이 판단할 것: 네이버 약관·robots 위반 위험(판례 잡코리아 v 사람인, 야놀자 v 여기어때), **저작권법 제91~98조 데이터베이스제작자 권리**의 "상당한 부분" 해당 여부, 판매점 상호·주소의 영업 관련성, 동행복권 정식 문의. **"공개정보이므로 자유이용" 은 성립하지 않는다** — 공개 여부와 이용 권리는 별개다
2026-07-09 | 기록 | `10-contracts/db-schema.md` | `lotto_prize` 를 채우기로 결정하면 **용어를 먼저 정리한다.** 동행복권·위젯은 "당첨게임 수" 를 쓰는데 `winner_cnt`(당첨자수)와 엄밀히 다르다 — 한 사람이 여러 게임을 살 수 있다. 컬럼 코멘트 반영 또는 컬럼명 변경(계약 변경)
2026-07-09 | 정정 | `20-design/components.md` · `30-seo/adsense-readiness.md` | **틀린 규칙 수정.** "미승인 상태에도 광고 슬롯의 min-height 를 예약한다" 는 규칙이 홈의 "주요 통계 ↔ 오늘의 추천 번호" 사이에 200px 짜리 빈 구멍을 만들었다(사용자 발견). 광고가 없으면 늦게 도착할 것도 없어 CLS 가 발생하지 않는다. 승인 시점에는 어차피 재빌드한다. `AdSlot` 은 미승인 시 `null` 을 반환한다
2026-07-09 | 정정 | `20-design/design-tokens.md` | **시안을 반대로 읽고 있었다.** 홈 6타일은 "파스텔 배경 + 컬러 글리프" 가 아니라 **진한 그라디언트 + 흰 글리프**다. `--svc-{s}` 는 타일 배경, `--svc-{s}-bg` 는 가이드 카드 파스텔 배경으로 용도를 분리하고 `--svc-fg`(흰색)를 추가했다. 그라디언트는 새 hex 없이 `color-mix` 로 유도한다
2026-07-09 | 구현 | `frontend/src/lib/fonts.ts` | **Pretendard 자체 호스팅.** 원본 가변 폰트 2MB → KS X 1001 상용 한글 2,350자 + 라틴으로 서브셋, `wght` 축을 400~800 으로 좁혀 **304KB**. 소스의 UI 문구 677자가 전부 범위 안임을 확인. `next/font/local` 이 preload 와 폴백 메트릭 오버라이드(size-adjust)를 자동 처리한다. 라이선스 SIL OFL 1.1 동봉
2026-07-09 | 구현 | `frontend/src/components/icons.tsx` | **이모지 아이콘 제거.** 📊 ★ ☁ 📰 ⚙ 는 OS·폰트마다 모양과 색이 달라 브랜드 색을 따르지 못한다. `currentColor` 를 쓰는 인라인 SVG 6종으로 교체. `accent` 값 하나가 색 토큰과 아이콘을 동시에 고른다. `6/45` 글리프는 SVG mask 로 흰 원에서 글자를 도려내 배경 그라디언트가 비치게 했다
2026-07-09 | 구현 | `frontend/src/components/HeroArt.tsx` | 히어로 일러스트를 인라인 SVG 로. 볼 색이 `--ball-*` 토큰을 참조하므로 **공식 5구간 규칙과 영원히 일치**한다 — 시안의 3D 렌더는 7번을 파랑으로 칠했지만 1~10은 노랑이다([[0009-official-ball-colors-over-mockup]]). 다크 모드도 자동으로 따른다
2026-07-09 | 발견 | `frontend/` | **OG 이미지와 파비콘이 없었다.** 공유 시 빈 카드가 나가고 브라우저 탭에 기본 아이콘이 뜬다. `public/og-image.png`(1200×630, 공식 색 볼 + 면책 문구)와 `src/app/icon.svg` 를 만들어 넣고 `metadata.openGraph.images` 에 연결
2026-07-09 | 생성 | `docs/raw/요청이미지정보.md` | 사람이 만들어 줄 이미지 스펙(히어로 3D 렌더·로고·가이드 카드 일러스트·애플 터치 아이콘). 전부 **없어도 화면은 완성 상태로 동작**하며, 코드로 그린 대체물이 들어가 있다. `docs/raw/` 는 읽기 전용이지만 사용자가 이 경로를 지정했다
2026-07-09 | ★ **데이터 오류 발견·교정** | `lotto_draw` · `50-ops/migration-sqlite-to-postgres.md` | **이관 원본 `backend/data/lotto.db` 가 1070·1071회 당첨번호를 서로 맞바꿔 갖고 있었다.** 실제: 1070회(2023-06-03) `3,6,14,22,30,41`+36 / 1071회(2023-06-10) `1,2,11,21,30,35`+39. 독립 소스(당시 언론 보도)와 네이버 위젯이 일치했고 DB 만 달랐다. Postgres 를 교정했다(원본 SQLite 는 읽기 전용이라 손대지 않음 — 재이관 시 오류가 되살아나므로 롤백 절에 교정 SQL 을 남겼다)
2026-07-09 | ★ **교훈** | `50-ops/migration-sqlite-to-postgres.md` | **이관 검증 5쿼리가 이 오류를 전부 통과시킨다.** 행수·회차 연속성·오름차순 불변식·번호 범위·추첨일 유일성 모두 정상이다. **구조는 완벽한데 내용이 틀렸다.** 구조적 무결성과 사실적 정확성은 별개이며, 후자는 독립된 소스와 대조해야만 확인된다. 오류가 드러난 유일한 이유는 백필이 **"위젯 번호가 DB 와 일치할 때만 UPDATE"** 라는 방어를 두었기 때문이다. 그 방어가 없었다면 1070회 행에 1071회 당첨금이 조용히 박히고, 틀린 번호를 예측 모듈과 회차 상세 페이지가 영원히 사실로 제공했을 것이다
2026-07-09 | 완료 | `lotto_draw` 1등 정보 백필 | **1,231회차 전부 `first_prize_amt`·`first_winner_cnt` 채움. NULL 0건.** 사용자가 "등위별·판매점은 수집하지 않고 1등 정보 백필만 재개" 로 결정. 조회 실패·`429`·`403` 0건. 당첨금 `0원` 14회차는 전부 당첨게임수도 0 — 1등 당첨자 없이 이월된 실제 회차다(`0` 과 `NULL` 을 섞지 않는다). 전 회차가 위젯과 번호 대조를 거쳤다(백필 대상 1,204 + 대상 아니던 1205~1231회 27개 별도 대조)
2026-07-09 | 검증 | `lotto_draw` 1070·1071회 | 교정 후 재처리해 1등 정보를 채웠고 값이 당시 언론 보도와 일치했다 — 1070회 `1,859,116,929원/14명`, 1071회 `5,183,979,750원/5명`. **교정이 옳았다는 독립적 확인이다**
2026-07-09 | 수정 | `worker/worker/sources/naver_widget.py` | **위젯의 정본 표기 `1등 당첨금 N원 (당첨게임 수 M개)` 를 파싱하지 못했다.** 기존 정규식은 옛 문구(`1등 당첨자는 총 N명`)만 찾아 `first_winner_cnt` 가 NULL 로 남는 회차가 있었다. 1등 당첨금 **바로 뒤의 괄호만** 읽는다 — 컨텍스트를 묶지 않으면 아래쪽 "등수별 당첨금액" 표의 2~5등 당첨게임 수를 주워 담는다. 최근 회차에는 값이 항상 있어 백필이 1회차에 닿고서야 드러났다
2026-07-09 | 함정 | 1회성 백필 스크립트 | `psycopg` 에서 `%(cnt)s IS NOT NULL` 은 값이 `None` 이면 `AmbiguousParameter` 로 죽는다. 옛날 회차에 당첨게임수가 없어 발생했고, 값이 항상 있던 최근 구간에서는 드러나지 않았다. `%(cnt)s::integer` 로 명시 캐스팅한다
2026-07-10 | **결정** | `90-external/naver-search-api.md` | **뉴스 썸네일을 긁지 않는다.** 검색 API 응답에는 이미지 필드가 없어(`title`·`originallink`·`link`·`description`·`pubDate` 뿐, 실측 확인) 기사 페이지의 `og:image` 를 스크래핑해야 하는데, ① `news.naver.com`·`n.news.naver.com` 은 `Disallow: /` 이면서 **`FacebookExternalHit`·`Twitterbot` 만 `Allow`** — "og:image 는 SNS 미리보기용으로만 연다" 는 명시적 의사표시다. 가져오려면 UA 사칭이 필요하고 이는 의도적 우회다 ② `link` 는 네이버 뉴스와 언론사 원문이 섞여 오며 정책이 제각각이다(`donga.com` 은 `/news/View` 금지) ③ **보도사진은 명백한 저작물**이라 "사실은 저작물이 아니다" 는 방어가 없고, `og:image` 핫링크는 언론사 대역폭을 쓴다 ④ 이 페이지의 "원문 미복제" 원칙과 충돌한다. 대안: 썸네일 없는 텍스트 카드 또는 자체 제작 그래픽(프론트 영역). 필요하면 언론사 정식 제휴
2026-07-10 | 교체 | `frontend/public/hero-lotto-machine.*` | 사용자가 제공한 3D 렌더를 히어로에 적용(인라인 SVG 대체). 원본은 **흰 배경 RGB** 여서 배경을 투명화해야 했다 — 다크 모드에서 흰 사각형이 뜬다. 단순 흰색 제거는 볼 안 흰 원판과 `LOTTO` 글자를 지운다. **테두리와 연결된 흰 성분만** 배경으로 판정하고(연결성분 분석), 본체 밖의 무채색 밝은 덩어리(버블 9개)를 지우되 유색 색종이 9개는 남겼다. 840×742 로 축소, WebP 75KB + PNG 545KB 를 `<picture>` 로 제공. **이 그림의 볼 색은 공식 5구간과 일치한다**(7 노랑·14 파랑·24 빨강·33 회색·42 초록)
2026-07-10 | 조정 | `frontend/src/components/LatestRoundCard.tsx` | 당첨결과 카드의 볼을 `lg` → `md` 로 축소. **값이 `null` 인 지표는 칸을 그리지 않는다** — `총 판매금액` 은 수집 소스에 없어 항상 `-` 만 표시됐다. '-' 만 적힌 상자는 자리만 차지하고 아무것도 알려주지 않는다. 소스가 확보되면 칸이 자동으로 나타난다(계약이 "언젠가 채워질 수 있음을 전제" 라고 한 것의 구현). 회차 상세도 같게 고쳤다
2026-07-10 | 구현 | `frontend/src/components/NewsList.tsx` | 뉴스 썸네일을 글리프로 채웠다. 백엔드가 기사 이미지를 주지 않는다(계약의 기사 객체에 이미지 필드 없음). 색·아이콘은 기사 링크 해시로 고르되, **해시만 쓰면 한 화면에서 색이 겹친다**(실제로 `reco, dream, reco` 가 나왔다) — 이미 쓴 색이면 다음 칸으로 밀고, 팔레트 5색을 다 쓴 뒤에는 직전 기사와만이라도 다르게 한다. 20건 목록에서 인접 중복 0건 확인
2026-07-10 | 조정 | `frontend/src/styles/components.css` | 홈 3분할 카드(최신 회차·D-day·뉴스)의 세로 길이를 약 20% 축소. 카드 패딩·제목 여백·지표 상자·카운트다운·뉴스 썸네일을 각각 한 단계씩 조였다. `.split-3 > .card` 로 한정해 3분할이 아닌 곳(회차 상세 등)의 자식에는 영향이 없다
2026-07-10 | 버그 | `frontend/src/styles/components.css` | **순위 막대가 그려지지 않았다**(사용자 발견). `.rank-bar-fill` 이 `<span>` 인데 `display` 를 지정하지 않아 인라인 요소가 되었고, 인라인 요소에는 `width`/`height` 가 적용되지 않는다. `.rank-bar` 는 grid 자식이라 자동으로 blockify 되어 회색 트랙만 살아남았다. 둘 다 `display: block` 명시
2026-07-10 | 버그 | `frontend/src/components/stats.tsx` | 막대를 살리자 두 번째 문제가 드러났다. **"안 나온 번호" 막대가 자기들끼리 정규화되어 꽉 차 보였다** — 적게 나왔다는 사실이 시각적으로 뒤집힌다. `RankList` 에 `max` 를 넘겨 HOT 최댓값을 공통 척도로 쓰게 하고 `tone="cold"` 로 색도 구분했다(회수 숫자가 옆에 있으므로 색이 유일한 전달자는 아니다). 홈 카드·hot-cold 상세·빈도 탐색기 세 곳에 적용
2026-07-10 | 기록 | `10-contracts/db-schema.md` | **통계 테이블을 두지 않는 이유**를 명문화. 빈도·HOT/COLD·패턴은 워커가 미리 계산해 저장하지 않고 백엔드가 요청마다 집계한다. 실측: 전 회차 1,231회 빈도 집계 **1.54ms**, 테이블 **248kB**, 주당 1행 증가. 미리 계산하면 ① `window`×`include_bonus`×통계3종 조합이 폭발하고 회차 추가마다 전부 재계산 ② 백엔드는 `app_reader` 라 못 쓰므로 워커가 써야 하는데 워커의 경계는 "계산하지 않는다" — 통계 로직이 두 곳으로 갈라지면 화면과 API 의 숫자가 달라졌을 때 판정 불가 ③ "새 회차 + 옛 통계" 를 보는 창이 생긴다. 무거워지면 백엔드 캐시가 첫 선택지
2026-07-10 | 제거 | `frontend/src/app/page.tsx` | 홈 3분할 아래의 "최근 N회 기준 X번은 …" 해석 문장을 제거(사용자 요청). 초안 6.1 이 요구한 "짧은 해석 문장" 은 홈 하단 본문(`행운상자는 어떤 서비스인가요?`)이 이미 충족한다 — 저가치 페이지 방지라는 목적은 그 본문이 담당한다
2026-07-10 | 버그 | `frontend/src/components/LottoBall.tsx` | **당첨번호/보너스 캡션이 볼과 어긋나 있었다.** 캡션 두 개를 한 줄에 두고 `justify-content: space-between` 으로 밀어 놓았기 때문이다 — 볼 개수·크기가 바뀌면 라벨이 따라오지 못한다. 볼을 그룹으로 묶고 각 그룹 아래에 캡션을 붙이는 구조(`BallRow captions`)로 바꿨다. 회차 상세도 같게 적용
2026-07-10 | 조정 | `frontend/src/styles/components.css` | **주요 통계 4카드가 필요 이상으로 길었다.** grid 의 기본값 `align-items: stretch` 때문에 내용이 적은 카드가 가장 긴 카드 높이로 늘어나 하단에 빈 공간이 생긴다. `align-items: start` 로 각 카드가 자기 내용만큼만 차지하게 하고, 4열이라 좁은 자리라서 패딩도 한 단계 조였다
2026-07-10 | 조정 | `frontend/` | 다음 추첨 카드의 회차 번호를 브랜드 색 굵은 글씨로 강조하고 카운트다운을 아래로 내렸다. "안 나온 번호" 캡션을 `최근 N회 기준 출현횟수가 적은 번호입니다` 로 교체. 패턴 분석 카드는 라벨이 곧 "무엇을 세었는가" 라는 정보이므로 `KeyValueList strongLabels` 로 라벨을 강조했다(조합 성향처럼 값이 주인공인 곳에서는 끈다)
2026-07-10 | 발견 | `frontend/src/components/RecommendCarousel.tsx` | **UI 가 오해를 만들고 있었다.** "완전 랜덤" 카드 아래에 `홀짝 1:5 · 합계 153` 을 라벨 없이 붙여 두니, 랜덤이 그 값을 **맞추려고** 뽑은 것처럼 읽힌다(사용자가 실제로 그렇게 읽었다). 실제로는 뽑은 뒤에 센 결과다. 숫자 위에 `조합 성향` 라벨을 붙이고, 제목 옆 ⓘ 버튼으로 설명 대화상자를 열게 했다
2026-07-10 | 생성 | `frontend/src/components/Modal.tsx` · `useModalBehavior.ts` | 공용 대화상자. 헤더 드로어·검색 오버레이에 있던 포커스 트랩 훅을 추출해 공유한다. **`Esc` 는 어떤 경우에도 막지 않는다** — "닫기 버튼으로만 닫힌다" 는 요구가 있어도 키보드 사용자가 갇히면 안 된다. 막는 것은 배경 클릭(`dismissible={false}`)이다
2026-07-10 | 생성 | `frontend/src/components/GenerateProgress.tsx` | 번호 생성 진행 대화상자(0s 준비 → 1s 시작 → 2s 조합 → 3s 완료 → 3.5s 닫힘). **백엔드가 200ms 만에 답해도 단계를 건너뛰지 않고, 4.2초 걸리면 완료 문구는 결과가 온 뒤에만 뜬다** — 진행 표시가 거짓말을 하면 안 된다. 타이밍 로직을 세 시나리오로 시뮬레이션해 검증
2026-07-10 | 조정 | `frontend/src/styles/` | 당첨번호+보너스 중앙 정렬. 카운트다운 글자 확대(1.25→1.4rem). 통계 4카드는 키를 맞추되(`stretch`) 하단 설명을 `margin-top: auto` 로 바닥에 붙여 네 카드의 설명 줄이 한 선에 정렬되게 했다. 섹션 보조설명의 숫자에 갈색 강조 토큰 `--color-accent-num` 추가(다크 오버라이드 포함)
2026-07-10 | 조정 | `frontend/src/styles/components.css` | 히어로 축소. eyebrow 를 키우고(0.95→1.1rem) 헤드라인을 낮춰(`--fs-hero` 최대 2.6→1.85rem) 둘의 대비를 줄였다. 헤드라인은 절 단위 `inline-block` 으로 묶어 좁은 화면에서 "로또의 모든 정보," / "여기서 확인하세요" 로만 끊기게 했다. 강조 색은 `정보` 와 `여기서 확인` 두 절에. 히어로 세로 패딩과 일러스트 폭을 함께 줄여 첫 화면이 히어로로 다 차지 않게 했다
2026-07-10 | 변경 | `frontend/src/components/HowRecommendWorks.tsx` | 설명 버튼을 아이콘(ⓘ)에서 **글자가 보이는 pill 버튼**("추천 방법은? (전략 설명)")으로 교체(사용자 제공 시안 `docs/raw/설명버튼예시.png`). 아이콘만 있는 버튼은 눌러 볼 이유를 만들지 못한다. 시안의 🤔 이모지는 OS 마다 모양이 달라 SVG 로 대체했다(홈 6타일에서 이모지를 뺀 것과 같은 이유)
2026-07-10 | 개선 | `frontend/src/components/HowRecommendWorks.tsx` | 설명 대화상자 가독성 재설계. 긴 문단을 걷어내고 다섯 방식을 색점 목록으로, "홀짝 4:2" 를 **실제 볼 6개에 홀/짝 배지를 달아 세어 보이는 그림**으로 바꿨다. "없습니다" 한 마디를 큰 글씨로 세우고, 이 도구의 입장은 강조 박스로 분리했다. 대화상자는 정독하는 곳이 아니라 훑어보는 곳이다
2026-07-10 | 생성 | `frontend/src/lib/share.ts` · `components/NumberActions.tsx` | 추천 번호 **복사 · 이미지 저장 · 공유**(초안 10.3). 홈 캐러셀은 복사만(훑어보는 자리, 카드 280px), `/lotto/recommend` 는 셋 다(번호를 가져가려고 온 자리). 공유는 **Web Share API** + 클립보드 폴백 — 카카오 SDK 는 앱키·외부 스크립트·개인정보처리방침 갱신을 요구하는데 OS 공유 시트에 이미 카카오톡이 있다. 이미지는 1080×1080 canvas 직접 그리기(라이브러리 0). **공유 문구와 이미지 모두 면책 문구를 항상 포함한다**
2026-07-10 | 버그 | `frontend/src/lib/api.ts` · `app/lotto/round/[roundNo]/page.tsx` | **빌드가 백엔드를 포화시켜 추천 페이지가 폴백으로 구워지고 있었다.** `ensemble` 은 몬테카를로 50,000회라 단독 2.6초, 동시 2건이면 16.4초다(실측). 그런데 빌드가 회차 상세 1,231개를 동시에 SSG 하며 백엔드를 두드려, 5초 타임아웃을 넘겼다. 추천 타임아웃을 45초로 분리하고(조회는 5초 유지), **회차 사전 생성을 최근 300개로 제한**했다(나머지 ISR, 사이트맵에는 전부). 빌드 페이지 1,456→526, 타임아웃 0건. 교훈: 정적 생성은 백엔드에 부하를 옮기는 일이다 — 페이지 수가 아니라 **동시 요청 수**를 보고 상한을 정한다
2026-07-10 | 생성 | `frontend/src/components/LogoMark.tsx` | 헤더·푸터의 로고를 글자 마크("행")에서 **파비콘과 같은 도형**으로 통일. 브라우저 탭에서 본 아이콘과 페이지 안의 로고가 다르면 같은 사이트라는 감각이 약해진다. 색은 브랜드 고정값이다 — 파비콘은 OS 테마를 따르지 않으므로 다크 모드에서도 로고를 바꾸지 않는다. 도형을 고칠 때는 `src/app/icon.svg` 와 함께 고친다
2026-07-10 | 변경 | `frontend/src/components/NumberActions.tsx` | 홈 캐러셀 카드에도 **복사·저장·공유 셋을 모두** 배치(사용자 요청). '다시 생성'을 전폭으로 두고 그 아래 3열로 눕혔다. 카드 폭이 280px 이라 "이미지 저장" 이 들어가지 않아 라벨을 두 글자로 줄이되(`저장`), `aria-label` 에는 온전한 이름("… 번호 이미지로 저장")을 남겼다 — 아이콘이 라벨을 거들고 스크린리더는 전체 이름을 읽는다
2026-07-15 | 생성 | `docs/raw/002-작업지시서_두번째_보완.md` | 사용자의 거친 UI 수정 메모(`002-작업지시서_두번째.md`)를 001 보완서 형식(장·절 + 표 + 요구사항 역매핑 + 세션 3분류)으로 정제. 요구를 R1~R35 로 번호화하고, 네 관점(기획·UI/UX·40대+ 사용자·개발) 분석과 세션별 작업 분류를 담았다. **워커는 변경 0건**(뉴스·회차 원천이 이미 있어 통계 재구성·동반출현·뉴스 조회는 전부 백엔드 조회 시 계산), 백엔드 계약 변경 3건, 프론트 나머지 전부. `docs/raw/` 는 읽기 전용이나 사용자가 이 경로에 보완서를 두는 것을 지정했다(001 선례)
2026-07-15 | 생성 | `docs/raw/002-이미지제작요청서.md` | 가이드 상세 이미지용 AI 생성 요청서. 복붙용 영문 프롬프트 7종(가이드 헤더 4 + 재미있는사실 스팟 3), 저장경로·파일명·크기 명시. **실제 사람 배제**, 브랜드 팔레트 플랫 일러스트로 통일, 배경 투명(다크모드). 등수판정(R19)은 이미지 대신 `LottoBall` 컴포넌트로, 뉴스 썸네일은 계속 미사용
2026-07-15 | **계약 변경** | `10-contracts/api-contract.md` | 002 개편 3건(하위호환). ① `stats/hot-cold` 의 `hot`·`cold` 에 `appearance_rate`(count/rounds_analyzed, **확률 아님**)·`last_seen_round`(마지막 출현 회차)·`trend`(window 최근절반 vs 이전절반 비교, up/down/flat) 추가, `overdue` 에 `last_seen_round` 추가 — 주요통계 샘플 표의 출현비율·최근출현·추세 열. ② 신규 `GET /api/lotto/stats/pairs?window=&number=&top=`(동반 출현, `pair_affinity` 집계 노출). ③ `GET /api/news` 에 `keyword`(제목·요약·키워드 대소문자무시 포함)·`period`(`1w|2w|1m|3m|6m|all`, API 기본 all·화면 기본 1w) 추가. 원천 데이터가 이미 있어 **워커 변경 없음**, 백엔드만 조회 시 계산. 근거: 002 보완서 4·5장
2026-07-15 | 추가 | `frontend/env.sample` · `10-contracts/env-vars.md` | `PEXELS_API_KEY` 키+주석 추가. 실제 값은 사용자가 `.env_frontend` 에 넣음. **서버 전용 — `NEXT_PUBLIC_` 미부착**(키가 번들에 노출되면 안 됨). 상세페이지 이미지 빌드타임 큐레이션용
2026-07-15 | 결정 | `00-decisions/0011-build-time-image-curation.md` · `90-external/pexels-image-usage.md` | 상세페이지 이미지는 **빌드타임에 사람이 골라 `public` 에 커밋**(런타임 Pexels 호출 아님·워커 수집 아님). 이유: 클라이언트 호출은 키 노출, 서버 런타임은 SSG 와 무의미+쿼터, 외부 실시간 이미지는 CLS, 저작권은 사람이 커밋 전 검수해야 함. 뉴스 썸네일 미수집 결정과 정합. Pexels License 사실 정리(무료·상업사용·출처불요, 재판매/인물 제한) — **최종 저작권 판단은 사용자**. 인물 사진 배제
2026-07-15 | 갱신 | `20-design/components.md` | 002 반영: StatCard→**StatBoard**(탭 테이블 4탭+차트+인사이트 2분할, 주요통계_샘플.png), NewsThumb 아이콘 1종+색만, RecommendCarousel 모바일 세로 스택, Footer 면책 강조(줄바꿈·색·볼드), Header 네비 폰트 2pt·active 볼드, 추천 설명 대화상자 톤 완화·라벨 "5가지 방식이란?", GuideCard 상세 하단 상호네비·헤더 이미지
2026-07-15 | 갱신 | `20-design/responsive-rules.md` | 히어로 CTA 2버튼(AI 번호 조합 추천·꿈으로 번호찾기), 추천 캐러셀 **모바일 가로스크롤→세로 스택**(R12)
2026-07-15 | 갱신 | `40-domain/forbidden-expressions.md` · `lotto-rules.md` | UI 라벨 규칙 추가: 화면 문구 "COLD"→"안 나오던 번호"(내부 필드 `cold`/`overdue` 는 유지), 'AI' 표현은 예측 암시 없는 선에서 허용(권장 "AI 번호 조합 추천", 지양 "AI 로또번호 예측"). 추첨 시각 사용자 문구 "매주 토요일 오후 8시 35분경" 표준화
2026-07-15 | 검증 | worker 전면 | **V2 전면개편(bfb31c0) 이후 워커 재검증.** V2 는 backend·frontend 중심이라 워커 계약(worker-jobs·db-schema·env-vars·component-boundaries)·`worker/` 코드를 건드리지 않았음을 확인. 계약↔코드 정합(잡별 락·409 원자성·compare_digest·OrTrigger·요일숫자 경고·신선도 필터·8일 낡음 경고·당첨게임수 파싱), 단위(파서 5·뉴스정제 3·신선도 6케이스), 통합(기동·상태코드 202/401/404/409·잡별 락 독립성·collect_job_log), 데이터 정합성(이관검증 5쿼리·app_reader 권한) **전부 통과.** 코드 변경 없음
2026-07-15 | 실증 | `lotto_draw` | **워커 신규 회차 자동 수집이 실제와 일치함을 확인.** lotto 수동 트리거가 1232회(2026-07-11) 1건을 자동 수집했고, 독립 소스(머니투데이·이투데이)와 대조: `12,15,19,22,24,36`+3, 1등 11명·각 25억3326만원(`2,533,260,819원`) 정확 일치. `.env_worker` 의 `LOTTO_CRON` 이 `40,50 20 * * sat;0 21 * * sat` 로 갱신되어 **요일 숫자 버그(0 21 * * 6 → 일요일) 해소 확인** — 기동 시 요일 경고가 더 이상 뜨지 않는다
2026-07-15 | 구현 | `backend/**` | **002 개편 계약 3건 구현·검증 완료.** ① `stats/hot-cold` 의 hot·cold 에 `appearance_rate`(count/rounds_analyzed, 회차 없으면 0.0)·`last_seen_round`(역대 전체에서 찾은 마지막 출현 회차, 없으면 null)·`trend`(window 를 최근절반 vs 이전절반으로 나눠 비교, 홀수는 가운데를 최근쪽에, 2회 미만이면 flat) 추가, overdue 에 `last_seen_round` 추가 ② 신규 `GET /api/lotto/stats/pairs?window=&number=&top=` — 각 회차 6C2=15쌍 집계, number 필터 시에도 numbers 는 오름차순 유지 ③ `GET /api/news` 에 `keyword`(title·summary·keyword_list ILIKE, `%`/`_` 이스케이프)·`period`(1w|2w|1m|3m|6m|all, make_interval, published_dttm NULL 은 기간필터에서 자연 제외) 추가. count 와 list 가 `_news_where` 공통 빌더를 써 total 이 필터 후 건수가 되게 함. **워커·DB 무변경**(원천 데이터로 조회 시 계산). 개발 DB(1,232회차·뉴스 172건)로 실검증, 테스트 87개(단위 47+통합 40) 통과. 금지필드 없음 확인
2026-07-15 | 구현 | `frontend/**` | **002 개편 프론트 전량 구현**(R1~R35 중 프론트 담당분). Footer 면책 분할·색·볼드(R1~4), 네비 2pt·active 볼드(R5~6), 히어로 2버튼 "AI 번호 조합 추천"/"꿈으로 번호찾기"(R7), 추천 캐러셀 모바일 세로 스택(R12), 뉴스 아이콘 1종+색만(R13/32), 추천 팝업 친절체+라벨 "5가지 방식이란?"(R11). 화면 노출 "COLD" 전량 "안 나오던 번호"로(R9) — `strategies.ts` 라벨·`lotto/page`·`stat/hot-cold`·`HowRecommendWorks`. 내부 필드(`cold`/`cold_return`)는 유지
2026-07-15 | 구현 | `frontend/src/components/StatBoard.tsx` | **주요통계 4카드 → 탭 테이블+차트+인사이트 2분할**(R8~10, 주요통계_샘플.png). 탭 4종(많이 나온/안 나오던/미출현 기간/동반 출현), 표 열 순위메달·볼·횟수·비율·최근출현·추세(색+기호 동시, WCAG 1.4.1). **백엔드 신규 필드 폴백**: `appearance_rate` 없으면 count/rounds 계산, `last_seen_round`/`trend` 없으면 '—', `pairs` 없으면 탭 "준비 중". **인사이트 문장은 프론트가 백엔드 수치로 조립**(계약대로 — 금지표현 관리 용이). 구 `StatCards.tsx` 삭제
2026-07-15 | 구현 | `frontend/src/app/news/page.tsx` | 뉴스 조회 UI·페이징·출처(R29~33). **조회 조건을 URL 쿼리스트링으로 관리**해 각 결과를 SSR 한다 — CSR 로 갈아끼우는 것보다 SEO·공유·뒤로가기에 낫다. 화면 기본 `period=1w`(API 기본 all 과 구분). `NewsFilter`(클라)·`Pagination`(rel prev/next). searchParams 를 읽어 라우트가 dynamic
2026-07-15 | 구현 | `frontend/src/app/guide/**` | 가이드 5종 개편(R14~28). 공통: 하단 `GuideNav`(홈 GuideCard 재사용, 현재 항목 제외), 헤더 이미지 슬롯 `GuideHeroImage`(파일 없으면 onError 로 숨김 — 사용자가 나중에 넣음), 줄바꿈 가독성. `how-to-check`: 동행복권·네이버 검색 안내(R18), **등수 판정을 `RankExplainer`(LottoBall)로 그림**(R19 — 이미지 대신 컴포넌트), FAQ 상세조회 링크(R20), 추첨시각 "오후 8시 35분경"(R22). `prize-claim`: 수령처(농협 본점/지점)·서류·지급기한 1년·세율 22%/33%·인터넷 구매분(R23~25) — 웹서치로 동행복권 공식 사실 확보. `lotto-rule`: 타이틀 "로또의 재미있는 사실"(R26), 조합수 8,145,060·확률 비유·전조합 비용·해외 사례(R27). `responsible-lottery`: 도박문제 상담 1336 보강(R28)
2026-07-15 | 계약정합 | `frontend/src/lib/{api,api-types}.ts` | 002 계약 3건 반영. hot/cold `appearance_rate`·`last_seen_round`·`trend`(optional — 폴백), `overdue.last_seen_round`, `PairsResult`+`getPairs`, 뉴스 `keyword`·`period`+`getNews` 확장. 신규 필드는 optional 로 두어 백엔드가 아직 안 줘도 화면이 폴백. 이미지·PEXELS 는 사용자 검수 후 직접 투입([[pexels-image-usage]]) — 프론트는 슬롯만 예약
2026-07-15 | 검증 | `frontend/` | 002 목 백엔드(신규 필드 포함)로 빌드·렌더 검증. StatBoard 비율(65%)·최근출현(1182회)·추세(3종)·인사이트 문장 렌더 확인. 뉴스 조회폼·페이징·키워드 SSR 확인. 가이드 5종 사실(세율·1336·8145060) 렌더 확인. 화면 COLD 0건, 금지표현 0건("당첨 확률"→"1등이 될 가능성" 순화). 각 가이드 고유 title·canonical·Article JSON-LD 확인. **375px 는 실측 못 함**(헤드리스 크로미움 libXdamage 누락, sudo 필요) — 넓은 요소(stat-table 360px·표 480px·차트)가 전부 `.table-scroll`/`.bar-chart-scroll` overflow 컨테이너 안에 있고 body `overflow-x:clip` 안전망 존재함을 정적 확인
2026-07-22 | 반영 | `frontend/public/guide/*.{png,webp}` | 사용자가 002 이미지제작요청서 프롬프트로 만든 가이드 이미지 7종 투입. **검수 결과 내용은 통과**(브랜드 팔레트 플랫 일러스트, 인물·글자·로고 없음, 볼 색 공식 규칙 준수, 세트 일관성). 다만 원본이 **불투명 RGB·1MB대** 여서 두 문제를 고쳤다: (1) 다크모드 대비 배경 투명화 — 히어로 때 검증된 방식(밝고 채도 낮은 픽셀 + 모서리 연결 성분만 배경, 경계 밝기비례 반투명)으로 일러스트 내부 흰 요소(용지·구름·볼 중앙)를 보존, (2) 규약 크기(헤더 1200×675·스팟 400×400)로 리사이즈 + WebP 생성. 결과 WebP 31~135KB. `<picture>` 가 webp 우선·png 폴백. 라이트/다크 양쪽 합성 검수 완료. 원본은 job tmp 에 백업
2026-07-22 | 개선 | `frontend/public/guide/*.{png,webp}` | 가이드 이미지 재압축·투명화 마무리. 원본(불투명 RGB 8.4MB 합계)에서 다시 처리: 배경 투명화 시 경계 반투명 대신 **배경을 물체 쪽 1px 확장 + 알파 0.6px 블러**로 다크모드 경계 얼룩 제거, PNG 는 알파 보존 팔레트 양자화(200색, flat 일러스트라 밴딩 없음)로 축소. WebP 합계 **465KB(원본의 5%)**, `<picture>` webp 우선 서빙 확인. 라이트/다크 합성 검수 통과(lotto-facts 볼 splash 하단 미세 잔여 얼룩은 원본이 흰 splash 를 배경색으로 그린 탓, 실사용 라이트 배경에서 비가시)
2026-07-22 | 수정 | `frontend/src/styles/components.css` | StatBoard 2분할에서 **오른쪽 차트 패널이 왼쪽 탭 테이블 절반 높이**로 짧던 문제 수정(사용자 지적). 원인은 `.statboard { align-items: start }` — 각 패널이 자기 내용만큼만 차지. `stretch` 로 바꿔 두 패널 높이를 맞추고, 오른쪽 내부는 차트 래퍼·막대를 `flex:1`(데스크톱만, min 160px)로 늘려 **빈 여백이 아니라 실제 차트로** 채우고 인사이트 콜아웃은 바닥 정렬. 통계 상세 페이지 차트(96px)는 영향 없음. Windows 크롬 headless 스크린샷으로 두 패널 높이 일치 확인
2026-07-23 | 수정 | `frontend/src/components/FrequencyChart.tsx`(신규) · `StatBoard.tsx` · `FrequencyExplorer.tsx` · `stats.tsx` · `styles/components.css` | **StatBoard 차트 가로폭이 패널보다 길어 데스크톱 2분할 레이아웃이 틀어지던 문제 수정 + 막대 클릭/호버 시 "몇 번이 몇 회" 표시**(사용자 지적). 원인은 차트에 걸린 `min-width:640px` 가 StatBoard 우측 패널에서도 적용돼 패널을 밀어냈던 것. 빈도차트를 `stats.tsx` 에서 분리해 클라이언트 컴포넌트 `FrequencyChart` 로 만들고 **두 변형**을 뒀다 — `fit`(StatBoard: 45개 막대를 `flex:1` 로 패널 폭에 꽉 맞춰 가로 스크롤 제거) · `scroll`(통계 상세: 640px 로 넓게 펼쳐 자기 컨테이너에서만 가로 스크롤). 막대를 `<button>` 으로 바꿔 `onPointerEnter`/`onClick`/`onFocus` 로 선택, 차트 위 readout 에 "N번 M회 나왔어요"(터치도 읽게 고정 표시, `aria-live=polite`, CLS 방지 `min-height`). 막대·readout 볼 색은 동행복권 공식 5구간(`bar-range-*`). 방어적으로 `.statboard-left/right`·`.freq-chart` 에 `min-width:0`(grid/flex 아이템이 내용 때문에 트랙을 넘지 않게). 데스크톱 1280 스크린샷으로 차트가 우측 패널 폭에 정확히 맞고 좌우 패널 높이 정렬됨 확인. **모바일 375 실측은 Windows 크롬 headless 가 작은 window-size 를 무시하고 넓게 렌더해 불가** — mobile-first CSS(fit 막대는 flex 라 컨테이너 초과 불가)로 정적 확인
2026-08-12 | 검증 | `backend/**` | **V2(002) 이후 백엔드 전면 재검증.** 계약(`api-contract.md`)의 전 엔드포인트를 개발 DB(1,232회차·뉴스 172건)에 붙여 실측 — 응답 키 집합·정렬 규약(rounds 내림차순/sitemap 오름차순/뉴스 pub_date 내림차순/hot·cold 동점시 번호 오름차순)·`traits` 6필드(회차·꿈) vs 8필드(추천)·`counts` 1~45 전수·`appearance_rate = count/rounds_analyzed`·범위 밖 page 빈배열 200·없는 회차 404·`window=7`/`sets=99`/`number=46`/`period=9y` 422·금지 필드명 부재를 자동 검사해 **위반 0건.** 단위+통합 87개 통과, `sets=3&seed=1` 두 호출 바이트 동일. **코드 변경 없음.** 주의: 꿈해몽 첫 요청이 0.27초였으나 이는 8/5부터 떠 있던 개발 서버가 이미 warm 이었기 때문이고, "첫 요청 20초"([[dream-pipeline]])가 반증된 것이 아니다 — 읽지 않은 조건에서 나온 숫자로 문서를 고치지 않는다
2026-08-12 | ★ **원인규명** | `40-domain/prediction-algorithm.md` | **추천 API 가 동시 요청에서 n² 로 무너지는 원인을 찾았다.** 실측 1건 2.52s / 2건 16.35s(6.5배) / 4건 62.58s(24.8배). 구간을 나누니 비용의 **99.6%가 `montecarlo.simulate`**(2,522ms)이고 DB 5ms·분석기 4종 합계 4.6ms 였다 — **분석기 캐시는 무의미하다.** 원인은 5만 회 루프가 매 회 하는 **작은 numpy 호출**이다. 호출마다 GIL 을 놓았다 잡으면서 스레드 간 손바꿈이 계산보다 비싸진다(convoy). 대조 실험이 이를 분리한다: 순수 파이썬 루프 2.0/4.1배, 큰 numpy 연산 2.2/4.1배(둘 다 정상 = GIL 직렬화), **작은 numpy 호출 5만 회만 8.6/35배.** 프론트가 추천 타임아웃을 45초로 뺀 것(log 2026-07-10)은 이 증상을 원인 모른 채 피한 것이었다
2026-08-12 | **결정** | `00-decisions/0012-serialize-monte-carlo.md` | **몬테카를로 경로는 프로세스 안에서 한 번에 하나만 실행한다.** GIL 아래에서는 총 처리량이 하나분이라 동시 실행의 이득이 0 이고 convoy 손해만 남는다 — 그래서 막는 것이 이득이다(4건 62.6초 → 약 10초). **벡터화하지 않는 이유**: 예측 모듈 재작성 금지(`backend/CLAUDE.md`)이고, 추출 순서를 바꾸면 같은 `seed` 가 다른 조합을 낸다. **프로세스 풀이 아닌 이유**: 배포 형태 미정([[deployment]])인 개인 규모에 이르다. **캐시가 아닌 이유**: 분석기는 4.6ms 라 의미가 없고, `seed` 없는 요청은 계약이 매번 다른 결과를 약속한다. 몬테카를로를 타지 않는 전략 5종(6~14ms)과 다른 엔드포인트는 **제한하지 않는다** — 가벼운 요청이 2.5초 뒤에 줄 서면 안 된다
2026-08-12 | **계약 변경** | `10-contracts/env-vars.md` · `backend/env.sample` | backend 에 `RECOMMEND_MAX_CONCURRENCY`(기본 `1`) 추가. 기본값이 있어 `.env_backend` 에 없어도 기동하며, `0` 이하면 기동을 거부한다(조용히 모든 추천이 멈추는 것보다 시끄럽게 죽는 편이 낫다). 값을 여는 이유는 튜닝이 아니라 훗날 알고리즘·프로세스 모델이 바뀌었을 때 코드를 고치지 않고 되돌리기 위해서다. **backend 만 영향.** `env-vars.md` 는 `owner: shared` 라 원칙상 백엔드 세션의 소유가 아니다 — 워커 세션이 변수 11개를 추가한 선례(2026-07-09)를 따라 여기 기록하고 사용자에게 알린다
2026-08-12 | **결정** | `20-design/home-v2-concept.md`(신규) · `20-design/responsive-rules.md` · `30-seo/metadata-strategy.md` | **새 메인 시안 `/v2` 를 추가한다. 운영 홈(`/`)은 교체하지 않고 그대로 둔다** — 두 안을 나란히 보고 사용자가 하나를 고르기 위해서다. 사용자가 준 UI/UX 트렌드 글(브런치 2026-08-03)에서 이 제품에 값을 하는 것만 취했다: 3D 스택 캐러셀·리퀴드 글래스 내비·셰이더·모션 집중은 채택, **무한 캔버스 갤러리는 기각**(검색 유입 사이트에서 SSR·SEO·모바일에 전부 해롭고 보여줄 이미지 자산도 없다). 시그니처는 **45칸 격자 하나가 페이지를 관통하는 것** — 10열×5행이라 **행이 곧 볼 공식 5구간**이 되어 색-구간 매핑이 레이아웃으로 드러난다(행 라벨 `1–10` 이 색 단독 전달을 막는다). 사용자 선택: WebGL 허용 · v2 전용 글래스 헤더로 교체 · 정보구조 재구성 허용 · Pretendard 단독. **`responsive-rules` 의 홈 섹션 순서 고정 규칙에 `/v2` 예외를 명시**했고, 채택 시 그 절을 갱신하기로 했다
2026-08-12 | 규정 | `30-seo/metadata-strategy.md` | **비공개 프리뷰 라우트 규정 신설**(위키에 전례 없던 항목). 페이지 `metadata.robots` 의 `index:false` 가 정본이고, `alternates.canonical` 을 반드시 명시한다 — 생략하면 루트의 `canonical:'/'` 를 상속해 **그 페이지가 자기를 홈이라 주장한다.** `sitemap.ts` 는 화이트리스트라 손대지 않아도 자동 제외된다. ⚠ **`robots.txt` 에 `Disallow` 를 넣지 않는다** — 직관과 반대지만 크롤을 막으면 크롤러가 noindex 메타를 **읽지 못해** 링크가 하나라도 있을 때 본문 없이 URL 만 색인된다. 정확한 조합은 "크롤 허용 + 페이지 noindex"
2026-08-12 | 구현 | `frontend/src/app/v2/**`(신규 15파일) | **새 메인 시안 `/v2` 구현 완료. 기존 파일 수정 0건**(`git status` 에 `frontend/` 의 `M` 이 하나도 없음 — 디렉터리 하나를 지우면 원상복구된다). 전역 헤더는 `body:has(.hb-root) .site-header{display:none}` 로 이 라우트에서만 감춘다 — 특이도 (0,2,1) 이라 **CSS 로드 순서와 무관하게** 이기고, `display:none` 이라 스크린리더가 내비를 두 번 읽지 않는다(JS 로 body 클래스를 붙이는 방식은 FOUC+CLS 를 함께 만들어 기각). CSS 는 `v2/layout.tsx` 에서 글로벌 1장을 import — 세그먼트 청크로 묶여 **다른 라우트 HTML 에는 `<link>` 가 붙지 않음을 확인**(홈 CSS 1장, v2 2장). 기존 UI 컴포넌트는 재사용하지 않고 v2 전용으로 새로 만들었다 — 기존 것은 테마에 따라 값이 뒤집히는 토큰에 묶여 있어 다크 고정 화면에서 라이트 모드 사용자에게만 깨진다. `lib/` 순수 함수·상수는 그대로 재사용. 셰이더는 raw WebGL2(three.js 0KB), 폴백 CSS 그라디언트가 **기본값**이고 첫 프레임 성공 후에만 캔버스를 드러낸다. First Load JS 112KB 로 **운영 홈(119KB)보다 작다**
2026-08-12 | ⚠ **함정** | `frontend/src/app/v2/_styles/sections.css` | **grid 컨테이너에서 `min-width:0` 은 넘침을 못 막는다.** 375px 에서 45칸 격자·통계·버튼이 통째로 화면 밖으로 잘려 나갔다. 원인은 item 이 아니라 **트랙**이었다 — 열을 명시하지 않은 grid 의 암시적 트랙은 `auto` 이고, auto 트랙은 자식의 max-content 만큼 커진다(10열 격자의 max-content 가 375 를 훌쩍 넘는다). `min-width:0` 은 item 의 축소 거부만 풀 뿐 트랙 크기는 건드리지 못한다. **`grid-template-columns: minmax(0, 1fr)` 을 명시**해야 트랙이 컨테이너 폭에 묶인다. 열 미지정 grid 7곳을 일괄 보정했다. 전역 `html,body{overflow-x:clip}` 안전망이 스크롤바를 숨겨 **브라우저에서는 증상이 안 보인다** — 스크린샷을 눈으로 봐야 발견된다
2026-08-12 | ⚠ **정정** | `20-design/home-v2-concept.md` | **히트맵이 당첨 칸을 덮어 볼 공식 색을 왜곡하고 있었다**(데스크톱 실측에서 21~30 빨강이 보라로 읽혔다). 히트 레이어가 셀 배경 위에 겹치는 구조였기 때문이다. `.hb-cell[data-win] .hb-cell-heat{display:none}` 으로 **당첨 칸에는 히트를 씌우지 않는다.** 볼 색은 사용자가 색만 보고 구간을 아는 학습된 매핑이라 어떤 연출도 그 위에 올릴 수 없다([[0009-official-ball-colors-over-mockup]]). 히트 상한도 100%→70% 로 낮췄다 — 꽉 채우면 그 위 숫자 대비가 무너진다
2026-08-12 | ⚠ **함정** | `frontend/src/app/v2/page.tsx` | 세그먼트 설정은 **리터럴이어야 한다.** `export const revalidate = REVALIDATE.home` 은 `Unsupported node type "MemberExpression"` 으로 빌드가 거부된다 — 세그먼트 설정은 번들 이전에 정적 분석으로 읽히기 때문이다. 값의 정본은 `lib/api.ts` 의 `REVALIDATE.home` 이고 코드에는 `3600` 을 적되 주석으로 연결했다
2026-08-12 | ★ **해결** | `50-ops/` (도구) | **375px 실측이 가능해졌다.** 2026-07-23 에 "Windows 크롬 headless 가 작은 window-size 를 무시해 불가" 로 남긴 항목의 원인을 특정했다 — `--window-size=375` 를 줘도 **창이 512px 아래로 줄지 않는다**(측정: `VIEWPORT=512x1102`). 레이아웃은 512 로 그려지고 스크린샷만 375 폭으로 잘려 저장되므로 **정상 레이아웃이 잘린 것처럼 보인다.** 우회법: 512 창 안에 `width:375px` 인 `<iframe>` 을 띄운 로컬 HTML 을 캡처하면 iframe 안쪽이 정확히 375 CSS px 뷰포트가 된다(오른쪽에 마젠타 여백을 두어 폭을 눈으로 검증). `sudo` 도 크로미움 설치도 필요 없다. 이 방법으로 위 두 함정을 실제로 발견해 고쳤다
2026-08-12 | 검증 | `frontend/` | `/v2` 검증 완료. 빌드 라우트 표에서 `○`(정적·ISR), `noindex, nofollow` + `canonical=/v2` 확인, `<h1>` 1개, 45칸·당첨 6·보너스 1·히트값 45개가 **서버 HTML 에 존재**(JS 없이도 번호가 보인다). **백엔드를 끊고(`API_BASE_URL=127.0.0.1:9999`) 빌드해 폴백을 실측** — 요청 실패 49건 경고에도 빌드가 통과하고, 격자는 중립(마킹 0·히트 0), 4개 구역이 각각 폴백 문구를 렌더한다. 375px·1280px 스크린샷 육안 검수 통과, 금지 표현·금지 식별자 0건, `shell.css` 무조건 선택자 0건. **미실측: Lighthouse, 실제 GPU 에서의 WebGL 셰이더**(헤드리스는 소프트웨어 렌더러라 의도대로 폴백된다 — 그 폴백 화면은 검수했다)
2026-08-12 | 구현 | `backend/app/concurrency.py`(신규) · `config.py` · `domain/recommend.py` · `routers/recommend.py` | [[0012-serialize-monte-carlo]] 구현. 루프별 `asyncio.Semaphore`(기본 1)로 `HEAVY_STRATEGIES`(현재 `ensemble` 뿐)만 감싼다. **`threading.Semaphore` 를 쓰지 않은 이유**: 무거운 계산이 이미 `asyncio.to_thread` 안에 있어 그 안에서 잡게 하면 간단해 보이지만, 대기 스레드가 기본 실행기 슬롯을 쥔 채 잠들어 **같은 실행기를 쓰는 꿈해몽이 굶는다.** **세마포어를 모듈 전역에 두지 않은 이유**: Python 3.10+ asyncio 동기화 객체는 처음 쓰인 루프에 자기를 묶어, 테스트마다 새 루프를 만드는 pytest-asyncio 에서 두 번째부터 터진다 → 루프를 약한 참조 키로 하는 등록부(기본 루프·uvloop 모두 weakref 가능함을 확인). `HEAVY_STRATEGIES` 를 라우터가 아니라 `domain/recommend.py` 에 둔 것은 어떤 전략이 무엇을 계산하는지 아는 곳이 거기이기 때문. 대기 1초 초과 시 경고 로그. `RECOMMEND_MAX_CONCURRENCY` 가 0 이하면 기동 거부(조용히 멈추는 대신 시끄럽게 죽는다)
2026-08-12 | 검증 | `backend/**` | **동시 4건 62.58초 → 10.11초**(2건 16.35→4.95초). 1건 2.51초는 그대로 — 그것은 convoy 가 아니라 알고리즘의 값이다. **`seed=1` 응답이 수정 전 파일과 바이트 단위로 동일** → 알고리즘 무변경을 실측으로 입증했다. 계약 정합성 자동검사 위반 0건 재확인. 무거운 2건이 도는 중 가벼운 전략 4종이 54~113ms 로 응답(게이트가 넓게 걸리지 않았음). 테스트 94개 통과(신규 7: 겹침 측정의 유효성을 보이는 **대조군 포함**, 게이트 직렬화, 예외 시 슬롯 반납, 대기 경고 경로, 설정 검증, 라우터 수준 ensemble 미겹침, 가벼운 전략 미차단). **속도를 재는 테스트는 쓰지 않았다** — 기계 상태에 흔들려 회귀를 못 잡는다. 겹침 여부라는 이산적 사실만 본다
2026-08-18 | **결정** | `frontend/src/app/v2/` · `20-design/home-v2-concept.md` | **새 메인 시안 `/v2` 를 쓰지 않기로 했다** (사용자 지시). 백엔드 세션이 지시를 받아 여기 남긴다 — 세 세션이 병렬로 돌고 위키가 유일한 통신 수단이라, 기록하지 않으면 프론트 세션이 계속 v2 를 다듬는다(지시 시점에 `home-v2-concept.md` 가 편집 중이었다). **정리는 프론트 세션의 몫이다**(루트 규칙 5) — 백엔드는 `frontend/` 와 `owner: frontend` 페이지를 고치지 않는다. 남은 일: `frontend/src/app/v2/`(16파일) 제거, `home-v2-concept.md` 를 `deprecated` 로 전환하거나 삭제, `index.md` 의 v2 항목 2곳(20-design 목록·Phase 문단) 정리. 전부 `24ad827` 에 커밋돼 있어 되돌릴 수 있다. **백엔드 영향은 0 이다** — v2 는 API 를 한 번도 부르지 않고(호출 0건) `10-contracts/` 를 변경하지 않았다. 따라서 [[api-contract]] 에 손댈 것이 없다
2026-08-18 | **계약 변경** | `10-contracts/api-contract.md` | **003 통계 개편 — 백엔드 선행 작업.** 사용자가 `/lotto/stat` 3종 화면 전면 개편을 요청했고, 화면만으로는 만들 수 없는 것이 다섯 개라 계약을 먼저 열었다. ① **기간 조회**(`from_round`·`to_round`) — 전 통계 엔드포인트. 하나만 오거나 역순이면 `422`, 데이터 밖은 에러가 아니라 **교집합으로 자르고** 응답의 구간 메타에 잘린 실제 값을 담는다. ② 응답 최상위 **구간 메타 4종**(`from_round`/`to_round`/`from_date`/`to_date`) — 화면이 "1213~1232회 (2026.02.28~2026.07.11)" 을 그리려면 회차→날짜를 따로 왕복하지 않아야 한다. ③ `hot-cold` 의 `top`(기본 10 유지 = 하위호환, 화면은 15 기본에 10~40 선택). ④ 신규 `GET /api/lotto/stats/number/{n}` — **`rank` 때문에 필요하다.** "15번은 최근 50회 중 3위"를 만들려면 45개를 정렬해야 하는데 그 집계를 브라우저에서 하면 서버와 숫자가 갈라진다([[component-boundaries]]). ⑤ 신규 `GET /api/lotto/rounds/index`(회차-날짜 경량 목록, 페이지네이션 없음) + `pattern` 의 `sum_histogram`·`consecutive_counts`. **전부 추가만 하는 하위호환 변경**이고, 프론트는 응답에 `from_round` 키가 없으면 구버전으로 보고 기간 UI 를 잠근다 — 세 세션이 병렬로 도는 동안 새 필드가 없다고 화면이 깨지면 안 된다. 사용자 지시로 사전 승인 대신 이 기록으로 갈음한다
2026-08-18 | 결정 | `20-design/` · `30-seo/metadata-strategy.md` | **`/lotto/stat` 이 허브가 아니라 '많이 나온 번호와 안 나온 번호' 화면 자체가 된다.** 사용자가 "들어오면 일단 그 화면이 보였으면" 하고, 지금은 다른 통계로 가려면 뒤로가기를 해야 하는 것이 문제라고 지적했다. 그래서 (1) `/lotto/stat` 에 hot-cold 콘텐츠를 두고 `/lotto/stat/hot-cold` 는 그리로 **영구 리다이렉트**, (2) 통계 3종 어디서든 서로 이동하는 **탭형 내비**를 상단에 고정한다. URL 이 하나 줄지만 **아직 배포 전이라 색인된 URL 이 없어** 리스크가 없다. `sitemap.ts`·`STAT_PAGES`·메타데이터 전략을 함께 고친다
2026-08-18 | 결정 | `frontend/src/app/lotto/stat/**` | **기간 조회는 CSR 로 한다.** 통계 라우트는 ISR 이고 [[metadata-strategy]] 가 `searchParams` 사용을 금지한다(읽는 순간 dynamic 이 되어 중복 URL 이 색인된다). 임의 기간은 조합이 무한해 서버가 미리 구울 수도 없다. 그래서 **기본 화면(최근 20/50/100/전체)은 SSR 로 굽고, 사용자가 기간·순위 개수·번호를 바꾸면 브라우저가 `NEXT_PUBLIC_API_BASE_URL` 로 직접 조회**한다. 색인되는 것은 기본 화면과 설명 본문이고 그것은 서버가 렌더한다. `lib/api.ts` 에 `browser*` 계열 조회 함수를 추가한다(종전에는 추천·꿈해몽만 브라우저에서 불렀다)
2026-08-18 | 구현 | `backend/**` | **003 통계 개편 백엔드 5건 구현.** ① 기간 조회 — `select_range` **한 함수**가 window·기간을 모두 처리하고 다섯 엔드포인트가 `RangeQuery` 의존성 하나를 공유한다(구간 계산이 흩어지면 화면마다 "몇 회차를 봤는가" 가 달라진다). 하나만/역순은 422, 데이터 밖은 교집합 절단, 응답에는 **잘린 실제 값**을 담는다. ② 구간 메타 4종 — `Draw` 에 `draw_date` 를 실어(기본값 `None`, 예측 모듈은 날짜를 쓰지 않는다) 회차→날짜 왕복을 없앴다. ③ `hot-cold` 의 `top`(1~45, 기본 10). **`hot_cold_sets` 는 `top` 에 흔들리지 않게 고정** — 사용자가 top=40 으로 본다고 추천의 `hot_count` 정의가 따라 바뀌면 두 화면을 대조할 수 없다. ④ `GET /stats/number/{n}` — `rank` 는 hot 정렬과 **같은 규칙**을 쓴다. ⑤ `GET /rounds/index` — ★ **`/rounds/{round_no}` 보다 먼저 선언해야 한다**(FastAPI 는 등록 순서로 매칭해 `index` 를 회차 번호로 읽는다). ⑥ `pattern` 에 `sum_histogram`(비율이 아니라 회차 수 — 사용자가 `rounds_analyzed` 와 더해 검산할 수 있어야 한다)·`consecutive_counts`. **워커·DB 무변경**
2026-08-18 | **계약 보강** | `10-contracts/api-contract.md` | 구현하며 계약이 비어 있던 세 곳을 채웠다(백엔드 소유 페이지). ① **"빈 결과" 가 무엇을 비우는지 표로 확정** — 구간에서 나온 값만 비운다. `hot`·`cold`·`pairs`·`companions`·`recent_appearances` 는 빈 배열, `rank` 는 `null`, `counts` 는 1~45 키를 유지하고 값만 0. 반면 `overdue`·`last_seen_round`·`rounds_since`·`max_gap` 은 **역대 전체에서 나오므로 그대로 채워진다**. 구간에 데이터가 없다는 것과 그 번호의 역사가 없다는 것은 다른 말이다. 이 갈림은 구현 중 테스트가 잡아냈다 — 처음 구현은 45개를 0회로 채워 "1번 0회 · 1위" 를 내보내고 있었다. ② `rank` 가 `null` 이 되는 조건 명시. ③ **`/rounds/index` 크기를 추정이 아니라 실측으로 정정** — 문서가 "30KB 미만" 이라 적었으나 1,232회차 실측 **51KB** 다(JSON 은 회차마다 키를 되풀이한다). 여전히 충분히 가볍지만 틀린 수를 남기지 않는다
2026-08-18 | 검증 | `backend/**` | 계약 전수 자동검사 **위반 0건**(신규 8항목 + 기존 회귀 검사 별도 통과). 구간 메타 5엔드포인트 전수, 기간 조회 시 `window=null`, 하나만/역순 422 × 5엔드포인트, 데이터 밖 절단(`to_round=999999`→1232), 빈 구간 200·빈 hot/cold·살아 있는 overdue, `top` 1/15/45 와 0·46 의 422, **`rank` 가 `hot(top=45)` 순위와 상위 5개까지 일치**, `companions`≤5·자기 번호 제외, `recent_appearances` 최신순·≤20, `sum_histogram` 합 = `rounds_analyzed`, `1 - consecutive_counts["0"] == consecutive_ratio`, `/rounds/index` 오름차순·경량·**회차 상세가 잡아먹히지 않음**. 테스트 145개 통과(신규 51). 응답 시간 3.8~11ms. 검사 중 `pairs?window=20&number=7` 이 빈 배열이라 한 번 실패했으나 **코드가 아니라 검사식이 데이터를 가정한 것**이었다(7번이 최근 20회에 0회 출현). 데이터에 의존하지 않는 단언으로 교체
2026-08-18 | 검토 | `10-contracts/api-contract.md` | **소프트캡(400줄) 초과 — 분할하지 않기로 결정하고 트리거를 명문화.** 현재 485줄(하드캡 800의 61%). 절별로 재니 **통계 절 하나가 211줄로 43%** 이고 나머지는 전부 64줄 이하라 분할 지점은 이미 명확하다(`api-contract-stats.md`, 274+215줄). **그럼에도 지금 떼지 않는 이유 셋**: ① 역링크 16개 중 넷이 통계 문맥이라, 분할 후에도 **정상 해석되지만 내용이 없는 페이지**로 데려간다 — 깨진 링크보다 나쁘고 lint 도 못 잡으며, 일부는 백엔드 소유가 아니라 함께 고칠 수도 없다 ② 프론트 세션이 **바로 그 절을 읽으며** 통계 화면을 개발 중이다. 위키가 유일한 통신 수단인데 상대가 읽는 문서를 가르는 것은 통신을 끊는 일이다 ③ "프론트는 이 문서만 읽는다" 가 이 페이지의 설계 의도다. 트리거: 통계 절 250줄 / 전체 600줄 / 통계 계약을 다시 크게 여는 개편 시작 — 셋 중 하나. 분할 시 역링크 넷 정정·포인터 한 줄·log 기록을 **함께** 한다. 덧붙여 이 페이지의 `[[log]]` 위키링크 3건을 상대 링크로 교체했다 — `log.md` 는 frontmatter 가 없어 lint 가 페이지로 인덱싱하지 않아 영원히 '깨짐' 으로 잡힌다. **항상 4건이 떠 있는 lint 는 아무도 보지 않게 되고, 그러면 진짜 깨진 링크를 놓친다**
2026-08-18 | 구현 | `frontend/src/components/stat/**`(신규 8) · `app/lotto/stat/**` · `lib/{api,api-types,site}.ts` · `styles/components.css` | **003 통계 3화면 전면 개편 완료.** `/lotto/stat` 이 곧 '많이 나온 번호와 안 나온 번호' 화면이 되고 `/lotto/stat/hot-cold` 는 308 리다이렉트. 세 화면 상단에 `StatNav` 탭을 고정해 **뒤로가기 없이 서로 이동**한다(사용자 최우선 요구). 순위 표는 홈 주요통계와 **같은 네 지표**(출현·비율·최근 출현·추세)를 쓰고 표시 개수 10~40위를 고를 수 있다. 기간 조회·번호별 조회는 CSR 이고, 프리셋 구간(20/50/100/전체)은 서버가 미리 구워 **네트워크 없이 전환되며 JS 를 꺼도 기본 구간이 HTML 에 있다.** 빈도 화면에 **히트맵 45칸(10열×5행, 행=볼 구간)과 번호대별 합계**를 더했고(막대차트는 기존 hover·click·focus 를 그대로 재사용), 패턴 화면은 카드 5장(홀짝·고저·합계 히스토그램·연속번호·끝자리)에 색 띠와 도움말을 붙였다. 하단 설명은 전부 "요약 → 쉽게 말씀드리자면 → 예를 들어" 구조로 다시 썼다
2026-08-18 | ★ **협업 확인** | `10-contracts/api-contract.md` | **위키 선행 기록이 실제로 작동했다.** 계약을 쓴 직후 백엔드 세션이 그것만 읽고 003 을 구현했고, 프론트가 코드를 한 줄도 보지 않은 채 붙였는데 **응답이 계약과 완전히 일치**했다 — 구간 메타 4종, `top`, `number/{n}` 의 `rank`·`max_gap`·`companions`, `rounds/index` 1,232건, `sum_histogram`·`consecutive_counts` 전부. 교차검증도 통과: `consecutive_ratio 0.5179` = `1 - consecutive_counts["0"] 0.4821`. 프론트의 버전 감지(`supportsRangeQuery`)는 응답에 `from_round` 키가 있는지로 판단하는데, 백엔드 미구현 구간에서 실제로 기간 UI 가 잠기고 안내 문구가 뜨는 것까지 확인한 뒤 구현본으로 열렸다
2026-08-18 | ⚠ **함정** | `frontend/` (Next 데이터 캐시) | **백엔드 계약이 바뀌어도 요청 URL 이 같으면 구버전 응답이 계속 나온다.** hot-cold 는 `top` 파라미터가 붙어 URL 이 달라진 덕에 새 필드가 즉시 들어왔는데, frequency·pattern 은 URL 이 그대로라 `.next/cache` 의 옛 응답(구간 메타 없음)을 재사용했다. 그래서 같은 빌드에서 **한 화면만 "기간 조회 준비 중"** 으로 뜨는 혼란이 생겼다. `rm -rf .next/cache` 후 재빌드로 해결. 운영에서는 `revalidate` 주기(통계 1주)가 지나면 저절로 갱신되지만, **계약 변경 직후 검증할 때는 캐시를 지우고 봐야 한다** — 안 그러면 백엔드가 멀쩡한데 프론트 버그로 오진한다
2026-08-18 | ⚠ **함정** | `styles/components.css` | 기존 `.trend` 배지는 `display: inline-grid` 라 자식이 **각각 한 행**을 차지한다. 순위 표에서 기호(↑)와 라벨("최근 자주")을 한 배지에 넣자 두 줄로 쪼개졌다. 이 표에 한정해 `inline-flex` 로 바꿨다(다른 화면의 `.trend` 는 건드리지 않는다). 또 좁은 화면에서 라벨을 `display:none` 으로 감추면 기호의 뜻이 스크린리더에 전달되지 않고, 텍스트를 따로 복제하면 두 번 읽힌다 — **시각적으로만 감추는 방식**(clip-path)으로 해결했다. 6열 표가 375px 를 넘던 문제는 `.stat-table-rank` 변형에서 `min-width`·`nowrap` 을 풀어 잡았다(실측 확인)
2026-08-18 | 판단 | `app/lotto/stat/page.tsx` | 금지 표현 자가 점검 grep 에 **"나올 때가 됐다"·"나올 차례"** 가 걸리지만 그대로 둔다 — 두 문구 모두 **그 생각이 틀렸다고 반박하는 문장** 안에 있다("…는 건 없습니다", "아주 흔한 생각이고 여기엔 도박사의 오류라는 이름까지 붙어 있습니다"). 금지 규약의 취지는 예측·보장을 **주장**하지 않는 것이고, 오히려 이 설명이 사용자의 오해를 푼다. 다음에 grep 을 돌리는 사람이 오탐으로 지우지 않도록 여기 남긴다
2026-08-18 | **계약 분할** | `10-contracts/api-contract-stats.md`(신규) ← `api-contract.md` | **통계 절을 별도 계약 페이지로 분리** (사용자 지시). 앞선 항목에서 "지금은 쪼개지 않는다" 고 판단했으나, 사용자가 차후 작업에 지장이 없도록 정리하라고 지시해 실행했다. 결과 **288줄 + 232줄** 로 둘 다 소프트캡(400) 아래. 분할 근거는 002·003 개편이 전부 통계로 쌓여 한 절이 부모의 43%(210줄)가 됐고, 통계가 자기만의 규칙 체계(`window`·기간 조회·구간 메타·정렬·`top`)를 갖고 독립적으로 읽힌다는 것 — SCHEMA 의 원자성 규칙. **`GET /api/lotto/rounds/index` 도 통계 쪽에 뒀다** — 회차 엔드포인트처럼 보이지만 기간 조회 UI 전용이다
2026-08-18 | ★ **분할 방식** | `10-contracts/api-contract.md` | **다른 세션의 링크를 깨지 않는 방법으로 쪼갰다.** 통계 문맥으로 `[[api-contract]]` 를 가리키는 곳이 셋인데 그중 둘이 **백엔드 소유가 아니다**(`components.md`=frontend, `db-schema.md`=worker). 남의 페이지를 고칠 수 없으므로 **원래 자리에 포인터 절을 남겼다** — 기존 링크는 그대로 부모에 도착하고, 거기서 한 홉으로 새 페이지로 간다. 프론트·워커 세션은 **아무것도 고치지 않아도 된다**(원하면 나중에 직접 가리키게 바꿔도 되고, 그건 각 세션의 선택이다). 백엔드 소유인 `prediction-algorithm.md` 의 링크 하나만 새 페이지로 직접 돌렸다. 부모 상단의 003 변경 안내에도 "다섯 가지는 전부 api-contract-stats 에 있다" 를 덧붙였다 — 그 블록만 읽고 통계를 찾는 사람이 있다. lint: 41페이지, 깨진 링크 0건(남은 1건은 프론트 소유 페이지의 기존 항목), 크기 초과 0건
2026-08-18 | 함정 | `10-contracts/env-vars.md` · `backend/env.sample` | ★ **`CORS_ORIGINS` 는 오리진을 문자 그대로 비교한다.** 003 으로 프론트가 브라우저에서 통계 API 를 직접 부르게 되면서(CSR) 이 값이 화면 절반을 좌우하게 됐다. 실측: `CORS_ORIGINS=http://localhost:3000` 인 서버에 `http://127.0.0.1:3000` 오리진으로 요청하면 **서버는 200 을 주는데 `access-control-allow-origin` 헤더가 없어 브라우저만 조용히 막는다.** 같은 곳을 가리켜도 오리진 문자열이 다르면 다른 오리진이다. **서버 로그에는 200 만 남아 원인이 안 보인다** — 화면이 비면 서버가 아니라 브라우저 콘솔을 먼저 본다. `env.sample` 기본값을 두 주소 모두로 바꾸고, 배포 시 실제 도메인 추가를 [[deployment]] 점검 항목으로 남긴다
2026-08-18 | 관측 | `lotto_draw` | **워커 정상 동작 확인.** 앞선 기록에서 "최신 1232회(2026-07-11), 38일 경과" 로 남겼으나, 그 직후 워커 크론이 14:33 에 실행돼 **5회차를 수집하고 1237회(2026-08-15)까지 채웠다**(`collect_job_log`: lotto/cron/success/5). 누락 0. 백엔드는 무변경이며, 통계·회차 응답이 새 회차를 즉시 반영한다(미리 계산해 저장하지 않으므로)
2026-08-18 | 정정 | `10-contracts/api-contract-stats.md` | 분할하며 낡은 문장을 발견해 고쳤다 — **"세 엔드포인트 모두 `window`·`rounds_analyzed` 를 담는다"** 는 통계가 셋이던 시절의 문장이다. 002 에서 `pairs`, 003 에서 `number/{n}` 이 늘어 **다섯**이 됐고 구간 메타 넷도 함께 나가므로, 같은 문서의 '기간 조회' 절과 서로 모순이었다. 다섯으로 고치고 "엔드포인트를 더할 때 이 문장을 함께 고친다" 를 남겼다. 목차용 소제목 2개와 관련 링크 푸터도 추가. 최종 288줄 + 241줄

2026-08-18 | ★ **복구** | `lotto_draw` · `lotto_news` | **워커가 33일 멈춰 있던 것을 발견하고 복구했다.** 잡 이력이 7/15 20:29 에서 끊겨 있었다 — 크론 버그가 아니라 개발 PC 에서 **워커 프로세스가 떠 있지 않았다.** 그 사이 1233~1237 다섯 회차(7/18~8/15)가 통째로 빠져 있었다. 기동하자 계약의 catch-up 이 즉시 발화해(`마지막 성공이 33 days ... 전이라 즉시 1회 실행한다`) **5회차를 전부 수집**하고 1238 회차는 미추첨으로 정상 종료했다. 다섯 회차 모두 **독립 언론 보도와 대조해 일치**(1237회 `10,20,23,34,37,40`+36 · 1등 23명 · 12억1493만2680원 = DB `1,214,932,680`, 1235회 9명 30억9096만1625원, 1234회 18명, 1233·1236 번호 일치). 결과: 1~1237 연속·결손 0, `first_prize_amt`/`first_winner_cnt` NULL 0, `429`/`403`/재시도 실패 0건, `running` 유령행 0건. **코드 변경 없음 — 설계가 의도대로 동작했다.** DB 기준 회차가 1,232 → **1,237** 로 바뀌었으므로 백엔드·프론트의 003 통계 검증 수치가 함께 움직인다
2026-08-18 | ⚠ **결손** | `lotto_news` | **뉴스 32일치(2026-07-16~08-16)는 복구되지 않는다.** 같은 중단 기간인데 회차와 결과가 갈렸다. `news` 크론은 15:00 정각에 정상 발화해 91건을 담았으나 **전부 당일·전날치**다 — `news` 는 catch-up 대상이 아니고 신선도 필터(`NEWS_MAX_AGE_DAYS=1`)가 하루 넘은 기사를 버리므로 크론을 몇 번 더 돌려도 같다. 갈린 이유는 **지난 것을 다시 부를 주소의 유무**다: 회차는 번호로 지목해 재조회되지만 뉴스는 검색 API 가 최신순으로만 준다. 현재 263건(7/8~8/18, `link_url` 중복 0). 되살리려면 잡이 아니라 `start` 로 과거 페이지를 훑는 **1회성 백필**이 필요하고, 그것은 [[naver-search-api]] 의 **재배포 약관 미확인**에 걸리는 정책 판단이라 **워커가 임의로 시작하지 않는다.** `index.md` 미결 항목에 올렸다
2026-08-18 | **계약 정정** | `10-contracts/worker-jobs.md` · `90-external/naver-search-api.md` | 위 결손이 **계약의 틀린 서술을 드러냈다.** `worker-jobs` 는 "`news` 잡은 catch-up 하지 않는다. **놓친 뉴스는 다음 주기에 어차피 검색된다**" 고 적어 왔는데, 뒷문장은 신선도 필터가 없던 시절의 것이고 필터를 넣은 2026-07-09 에 함께 고쳤어야 했다. 동작은 그대로 두고(catch-up 여전히 안 함) **근거와 대가를 정확히** 다시 썼다 — 즉시 돌려도 놓친 구간이 돌아오지 않으므로 catch-up 이 무의미한 것이지, 나중에 검색되기 때문이 아니다. `lotto`↔`news` 대비표와 "워커 중단은 회차엔 지연, 뉴스엔 소실" 운영 함의를 넣었고, `naver-search-api` 에는 필터의 대가를 실측과 함께 남겼다(필터를 푸는 것은 답이 아니다 — 평상시 목록이 며칠씩 밀린다). **동작·스키마 변경 0, 다른 세션이 코드를 고칠 일 없음.** 사용자 지시로 사전 승인 대신 이 기록으로 갈음한다
2026-08-18 | 수정 | `components/stat/StatNav.tsx` · `styles/components.css` | **통계 화면 시각 균형 3건 수정**(사용자 피드백). ① 통계 3종 이동을 **작은 탭 → 카드**로 되돌렸다. 뒤로가기 문제를 없애려고 탭으로 만들었더니 "너무 작아 눈에 띄지 않는다" 는 지적이 나왔다 — 예전 허브의 파스텔 카드(제목·한 줄 요약·바로가기)를 세 화면 **어디에나** 두는 형태로 절충했다. 현재 항목은 링크가 아니라 `<div>` 로 낸다(자기 자신 링크는 눌러도 아무 일이 없어 혼란스럽다). ② 조회 버튼이 작았던 원인은 **`.chip` 을 재사용한 것**이었다 — 그쪽은 뉴스 키워드처럼 읽기만 하는 11px 라벨이고 이쪽은 누르는 컨트롤이라 크기가 달라야 한다. 44px 높이의 `.scope-chip` 을 따로 만들었다(`.chip` 은 건드리지 않는다 — 뉴스·꿈해몽이 함께 쓴다). ③ "왼쪽으로 쏠려 보인다" 는 지적은 두 원인이었다 — 조회 조건이 배경 없이 떠 있어 무게가 없었고(카드로 감쌌다), 넓은 화면에서 세 표를 세로로만 쌓아 오른쪽이 통째로 비었다(1024px↑ 2열 + 세 번째만 전폭). 표 내부도 텍스트 열 하나가 남는 폭을 흡수하게 해 열이 좌우로 퍼지게 했다
2026-08-18 | ⚠ **함정** | `components/stat/HotColdBoard.tsx` | **재검증 주기가 다른 두 응답을 한 화면에서 섞으면 숫자가 어긋난다.** 최신 회차는 `/lotto/latest`(10분), 통계는 hot-cold(1주) 라 워커가 새 회차를 채운 뒤 "1213~1232회 구간" 을 보면서 "1232회 · **5회 전**" 이 표시됐다(최신은 이미 1237). 그래서 '몇 회 전' 의 기준을 **응답이 말하는 구간의 끝**(`to_round`)으로 바꿨다 — 화면 안에서 숫자가 서로 맞는 편이 바깥의 최신 회차와 맞는 것보다 덜 혼란스럽다. 기간 조회에서도 같은 이유로 일관된다
2026-08-18 | 수정 | `lib/stat-insight.ts` · `components/stat/HotColdBoard.tsx` | **추세 라벨을 다듬고 열 머리글에 도움말을 붙였다.** 사용자가 "'최근 뜸' 은 뭐야?" 라고 물어 왔다 — 질문이 나왔다는 것 자체가 라벨이 실패했다는 뜻이다. 문제는 둘이었다: '뜸' 은 명사로 쓰면 어색하고, **무엇과 비교한 값인지** 라벨에 드러나지 않았다(구간을 반으로 갈라 뒤쪽 절반과 앞쪽 절반을 비교한 것이다). `최근 자주/최근 뜸` → **`후반에 더/후반에 덜/비슷`** 으로 바꾸고, '추세' 열 머리글에 계산 방식과 예시를 담은 `HelpTip` 을 달았다. ⚠ "늘어남/줄어듦" 같은 표현은 후보에서 뺐다 — 추세가 이어질 것처럼 읽혀 예측이 된다([[forbidden-expressions]]). `TREND_DISPLAY` 는 홈 StatBoard 도 공유하므로 두 화면의 라벨이 함께 바뀐다(의도한 일관성)
2026-08-19 | 수정 | `components/stat/HotColdBoard.tsx` · `styles/components.css` | **표 안의 도움말 팝업이 잘려 상시 범례로 바꿨다.** '추세' 열 머리글에 붙인 `HelpTip` 이 `.table-scroll` 의 `overflow-x: auto` 에 잘려 내용이 반만 보였다(사용자 지적). **스크롤 컨테이너 안에서는 absolute 팝업이 원리상 빠져나올 수 없다** — 위치를 계산해 띄우는 대신 팝업을 쓰지 않기로 했다. 표 아래에 배지와 설명을 **항상 보이는 한 줄**로 두었고, 누르지 않아도 뜻이 전달되는 이점이 덤으로 생겼다. 앞으로도 `.table-scroll` 안에는 팝오버를 두지 않는다
2026-08-19 | 수정 | `components/stat/PatternBoard.tsx` · `styles/components.css` | **패턴 카드의 왼쪽 색 띠를 없앴다.** 사용자가 "AI 스럽다" 고 했고, 실제로 그랬다 — 홀짝이 보라색이고 고저가 초록색일 **근거가 없었다.** 서비스 팔레트를 카드 순서대로 배정했을 뿐이라 색이 아무 사실도 가리키지 않았고, 뜻 없는 장식이 화면을 템플릿처럼 보이게 했다. 대신 ① 카드를 중립으로 두고 ② **각 카드의 결론 한 줄**(“가장 흔한 모양은 3:3 · 33.4%”)을 제목 아래 크게 넣고 ③ 색은 **가장 흔한 항목 하나**를 진하게 칠하는 데만 썼다(나머지는 같은 색의 옅은 톤). 합계 히스토그램은 최빈 구간이 진해져 종 모양의 꼭대기가 바로 보인다. ⚠ 이때 **결론 문장과 강조 색이 같은 행을 가리키게** 맞추는 것이 중요하다 — 연속번호 카드만 `consecutive_ratio`(51.8%)를 결론으로 쓰는 바람에 색은 '연속 없음 48.2%' 를 가리켜 둘이 어긋났다. 규칙을 통일하고 뒤집어 말한 사실은 footer 로 내렸다
2026-08-19 | 수정 | `worker/worker/jobs/news.py` · `sources/naver_news.py` · `worker/CLAUDE.md` | **어제 계약에서 정정한 문장이 코드 주석에 그대로 남아 있었다.** `news.py` 모듈 docstring 이 여전히 "놓친 뉴스는 다음 주기에 어차피 검색된다" 고 말하고 있었다 — 계약만 고치고 코드를 놓치면 다음 세션이 코드를 읽고 같은 오해를 한다. 실측(33일 중단 → 32일치 0건)과 `lotto` 와 갈리는 이유를 넣어 다시 썼다. 함께 발견한 낡은 서술 둘도 고쳤다: `naver_news.py` 가 "일일 쿼터 **미확인**" 이라 적고 있었으나 2026-07-09 에 **25,000 으로 확인**됐고(미확인인 것은 재배포 약관 쪽이다), `worker/CLAUDE.md` 의 위키 안내표도 같은 오류였다 — 이 파일은 매 턴 컨텍스트에 주입되므로 틀린 값이 계속 따라다닌다. **동작 변경 0건**(주석·문서만), import 및 `_is_fresh` 판정 재확인
2026-08-19 | **계약 변경** | `10-contracts/worker-jobs.md` · `env-vars.md` | **뉴스 주제 적합성 필터 신설.** 사용자가 "복권과 관련없는 뉴스가 수집된 것 같다" 고 지적해 실측했더니 맞았다 — 332건 중 **제목에 '로또'/'복권' 이 아예 없는 기사가 223건(67%)**. 야구 중계·카카오페이 제휴·편의점 프로모션·과학해설사 모집·OTT 라인업·트롯 순위·여행기가 섞여 있었다. 원인은 신선도 필터의 사각지대가 아니라 **검색 API 가 본문 전문을 뒤진다는 것**이다: 과학관 보도자료 말미의 "이 사업은 복권기금으로 운영된다" 한 줄이 걸린다. 기사의 주제가 아니라 각주에 스친 단어다. → 두 규칙을 **제목에만** 적용한다. ① `NEWS_TITLE_MUST_MATCH`(기본 true) 제목에 검색어가 없으면 버린다 ② `NEWS_EXCLUDE_KEYWORDS` 제목에 부동산·증시 비유어(`청약,줍줍,분양,…,공모주,따상`)가 있으면 버린다 — '로또 청약' 은 ①을 통과하지만 이 사이트에 온 사람이 찾는 뉴스가 아니고, 몰리면(안유진 청약 13건·송파 롯데캐슬 12건) 목록을 통째로 덮는다. **요약(description)을 보지 않는 이유**: 네이버가 검색어 주변을 잘라 주는 스니펫이라 332건 **전부**가 통과해 변별력이 0 이다. 제목만이 주제를 말해 준다. 낱말은 코드에 박지 않는다(새 유행어를 `.env_worker` 한 줄로 막는다). **프론트 영향**: 뉴스 목록 건수가 줄어든다(수집분 기준 100→24). 백엔드는 조회만 하므로 코드 영향 0. 사용자 지시로 사전 승인 대신 이 기록으로 갈음한다
2026-08-19 | 구현·검증 | `worker/worker/jobs/news.py` · `config.py` · `env.sample` | 위 필터 구현. `_is_on_topic()` 을 병합 직후·신선도 **앞**에 둔다(문자열 검사가 날짜 변환보다 싸다. 순서를 바꿔도 결과는 같다). 로그가 단계별 감소를 다 보이게 고쳤다 — `조회 100 → 링크 중복제거 94 → 주제적합 24 → 신선 24 → 신규 N`. 어느 단계가 얼마나 깎았는지 보이지 않으면 필터를 조정할 근거가 없다. **검증**: ① 대표 사례 12건 단위 판정 전부 통과(1237회 당첨·연금복권·미수령 당첨금·복권기금은 담고, 야구·카카오페이·엘칸토·로또청약·줍줍·공모주·운세는 버린다) ② **오탈락 실측 — 걸러진 248건 중 진짜 복권 뉴스 0건.** '당첨' 이 제목에 있으면서 제외된 8건은 전부 청약 당첨(안유진·송파)이었다 ③ 새 코드로 잡 1회 실행: 조회 100 → 주제적합 24(70건 제외) → 신규 0(이미 있는 기사) ④ 기존 332건 전수 재판정이 드라이런과 일치(통과 84). **⚠ 제외어를 늘릴 때는 반드시 기존 데이터에 드라이런한다** — 과잉 차단은 조용히 진짜 뉴스를 지운다. 이 주의를 코드 docstring 에도 남겼다
2026-08-19 | 수정 | `components/stat/FrequencyHeatGrid.tsx` · `styles/components.css` | **번호판 히트맵을 숫자에서 실제 볼로 바꿨다**(사용자 제안). 진짜 번호판처럼 읽히고 볼 색이 구간을 한 번 더 말해 준다. ⚠ 빈도는 **볼 뒤 칸 배경**에만 칠한다 — 볼 자체에 칠하면 공식 5구간 색이 왜곡된다([[0009-official-ball-colors-over-mockup]]). 칸은 사각형이고 볼은 원이라 네 모서리가 늘 배경으로 남아 얇은 링에서도 진하기가 읽힌다. **볼에 배경색 링(2px)이 반드시 필요하다** — 없으면 파란 볼(11~20)이 진한 파란 히트 배경에 묻혀 번호가 안 보인다(실측). 히트 색을 무엇으로 바꾸든 우연히 같은 계열이 되는 볼이 있으므로 링은 상시 필요하다. 진하기 상한을 82%→100% 로 올려 0회 칸이 무색으로 분명해졌고, 행 라벨은 `1–10`→`01–10` 으로 자릿수를 맞췄다(폭이 달라 격자가 한 칸 밀린 것처럼 보였다). 볼은 `.ball-sm`(28px)이 375px 칸(약 25px)보다 커서 칸에 맞춰 줄인다
2026-08-19 | **결정** | `30-seo/metadata-strategy.md` · `lib/site.ts` · `sitemap.ts` · `app/dream/**` | **꿈 키워드 색인을 4,802개 → 10개로 줄였다.** 사용자가 "URL이 많이 필요 없다, 길몽·재물운 키워드 10개 정도만" 이라고 했고 근거가 맞았다 — 백엔드는 표제어와 번호만 주고 해몽 풀이는 주지 않으며 **없는 내용을 지어내지 않기로 이미 정해 두었다.** 그러면 4,802개는 서로 거의 같은 얇은 페이지이고, 사이트맵의 79%가 그것이면 색인 이득보다 사이트 전체가 저품질로 평가될 위험이 크다. 남긴 것: 돼지·똥·대통령·조상·용·불·물·뱀·호랑이·돈(전부 사전 존재 확인). 선별 밖은 `index:false, follow:true` 라 **페이지는 그대로 열리고 번호 생성도 정상 동작한다** — 검색에만 안 나간다. 사전 생성도 200개→10개로 줄여 빌드가 가벼워졌고, `/dream` 허브의 미리보기 60개도 선별 키워드를 앞으로 정렬했다(종전에는 사전 순서라 정작 '돼지꿈'이 화면에 없었다). **사이트맵 6,058 → 1,266 URL.** 나중에 고유 본문을 확보하면 그때 목록을 넓힌다 — 순서가 거꾸로 되지 않게 한다
2026-08-19 | **정리** | `lotto_news` | **사용자 지시로 기존 노이즈를 새 규칙으로 정리했다.** 332건 → **80건**(248건 1차 + 4건 2차 삭제). 판정은 SQL 로 옮겨 적지 않고 `_is_on_topic` 을 그대로 불러 썼다 — 규칙의 정본이 둘이 되면 나중에 한쪽만 고쳐 '수집은 막는데 정리는 안 되는' 상태가 된다. 지운 것은 제목·링크·발행일까지 TSV 로 백업했다(되돌릴 수는 없어도 무엇을 지웠는지는 남는다). PK(`news_id`)로 지운다 — 링크 문자열은 길고 이스케이프 실수가 난다. **남은 80건은 전량 육안 검수**했다: 회차 당첨(1232·1237)·연금복권 720·당첨자 후기·미수령 당첨금·동행복권 행사·복권기금 배분 정책
2026-08-19 | **계약 변경** | `10-contracts/worker-jobs.md` · `env-vars.md` | **제외어를 13 → 22개로 확장하고 검색어에 `연금복권` 을 더했다**(사용자 지시: "로또나 복권, 연금복권 관련 뉴스만"). 1차 필터 후 남은 84건을 눈으로 훑어 누수 4건을 찾았다 — `'11억 로또' 송파 롯데캐슬`(부동산), `용산 '로또 임대'`(부동산), `연 13% 역대급 '로또 적금'`(금융상품), `800조 반도체 복권`(산업 비유), 그리고 `시국사범 2,335명 **사면·복권**(1987)`(復權, **한자어 동음이의**). 제외어를 세 갈래로 정리했다: 부동산 / 증시·금융 / 동음이의. ★ **`사면` 만으로 두지 않았다** — "복권 **사면** 1등" 같은 진짜 기사가 함께 죽는다. 동음이의어는 낱말이 아니라 **실제로 쓰이는 꼴**(`사면·복권`)로 적는다. `억 로또` 처럼 띄어쓰기를 포함한 항목도 부분문자열로 동작한다. **검증**: 후보 9개를 332건 전량에 드라이런해 **진짜 복권 뉴스 오탈락 0건**, 정확히 누수 4건만 걸림을 확인한 뒤 채택했다. 실행 결과 조회 100 → 주제적합 **22**(72건 제외). **`연금복권` 은 판정이 아니라 검색 커버리지용**이다 — 주제 판정에서는 '복권' 이 '연금복권' 을 부분문자열로 이미 잡지만, 네이버는 질의어별로 다른 결과를 주므로 연금복권 추첨 기사가 '복권' 상위 50건 밖으로 밀리면 영영 안 들어온다. 쿼터는 48 → 72회/일(0.3%)로 늘고 관련 문서 5곳의 계산을 함께 고쳤다. ⚠ **`.env_worker` 의 `NAVER_NEWS_QUERY` 는 사용자가 갱신해야 반영된다** — 워커가 읽는 값이 코드 기본값을 덮는다(실행 로그에 `검색어 ['로또','복권']` 로 확인)

2026-08-21 | 삭제 | `frontend/src/app/v2/**` | **미채택 시안 `/v2` 를 삭제했다**(사용자 지시). 삭제 전 영향도를 전수 조사했다 — v2 는 `@/lib/*` 를 **쓰기만 하는 leaf** 였고, 바깥에서 v2 를 import 하는 곳 0건, `robots.ts`·`sitemap.ts` 언급 0건, 격리용 `hb-` 클래스와 `body:has(.hb-root)` 도 전부 v2 폴더 안에만 있었다. v2 가 쓰던 심볼 7종은 모두 다른 사용처가 있어 고아가 되지 않는다. 삭제 전후 라우트 매니페스트를 비교해 **사라진 라우트가 `/v2` 하나뿐**이고 공유 청크 해시까지 동일함을 확인했다(27 → 26). 코드는 `24ad827` 에 남아 있어 되돌릴 수 있다. 기법 기록인 [[home-v2-concept]] 는 그대로 둔다 — 코드가 사라졌다고 배운 것까지 지울 이유는 없다
2026-08-21 | ★ **실측** | `frontend/**` | **미측정으로 남아 있던 Lighthouse·375px 을 전부 실측하고 드러난 결함을 고쳤다.** 종전 기록의 "헤드리스 브라우저 설치에 sudo 가 필요해 불가" 는 **Windows 쪽 node·크롬을 `cmd.exe /c` 로 부르면 해결된다**(WSL↔Windows 는 localhost 포워딩). 절차는 `frontend/README.md`. 결과: 14개 페이지 **SEO 100 · 접근성 100 · 모범사례 100 · CLS 0.000**, 성능 83~92. axe(WCAG 2.2 AA)로 20개 페이지 × 라이트·다크 **위반 0건**, 375px 가로 넘침 0건(도움말 전개 상태 포함)
2026-08-21 | ⚠ **측정 함정** | `frontend/README.md` · `next.config.ts` | **처음 잰 값이 통째로 무효였다.** ① `next dev` 가 떠 있으면 같은 `.next` 를 계속 덮어써서 `next start` 가 **개발용 청크를 서빙한다**(`main-app.js?v=…`, 번들 1.75MB). 그 상태의 첫 측정이 성능 50 · LCP 16초였다 — 에러가 없어 알아채기 어렵다. `distDir: process.env.NEXT_DIST_DIR || '.next'` 탈출구를 만들어 산출물을 가른다(⚠ 그 대신 Next 가 `next-env.d.ts` 참조 경로를 고쳐 놓으므로 끝나면 `git checkout`). ② **백엔드가 꺼져 있으면 통계 화면이 껍데기만 렌더돼 아무것도 검증하지 못한다.** 계약대로 응답하는 목 서버를 스크래치패드에 띄워 다시 쟀고, 그러자 접근성 위반과 375px 넘침이 **새로 드러났다.** 목 값은 평균이 아니라 화면을 가장 세게 미는 극단값으로 채운다
2026-08-21 | **정정** | `20-design/accessibility.md` · `design-tokens.md` | ★ **접근성 문서의 전제가 틀려 있었다.** "노랑·초록 볼만 흰 숫자로 AA 미달" 이라고 적어 왔으나 실측 결과 **다섯 색 전부 미달**이다(파랑 1.88 · 빨강 2.66 · 회색 2.32 · 노랑 1.62 · 초록 1.65). 즉 같은 문서의 원칙("볼에는 항상 숫자가 보인다 — 색 없이도 읽힌다")이 흰 숫자로는 성립하지 않고 있었다. **볼 배경색은 그대로 두고**([[0009-official-ball-colors-over-mockup]]) 숫자만 `--ball-fg-dark` 로 통일했다(6.28~10.32 통과) — 숫자 색은 색-구간 매핑에 아무 역할도 하지 않으므로 규약을 깨지 않는다. `--ball-fg`(흰색)는 이제 진한 `--svc-*` 타일 글리프 전용이다
2026-08-21 | **토큰 변경** | `20-design/design-tokens.md` · `styles/tokens.css` | **대비 미달 토큰을 실제로 얹히는 배경 기준으로 재산정했다.** 전부 "흰 배경에서는 통과하는데 실제 배경에서는 미달" 이었다. `--color-text-muted` #6b7280→**#626875**(파스텔 카드 위 4.00 → 4.6↑), `--color-text-subtle` #98a2b3→**#647188**(흰 배경에서도 2.58 이었다. 중립 배경 전용으로 못박음), 시맨틱 4색을 짝 `-soft` 배경 기준으로 재산정(danger 3.95·info 3.57 미달), **다크 전용 시맨틱 4색 신설**(종전엔 `-soft` 만 덮고 진한 값은 라이트 값을 그대로 써서 danger 3.34·info 3.64 로 처음부터 미달), **`--color-on-accent` 신설** — 채워진 강조 배경 위 글자색. 흰색을 직접 박으면 다크에서 2.97 로 무너진다(주요 버튼·선택 칩 전부가 그랬다). 규칙으로 남겼다: **토큰은 그 토큰이 얹히는 배경 전부를 계산하고, 다크는 axe 로 따로 검사한다**
2026-08-21 | 수정 | `styles/base.css` · `components/stat/HelpTip.tsx` · `FrequencyChart.tsx` · `StatBoard.tsx` · `NumberActions.tsx` · 신규 `components/ScrollArea.tsx` | **접근성 결함 5종을 고쳤다.** ① 문장 속 인라인 링크가 색으로만 구분됐다(링크색↔본문색 대비 1.31) → `p`·`.prose` 안의 클래스 없는 링크에 밑줄. 내비게이션·카드는 제외. ② **도움말 팝오버가 375px 에서 최대 203px 잘렸다** — `left:0` 으로 버튼에 붙어 있어 버튼이 줄 오른쪽이면 화면을 벗어난다. CSS 로는 알 수 없어 열릴 때 재서 `transform` 으로 민다. ③ **빈도 막대차트 버튼이 5.6px** 이었다(WCAG 2.5.8 은 24×24). scroll 모드는 `min-width:1168px` 로 막대당 24px 확보, **fit 모드(홈)는 `<div>` 로 바꿔 아예 컨트롤이 아니게 했다** — 폭에 45개를 맞추면 어떤 값으로도 24px 를 만들 수 없다. ④ role 없는 `<span>` 의 `aria-label` 은 규격상 무시된다 → `aria-hidden` + `.sr-only`. ⑤ 보이는 글자("이미지 저장")가 접근성 이름("이미지**로** 저장")에 없어 음성 명령이 닿지 않았다(WCAG 2.5.3). 추가로 **가로 스크롤 영역 8곳에 키보드 접근이 없었다** — 안에 포커스 요소가 없으면 키보드로는 좌우로 못 움직인다. `ScrollArea`(role=region + label + tabindex)로 일원화
2026-08-21 | ⚠ **CLS 함정** | `lib/fonts.ts` · `20-design/design-tokens.md` | **`next/font` 의 메트릭 오버라이드는 한글에 걸리지 않는다.** 그 폴백 페이스는 `local("Arial")` 기준인데 Arial 에는 한글 글리프가 없어, 한글은 그 단계를 건너뛰고 OS 별 시스템 한글 폰트로 떨어진다. 그래서 `display:'swap'` 이면 Pretendard 도착 시 줄바꿈이 다시 계산돼 문단이 밀린다 — 느린 4G 실측에서 2,825ms 에 25px 줄며 **`/contact` CLS 0.207 · `/privacy` 0.128**(기준 0.1 초과). 문서가 오랫동안 메트릭 오버라이드를 "CLS 방지의 핵심" 이라 적어 온 것이 **한글 사이트에서는 반만 맞았다.** `display:'optional'` 로 바꿔 전 페이지 **CLS 0.000**. 대가는 느린 회선의 첫 화면이 시스템 폰트라는 것이고, 산 것은 모든 회선의 레이아웃 안정성이다
2026-08-21 | ⚠ **SEO 함정** | `30-seo/metadata-strategy.md` · `next.config.ts` · `app/news/page.tsx` | **dynamic 라우트에서 `<meta>` 가 `<body>` 에 앉는다.** Next 15.2 부터 메타데이터가 기본 스트리밍이라 셸(`<head>`)을 먼저 보내고 태그를 스트림 뒤쪽에 꽂는다. `searchParams` 를 읽는 `/news` 만 해당됐고, **JS 실행 후에도 `<head>` 로 옮겨지지 않았다.** Lighthouse SEO 가 `/news` 만 91점(`meta-description`). Next 는 `htmlLimitedBots` 목록의 UA 에게만 블로킹으로 주는데 **Googlebot 은 JS 를 실행한다는 이유로 일부러 빠져 있고**, Lighthouse 12 는 UA 에서 `Chrome-Lighthouse` 를 빼서 일반 크롬과 구분되지 않는다. 해법 둘: `htmlLimitedBots: /.*/`(우리 메타데이터는 전부 정적이라 대가가 없다) + **페이지 컴포넌트에서 await 하지 않고 `<Suspense>` 자식이 조회**. 전 UA 에서 `<head>` 진입 확인
2026-08-21 | **버그** | `components/analytics/PageViewTracker.tsx` | **네이버 애널리틱스에 계정 ID 를 넣는 `wcs_add["wa"]` 가 빠져 있었다.** 측정 ID 를 채워도 스크립트만 로드되고 **집계가 한 건도 잡히지 않는 상태**였다(에러가 없어 알아채기 어렵다). 종전 코드는 `wcs.event` 에 account 를 넣었는데 그건 전환 이벤트용 인터페이스다. 공식 스니펫 형태로 고쳤다. 목 측정 ID 를 주입해 런타임 검증 — 최초 진입과 **링크 클릭 이동** 양쪽에서 `wa=` 가 실린 요청이 나가고, GA4 는 이동마다 정확히 1건씩 `page_view` 를 보낸다(중복 없음). 나머지 env 연동(소유확인 메타·canonical·robots·sitemap·문의 메일·애드센스 로더)도 목 값으로 전수 확인했다. **남은 것은 사용자가 실제 값을 채우는 일뿐이다**
2026-08-21 | 수정 | `components/FrequencyChart.tsx` · `styles/components.css` · `20-design/components.md` · `responsive-rules.md` · `accessibility.md` | **홈 빈도 그래프에 호버·탭·키보드 조회를 되살렸다**(사용자 요청). 같은 날 앞선 항목에서 터치 타겟(WCAG 2.5.8) 때문에 fit 모드 막대를 `<div>` 로 바꾸고 readout 을 지웠는데, **그 결정이 [[components]] 사양("호버 툴팁 `33번 (13회)`")과 어긋났다** — 규칙을 지키려다 명세된 기능을 없앤 것이라 잘못이었다. 되살리되 막대를 다시 버튼으로 만들지는 않았다: 375px 에서 막대 하나가 **6.7px** 이라 24×24 를 어떤 값으로도 만들 수 없기 때문이다. 대신 **차트 전체를 하나의 조작면**으로 두고 포인터 x 좌표를 45등분해 번호를 고른다 — 조작 대상이 전폭 하나뿐이라 `target-size` 를 지키면서 호버·탭이 모두 동작한다(차트 툴팁의 표준 패턴). 마우스 전용이 되지 않게 `tabIndex` + **좌우 방향키·Home·End** 를 붙였고 readout 은 `aria-live` 라 스크린리더가 값을 읽는다. ⚠ 함정 둘을 코드 주석에 남겼다 — ① 막대에 `pointer-events: none` 이 없으면 막대 사이 gap(2px)에서만 이벤트가 와 움직임이 끊긴다 ② **터치에서는 `pointerleave` 로 선택을 지우면 안 된다**(손을 떼는 순간 결과가 사라진다. `pointerType === 'mouse'` 일 때만 지운다). 실측: 데스크톱 1280 에서 왼쪽/25%/가운데/오른쪽 → 1·12·23·45번 정확, 이탈 시 초기화, 모바일 375 탭도 동일, 방향키 Home/→×3/End → 1·4·45번. 회귀 없음(홈·통계 3면 접근성 100 · SEO 100 · CLS 0 · `target-size` 통과, axe 라이트·다크 위반 0, 375px 넘침 0)
2026-08-21 | **정정** | `20-design/responsive-rules.md` · `components.md` | ★ **"모바일은 막대차트를 상위 5개로 축약한다" 는 규칙을 폐기했다 — 코드가 한 번도 그렇게 동작한 적이 없다.** 홈 차트는 처음부터 45개를 폭에 맞춰 그렸다. 위 작업으로 값 조회가 readout 으로 돌아오면서 축약의 근거("45개는 모바일에서 가독성이 없다")도 절반만 맞게 됐다 — 이 차트가 하는 일은 막대 하나를 읽는 게 아니라 **분포의 모양**을 보이는 것이고 정확한 값은 readout 이 준다. 문서를 구현에 맞추고 폐기 사유를 남겼다. **동작 변경 0건**(문서만)
2026-08-21 | 수정 | `styles/components.css` · `lib/site.ts` · `components/stat/NumberInspector.tsx` · `FrequencyHeatGrid` 관련 CSS | **통계 화면 사용성 5건**(사용자 요청). ① 카드의 "보러 가기 →" 를 `--fs-xs` 의 **1.5배 + bold** 로 키웠다 — 카드에서 누를 수 있음을 알리는 유일한 문구인데 작았다(18.72px/700 실측, 파스텔 3색 위 대비 5.29~5.66 으로 AA 여유). ② 순위 목록 기본 개수 **15 → 10**(`STAT_TOP_DEFAULT`). 서버 프리페치와 클라이언트 기본값이 같은 상수를 보므로 SSR/CSR 이 어긋나지 않는다. ③ **'번호 하나만 보기' 가 1번을 고른 채로 시작한다** — 종전에는 아무것도 선택되지 않아 무엇을 누르면 무엇이 나오는지 눌러 보기 전엔 알 수 없었다. ⚠ 최초 1회만 돌도록 ref 로 잠갔다(`load` 가 상태를 바꾸므로 의존성에 넣으면 무한 루프, StrictMode 이중 마운트도 함께 막힌다). ④ **"이 구간에서 나온 회차" 가 눌리는 것처럼 보이게** 고쳤다 — 링크였지만 알리는 것이 호버뿐이라 터치 사용자에게는 아무 신호가 없었다. 정지 상태에서 링크색 테두리·글자 + 이동 표식(›) + "회차를 누르면 …" 안내 한 줄을 함께 둔다. ⑤ 히트맵 볼 확대 — 아래 별도 항목
2026-08-21 | ★ **버그** | `styles/components.css` `.heat-cell .ball` | **히트맵 볼이 무너져 있었다 — 크기 취향 문제가 아니었다.** 사용자가 "볼을 2배쯤 키워 달라" 고 해서 실측했더니 **칸 24px 에 볼 5px**, 데스크톱은 **칸 97px 에 볼 8px** 이었다. 원인은 `LottoBall` 의 3중 구조다 — `.ball-hit > .ball-wrap > .ball` 에서 가운데 `.ball-wrap` 이 `display: inline-grid` 라 **자기 내용(=볼)에 맞춰 크기가 정해지는데**, 그 안에서 볼이 다시 `width: 100%` 를 요구하면 서로를 기준으로 삼는 순환이 되어 글자 크기까지 쪼그라든다. 두 껍데기를 `place-items: stretch` 로 먼저 칸에 맞춰 늘려야 한다(`.ball-hit` 의 min 44px 도 함께 푼다). 결과 **5px → 21px(모바일) · 8px → 48px(데스크톱)**. 함께 처리한 것 둘: **격자 폭 상한 620px** — 칸이 남는 폭을 10등분하므로 상한이 없으면 볼을 고친 순간 번호판이 화면을 뒤덮는다(1280px 에서 칸 97px). **글자를 vw 가 아니라 `cqw`(칸 폭) 기준으로** 바꿨다 — vw 로 묶으면 격자가 상한에 닿은 뒤에도 화면 폭만 따라가 볼과 숫자 비율이 어긋난다. 이제 어느 폭에서도 글자/볼 ≈ 0.37 로 일정하고 숫자 잘림 0건. ⚠ **10열 고정이라 375px 에서 칸은 24px 가 상한이다**(라벨 32px + gap 을 빼면 10칸에 그 이상 줄 폭이 없다). 모바일에서 볼을 더 키우려면 열 수를 줄이거나 가로 스크롤을 도입해야 하는데, 둘 다 "행 = 공식 5구간" 과 "한눈에 본다" 를 깨므로 하지 않았다
2026-08-21 | **발견** | `lib/api.ts` `browserGetJson` · `NumberInspector` | 위 ③(진입과 동시에 조회)이 **묻혀 있던 결함 셋을 드러냈다.** ⓐ 응답이 아예 오지 못하는 경우(CORS 차단·오프라인·타임아웃) fetch 가 던지는 `TypeError: Failed to fetch` 가 화면에 **영어 그대로** 노출됐다 — 화면들이 `err.message` 를 그대로 쓴다. 한국어 문구로 감싸고 원본은 콘솔에 남긴다(가장 흔한 원인이 CORS 오리진 불일치인데 서버 로그에는 200 만 남는다). ⓑ 결과 패널의 `h4` 가 감싸는 `h2` 바로 아래라 **h3 를 건너뛰고 있었다**(axe `heading-order`). 종전에는 번호를 고르기 전까지 패널이 렌더되지 않아 감사에 걸리지 않았을 뿐이다. ⓒ 이번에 붙인 회차 링크의 `aria-label` 이 **보이는 글자를 접근성 이름 밖으로 밀어냈다**(WCAG 2.5.3 Label in Name) — 음성 명령이 닿지 않는다. `aria-label` 을 걷고 보이는 글자 뒤에 `sr-only` 로 덧붙이는 방식으로 고쳤다. **교훈: 조건부로만 렌더되는 영역은 그 조건을 만들어 놓고 감사해야 한다.** 최종: 홈·통계 3면 접근성 100 · SEO 100 · 모범사례 100 · CLS 0, axe 라이트·다크 위반 0, 375px 넘침 0
2026-08-21 | **재설계** | `components/stat/StatGuide.tsx`(신규) · `StatNav.tsx` · 통계 3화면 · `20-design/components.md` | **통계 화면 하단 설명을 줄글에서 '표본 해부'로 바꿨다**(사용자 선택 A안, 시안 비교 후). 세 화면 모두 `.prose` 안 h3 네 개 + 문단 열 개였는데, 모바일에서 벽처럼 읽히고 "쉽게 말씀드리자면 —" 류 상투구가 반복돼 기계가 쓴 글로 읽혔다. 이제 **화면에 실제로 보이는 조각**(표 한 줄·추세 배지·볼 5색·히트맵 칸·미니 막대)에 라벨을 붙인다. 사실은 하나도 버리지 않았다 — 도박사의 오류, 표본 크기(20회=공 120번, 번호당 2.7회), 보너스 번호의 역할, 3:3 이 흔한 이유, 합계 종 모양, 연속번호 51.8%, 모양≠조합이 전부 남아 `GuideNote` 결론 블록으로 옮겨졌다. **데스크톱을 함께 짰다** — 375 1열 → 640 2열(홀수면 마지막만 전폭) → 1024 3열. 기존 관례(1024px↑ 다열, 세 번째만 전폭)를 그대로 따랐다. 바로가기 카드에는 내용을 가리키는 아이콘을 넣었다(순위=막대, 빈도=격자, 패턴=반원). ⚠ **트로피·달력은 뺐다** — 트로피는 '당첨'을 암시하고 달력은 이 화면이 날짜가 아니라 번호에 관한 것이라 틀린 라벨이다. 검증: 3화면 × 3폭 레이아웃 의도대로, axe 라이트·다크 위반 0, 375px 넘침 0, SEO 100 · 접근성 100 · CLS 0, JS 없는 HTML 에 설명 전문 포함, 금지 표현 0건
2026-08-21 | 판단 | `components/stat/StatGuide.tsx` | ★ **표본의 숫자는 실데이터가 아니라 '예시' 다 — 그리고 그렇게 명시한다.** 실제 값을 쓰면 항상 맞아 보여 좋을 것 같지만 그렇지 않다: 설명 절은 서버 렌더이고 위쪽 표는 조회 구간을 바꾸면 클라이언트에서 다시 그려지므로, 사용자가 구간을 한 번만 옮겨도 둘이 어긋난다. 어긋난 두 숫자가 나란히 있으면 "어느 쪽이 맞나" 라는 새 혼란을 만든다. 그래서 `GuideCard` 가 '예시' 배지를 **강제로** 붙인다(호출부가 끌 수 없다). 덕분에 **백엔드 작업이 0건**이다 — 새 API 필드도, 계약 변경도 필요 없어 백엔드 세션을 기다리지 않고 프론트만으로 끝냈다
2026-08-21 | **확산** | `components/GuideSection.tsx`(이동·개명) · `components/Faq.tsx` · 화면 9곳 | **하단 안내 절을 통계 3화면 밖으로 넓히고 FAQ 를 붙였다**(사용자 요청). 대상은 메뉴 단위 — 홈 · `/lotto` · `/lotto/latest` · `/lotto/recommend` · 통계 3종 · `/dream` · `/news`. **가이드 5편과 약관·개인정보·면책은 손대지 않았다** — 그 글 자체가 콘텐츠이고, 표본 해부로 바꾸면 오히려 얇아진다. `StatGuide` 를 `components/stat/` 에서 `components/GuideSection.tsx` 로 옮겼다(뉴스·꿈해몽도 쓰게 됐는데 통계 폴더에 두면 이름이 거짓말이 된다). FAQ 는 화면당 4~6문항, `<details>/<summary>` 라 JS 없이 펼쳐지고 FAQPage JSON-LD 를 함께 낸다. 검증: 9화면 × 2폭 구조 확인, 접근성·SEO·모범사례 100 · CLS 0(7화면), axe 라이트·다크 위반 0, 375px 넘침 0, **FAQPage 의 질문·답변이 화면 텍스트와 100% 일치**(자동 대조), 금지 표현 0건
2026-08-21 | ⚠ **함정** | `styles/components.css` | ★ **`.guide-card`·`.guide-grid` 는 이미 임자가 있었다.** 표본 카드에 같은 이름을 붙였더니, 내 규칙이 스타일시트 뒤쪽에 있어 **가이드 문서 링크 카드**(`components/GuideCard.tsx`, 홈·`/guide`·`/lotto` 의 파스텔 카드)를 통째로 덮어썼다. 타입 오류도 빌드 실패도 없어서 눈치채지 못했고, 검증 스크립트가 "표본 0개" 를 보고하면서 드러났다(`querySelector('.guide-grid')` 가 옛 격자를 먼저 잡았다). 접두어를 `spec-` 으로 갈라 해결했고 컴포넌트 이름도 `SpecimenCard`/`SpecimenGrid` 로 바꿨다. **교훈: 새 클래스를 만들기 전에 `grep -n '^\.이름' components.css` 를 먼저 돌린다.** 전역 스타일시트 하나를 공유하는 구조에서는 이름 충돌이 조용히 남의 화면을 망가뜨린다
2026-08-21 | 정정 | `components/Faq.tsx` | **이미 있던 컴포넌트를 읽지 않고 덮어썼다.** 가이드 3편이 쓰고 있었고 원본도 이미 `<details>` + FAQPage 였다. 개선분(중복 id 방지용 `headingId`, JSON-LD 끄기, 인라인 style → CSS 클래스)은 살리되 `headingId` 에 기본값을 둬 기존 호출부가 그대로 동작하게 되돌렸다. **파일을 만들기 전에 있는지 먼저 본다**
2026-08-25 | 운영 | `worker/scripts/worker_local.sh`, `worker/README.md` | **로컬(WSL) 워커 상시 실행 체계 구축.** systemd 가 없는 WSL 이라 `systemctl` 을 쓸 수 없어 멱등 기동 스크립트 + 사용자 crontab 5분 감시로 대체했다. 수집 주기는 여전히 워커 프로세스 안의 APScheduler 가 정하고, OS 크론은 프로세스 생존만 확인한다. 워커가 2026-08-20 00:00 이후 5일간 멈춰 있었고 기동 시 catch-up 이 1238회차(2026-08-22)를 회수했다. 같은 기간 뉴스 5일치는 catch-up 대상이 아니라 결손으로 남는다(기존 32일 결손과 같은 원인)
2026-08-25 | 문구 | `components/Footer.tsx` | **풋터 면책의 추천번호 성격 서술을 바꿨다**(사용자 지시). `추천번호는 참고용·재미용 시뮬레이션입니다` → **`추천번호는 통계와 AI를 이용한 시뮬레이션 결과로 참고용으로만 제공됩니다`**. 종전 문구는 틀린 말은 아니지만 **서비스가 무엇을 하는지 하나도 말하지 않아** 여기까지 읽은 사용자에게 "그래서 이걸 왜 보나" 라는 인상을 남긴다. ⚠ **"AI" 는 사이트 전체 기준으로 쓴 말이다** — 번호추천(`/lotto/recommend`)은 통계 모듈 가중합 + 몬테카를로라 학습된 모델이 없고([[prediction-algorithm]]), 꿈해몽(`/dream`)은 형태소 분석 + 벡터 임베딩 유사도를 실제로 쓴다([[dream-pipeline]]). 두 경로 모두 '추천번호' 를 만들므로 사이트 전체를 가리키는 이 한 문장에서는 성립한다. **개별 화면에서는 그 화면이 실제로 쓰는 방법만 말한다 — 추천 화면에 "AI" 를 붙이지 않는다.** 뒤 문장의 "당첨을 보장하지 않으며" 는 그대로 두었고 금지 표현도 늘지 않았다([[forbidden-expressions]] 목록에 'AI' 는 없다). 색 한 곳·볼드 한 곳 원칙 유지, 375px 넘침 0
2026-08-25 | **문구 전면 정리** | 화면 17곳 · `lib/site.ts` · `lib/share.ts` | **"재미용" 표현을 전부 걷어냈다**(사용자 지시). 틀린 말은 아니지만 서비스가 무엇을 하는지 하나도 말하지 않아 "그래서 이걸 왜 보나" 라는 인상을 남긴다. **화면마다 그 화면이 실제로 쓰는 방법을 말하게** 바꿨다 — 번호추천은 "통계를 활용한", 꿈해몽은 "뜻이 비슷한 단어까지 찾아", 사이트 전체 문구(풋터·면책·layout 메타)는 "통계와 AI를 이용한". ⚠ **개별 화면에 "AI" 를 붙이지 않는다** — 추천 파이프라인에는 학습된 모델이 없다([[prediction-algorithm]]). 정책 문서(`/terms` 제4조·`/disclaimer`)도 "참고용" 으로 맞췄고 "당첨을 보장하지 않는다" 는 전부 그대로다. 공유 이미지에 박히는 `share.ts` 의 문구도 함께 바꿨다
2026-08-25 | ★ **버그** | `styles/components.css` `.faq-list` | **FAQ 한 질문을 펼치면 옆 질문도 빈 채로 펼쳐졌다.** 1024px 이상에서 `.faq-list` 가 2열 grid 인데 `align-items` 가 없어 기본값 `stretch` 였고, grid 는 같은 행의 칸 높이를 맞추므로 한쪽이 열리면 반대쪽 상자도 같이 늘어났다. 그 안은 비어 있으니 "누르지도 않은 질문이 열렸다" 로 읽힌다. `align-items: start` 한 줄로 해결. 실측: 1번만 펼쳤을 때 나머지 다섯은 48px 그대로
2026-08-25 | 수정 | `components/Faq.tsx` · `components.css` | **FAQ 갈매기가 "영역을 다 차지한다"** 는 지적. 크기를 CSS(`.faq-chevron svg`)에만 맡기고 있었는데, 그 규칙이 어떤 이유로든 안 먹으면 SVG 가 부모 칸을 통째로 채운다. **속성(`width`/`height`)으로도 못박아** 원인과 무관하게 재발하지 않게 했고 14px 로 줄였다. 함께: 펼침 상태를 테두리 색 하나가 아니라 배경 + 왼쪽 막대로 알리고, 답변 왼쪽에 세로선을 둬 어느 질문의 답인지 잇는다. **답변 41개를 다시 썼다** — 결론을 먼저 말하고 이유를 뒤에 붙이며, 어려운 말은 예를 하나 들어 푼다(동전 스무 번, "큰 돼지가 집에 들어왔다" 등). 화면마다 도입 한 줄(`intro`)도 넣었다
2026-08-25 | 수정 | `components/GuideSection.tsx` · `components.css` | **표본 카드의 라벨 정렬이 들쑥날쑥했다**(사용자 지적). dt/dd 를 감싼 `<div>` 마다 grid 를 따로 만들어 `max-content` 가 **줄마다 따로 계산**됐고, 설명 글이 시작하는 x 좌표가 제각각이었다. **dl 자체를 2열 grid 로** 바꿔 라벨 칸을 카드 안에서 공유하게 했다(감싸는 div 제거). 9화면 27장 전수 실측에서 어긋난 카드 0
2026-08-25 | **디자인** | `components/icons.tsx` · `GuideSection.tsx` · `components.css` · 화면 9곳 | **하단 안내 절에 디자인을 입혔다**(승인된 계획 실행). 카드마다 파스텔 + 흰 타일 아이콘 + 칩 라벨 + "예:" 줄, 결론 블록에 원형 아이콘, 절 전체를 강조 테두리 상자로 묶음. `icons.tsx` 에 stroke 계열 14종 추가(`STROKE_BASE` 로 굵기 통일, 트로피·달력 없음). ★ **`--spec-ink` 기법** — 참조 시안의 원색 제목은 여섯 색 전부 AA 미달(1.91~3.64)이라 쓸 수 없어 `--color-text` 와 섞었고, `--color-text` 가 테마에 따라 뒤집히므로 **한 줄이 라이트·다크를 동시에 만족**한다. sRGB 로 사전 계산한 비율이 oklab 실제값에서도 통과했다(axe 9화면 × 양 테마 위반 0). 검증: 접근성·SEO 100 · CLS 0, 375px 넘침 0, FAQPage 질문·답변이 화면 텍스트와 100% 일치, 금지 표현 0
2026-08-25 | 문구 | 화면 9곳 · `Footer.tsx` · `lib/share.ts` | **"통계" 만 쓰던 곳에 "AI" 를 병기했다**(사용자 지시). 풋터 소개문은 "통계와 AI 기반 번호추천 정보를 제공하는" 으로. ⚠ **종전 방침을 접었다** — 8/25 오전에는 "개별 화면에는 AI 를 붙이지 않는다" 로 정했으나 사용자가 전역 병기를 지시했다. 근거는 꿈해몽(형태소 분석 + 벡터 임베딩)이고, 번호추천 파이프라인에는 학습된 모델이 없다는 사실은 그대로다([[prediction-algorithm]]). **방법을 구체적으로 주장하는 문장에는 여전히 사실만 쓴다** — 추천 알고리즘 설명에 "학습"·"모델" 을 넣지 않는다. `통계 기반 방식은 회차가 50개 이상…` 같이 랜덤 전략과 구분하는 사실 서술은 그대로 두었다
2026-08-25 | ★ **재설계** | `components/FrequencyChart.tsx` · `components.css` | **막대차트의 가로 스크롤을 없애고 항상 카드 폭에 맞춘다.** 종전에는 통계 상세에서 45개를 1168px 로 펼쳐 스크롤하게 했는데(막대당 24px 로 터치 타겟을 맞추려던 것), 대가가 셋이었다 — ① **readout 이 함께 흘러가** 오른쪽 끝 막대를 짚어도 몇 번인지 알 수 없다(사용자 지적) ② 카드 밖으로 넘친다 ③ 아래 눈금이 막대와 어긋난다. `scroll`/`fit` 두 모드를 없애고 **차트 전체를 하나의 조작면**으로 통일했다(포인터 x → 45등분). 조작 대상이 전폭 하나뿐이라 `target-size` 를 지키면서 호버·탭·좌우 방향키가 모두 동작한다. 실측: 가로 스크롤 컨테이너 0개, 문서 가로넘침 없음, 오른쪽 끝 호버 시 "45번 2회" 가 화면 안에 보임
2026-08-25 | 수정 | `.chart-axis` | **아래 눈금이 막대와 어긋났다**(사용자 지적: "10번째 Bar 밑에 10이 있어야 하는데 엉뚱한 데 있다"). 원인은 `justify-content: space-between` — 여섯 개를 균등 분배하면 10번 막대 중심(전체 폭의 21.1%)이 아니라 20% 자리에 놓이고, 라벨마다 글자 폭이 달라 더 밀린다. **막대와 같은 45열 격자**로 바꾸고 컴포넌트가 `gridColumn` 으로 열을 지정한다. ⚠ 격자 `gap` 은 `.bar-chart` 와 같아야 겹친다(둘 다 2px). ⚠ 양 끝 라벨도 가운데 정렬이다 — 안쪽으로 붙이면 1·45 만 5~7px 어긋나 오히려 눈에 띈다. 실측: 여섯 눈금 전부 **0px**
2026-08-25 | 수정 | `.heat-grid` · `.heat-cell` | **히트맵을 카드 폭에 꽉 채우고 볼을 40% 줄였다**(사용자 지시). 폭 상한 620px 을 없앴고(넓은 화면에서 번호판이 왼쪽에 몰려 보였다), 칸 여백을 7% → 24% 로 올려 **볼이 칸의 86% → 52%** 가 되게 했다. 볼이 칸을 덮어 히트 배경(=빈도)이 안 보인다는 지적을 푼 것이다. 글자도 36cqw → 21cqw 로 함께 줄였다. ⚠ **좁은 화면은 반대로 보정한다** — 375px 에서 칸이 24px 라 같은 비율이면 볼이 12px 로 읽을 수 없다. `max-width: 639px` 에서 여백 10%·글자 32cqw 로 되돌린다. 실측: 1280px 격자 1030px·칸 97px·볼 50px(52%), 375px 볼 19px(79%)
2026-08-25 | 수정 | `.faq-list` · `.faq-item` · `.faq-answer` | FAQ 를 **항상 한 줄씩** 쌓는다(사용자 지시). 1024px 이상 2열을 없앴다 — 질문이 좌우로 흩어져 읽는 순서가 흐려지고, grid 행 높이 맞춤 때문에 옆 항목이 같이 늘어나는 문제의 근원이기도 했다. 펼침 항목의 **왼쪽 세로 막대와 답변 왼쪽 세로선을 걷어냈다**(장식 과하다는 지적). 상태는 배경 하나로 읽히고 `<details>` 가 이미 말해 준다
2026-08-25 | 수정 | `RecommendStudio.tsx` · `.strategy-picker` | **추천 기준 선택을 탭 트랙에서 버튼 줄로 바꿨다**(사용자 지시). 종전 `.tabs` 는 `overflow-x: auto` 라 **모바일에서 아래에 가로 스크롤바가 생겨** 메뉴가 아니라 잘린 목록처럼 보였다. 스크롤바를 감추고 `scroll-snap-type: x proximity` 로 **손가락으로 밀면 버튼 왼쪽에 맞춰 멈추게** 했으며, 감춘 대신 오른쪽 가장자리에 페이드를 둬 더 있다는 것을 알린다. 640px 이상에서는 줄바꿈해 한눈에 다 보이므로 페이드도 끈다. ⚠ `role="tablist"` 를 버렸다 — 탭 위젯은 방향키 이동·단일 Tab 스톱 규약인데 이 버튼들은 누르면 **결과가 새로 생성**되므로 탭 전환이 아니라 실행이다. `aria-pressed` 가 맞는 표현이다. 칩 높이 44px
2026-08-25 | 기능 | `RecommendStudio.tsx` · `.set-count` | **조합 개수를 1~10 에서 고를 수 있게 했다**(사용자 요청). 계약이 허용하는 범위 그대로다([[api-contract]]) — 선택지를 줄이면 "왜 5개만 되지" 가 된다. 서버가 처음 넘겨준 개수를 초기값으로 이어받아 화면과 값이 어긋나지 않는다. 칩은 44×44 를 지키고 좁은 화면에서 줄바꿈한다. CTA 문구도 개수를 따라간다("3조합 다시 생성")
2026-08-25 | 기능 | 신규 `components/BulkActions.tsx` · `lib/share.ts` | **생성된 조합을 한 번에 복사·저장·공유**한다(사용자 요청). 개별 버튼(`NumberActions`)은 그대로 둔다 — 마음에 드는 하나만 가져가려는 사람이 훨씬 많다. `share.ts` 에 `buildShareTextAll`·`drawSetsImage`·`shareSets` 를 더했다. ⚠ 텍스트는 조합에 **번호를 매기고 면책은 맨 아래 한 번만** 붙인다(조합마다 반복하면 번호가 묻힌다). ⚠ 이미지는 정사각형이 아니라 **세로로 긴 한 장**이다 — 조합 수에 따라 높이가 달라지므로 레이아웃 상수를 위에 모아 두었다. 볼 그리는 방식은 `drawNumbersImage` 와 같아야 한다(두 이미지가 나란히 공유될 수 있다). 실측: 10조합 복사 15줄·면책 포함, 이미지 `행운상자-통계 종합-10조합.png` 생성
2026-08-25 | 디자인 | `app/lotto/recommend/page.tsx` · `.strategy-list` | **'추천 기준 여섯 가지' 를 줄글에서 번호 목록 카드로** 바꿨다(사용자 요청 — "화려할 필요는 없으나 조금 성의있게"). 참조 시안(`docs/raw/추천알고리즘설명화면.png`)의 문법을 따랐다 — 번호 배지 + 제목 + 한 줄 요약 칩 + 설명. ⚠ **번호를 붙인 이유는 순서에 뜻이 있기 때문**이다: 1번 완전 랜덤이 통제군이고 나머지는 그것과 견주라는 배치다([[prediction-algorithm]] · `lib/strategies.ts`). 그래서 완전 랜덤만 배지를 중립색으로 둬 나머지 다섯과 구분한다 — 똑같이 생기면 그냥 첫 항목으로만 읽힌다. 1024px 이상 2열. 검증: 접근성 100 · SEO 100 · CLS 0 · target-size 통과, axe 라이트·다크 위반 0, 375px 넘침 0
2026-08-27 | 수정 | `RecommendStudio.tsx` · `.strategy-picker-wrap` | ★ **"밀 수 있다" 를 알리는 신호를 하나에서 셋으로 늘렸다.** 오른쪽 페이드만 뒀더니 **"그냥 보면 메뉴 3개만 있는 것처럼 보인다"** 는 지적을 받았다(사용자). 셋이 함께 말하게 했다 — ① **잘린 칩**(페이드 폭을 `--space-8` → `--space-6` 으로 줄여 4번째 칩이 반쯤 비친다. 잘린 것이 보이는 것이 가장 강한 신호다) ② **좌우 화살표**(넘칠 때만 렌더링. "더 있다" 를 말할 뿐 아니라 마우스만 쓰는 좁은 창에서 실제 이동 수단이 된다. 한 번에 보이는 폭의 80% 씩 민다 — 100% 면 경계의 칩을 건너뛴 것처럼 보인다) ③ **안내 문구**("옆으로 밀면 6가지 기준을 모두 볼 수 있어요"). 셋 다 끝까지 밀면 사라지고 640px 이상에서는 아예 나오지 않는다(줄바꿈해 다 보인다). ⚠ **페이드는 스크롤 컨테이너가 아니라 바깥 래퍼에 건다** — 스크롤되는 요소 안에 absolute 로 두면 내용과 함께 흘러가 가장자리에 머물지 못한다. ⚠ **안내 문구는 조건부 렌더링하지 않는다** — `canRight` 가 마운트 뒤에 정해져서 없다가 생기면 아래가 밀린다(실측 CLS 0 → 0.02). 항상 그리고 `visibility` 만 토글해 0 으로 되돌렸다. 실측: 375px 온전히 보이는 칩 3 · 잘린 칩 1 · 화살표 1 · 안내 표시, 끝까지 밀면 셋 다 사라짐. 640/768/1280px 은 화살표 0 · 안내 `display:none`. 접근성 100 · SEO 100 · CLS 0
2026-08-27 | **백엔드 요청** | `40-domain/dream-pipeline.md` | **꿈 번호 부족분 채우기를 무작위 → 통계 기반으로 바꿔 달라**(프론트 세션이 남김). 사용자가 꿈 화면에서 "모자란 번호는 통계적 추천으로 채웠다" 고 안내하기를 원하는데, 현재 `_generate_combo` 는 `1~45 무작위`라 그렇게 쓰면 거짓말이 된다. **응답 스키마 변경은 필요 없다** — 프론트가 `tiers.tierN.pool` 과 세트 `numbers` 를 비교해 '꿈에서 온 번호' 와 '채워진 번호' 를 이미 구분한다. 채우는 방법만 바뀌면 된다. 그때까지 프론트는 "나머지는 무작위로 채웠습니다" 라고 사실대로 적어 둔다. **하지 않아도 화면은 정상 동작하므로 급하지 않다.** 나머지 꿈 화면 개편(개수 1~10·분석 결과 상세·세 가지 추천 방식·전체 내보내기)은 계약이 이미 다 지원하므로 프론트만으로 끝낸다 — `sets_per_tier`(1~30)·`gubun`(1 정확/2 포함/3 유사)·누적 tier 가 전부 있다([[api-contract]])
2026-08-27 | 구현 | 신규 `app/domain/number_scores.py` · `app/dream/generator.py` · `app/dream/service.py` · `app/routers/dream.py` | **꿈해몽 부족분 채우기를 무작위 → 통계 기반으로 바꿨다**(프론트 세션의 위키 요청, [[dream-pipeline]]). 화면이 "모자란 번호는 통계로 채웠습니다" 라고 안내하려면 그 말이 사실이어야 한다. **응답 스키마 변경 0건** — 프론트가 `pool` 과 `numbers` 를 대조해 이미 두 종류를 구분하므로 채우는 방법만 바꾸면 된다는 요청 그대로다. 점수는 `/api/lotto/recommend` 의 `ensemble` 과 **같은 함수**에서 나온다(분석기 4종 + `ensemble.score`) — 두 화면이 "눈여겨볼 번호" 를 다르게 계산하면 사용자는 어느 쪽도 믿지 않는다. ⚠ **몬테카를로는 타지 않는다.** 그 5만 회는 점수 *이후* 조합을 고르는 단계이고 동시 실행 게이트까지 걸려 있어([[0012-serialize-monte-carlo]]), 태웠으면 꿈해몽이 추천과 같은 대기열에서 굶었다. ⚠ **꿈에서 나온 번호에는 가중치를 걸지 않는다** — 풀 안의 선택까지 통계가 흔들면 프론트의 `pool` 대조가 뜻을 잃는다. 점수는 최신 회차를 키로 캐시한다(주 1회만 바뀐다). 실측(1,238회차): 분석기 24.9ms(캐시 미스 1회만) · 3 tier × 30세트 생성 4.4ms · 꿈해몽 warm 요청 0.07초 · 점수 0.318~0.718(2.3배) · 채움 3,000회에서 상위5 번호가 하위5 대비 **1.75배**(431 vs 247). **되돌아가는 경우는 둘뿐이다**(회차 50 미만 / DB 조회 실패) — 그때는 균등 무작위로 조용히 되돌아가되 로그에 시끄럽게 남긴다. 채움 방식 하나 때문에 멀쩡한 화면을 500 으로 만드는 것이 더 나쁘고, 두 경우 모두 운영 정상 상태에서는 발생하지 않는다. 테스트 21개 추가(전체 **166개 통과**), `sets=3&seed=1` 두 호출 바이트 동일 유지, 금지 필드·표현 0건. **워커·DB·계약 무변경**
2026-08-27 | ⚠ **함정** | `app/domain/number_scores.py` | **`rng.choice(replace=False, p=...)` 는 0 이 아닌 항목이 뽑을 개수보다 적으면 예외를 던진다**(`Fewer non-zero entries in p than size`). 실제 앙상블 점수는 패턴 항의 하한 덕에 모든 번호가 0.1 이상이라 도달하지 않지만, **음수·0 을 잘라내는 방어 코드가 오히려 꿈해몽 전체를 500 으로 만드는** 형태였다 — 테스트가 잡았다. 되돌아가는 방식을 균등이 아니라 **2단 추출**로 했다: 점수가 있는 번호를 먼저 전부 가중으로 뽑고 남는 자리만 균등으로 채운다. 통째로 균등으로 돌아가면 멀쩡히 존재하는 통계 신호를 버리게 된다
2026-08-27 | 요청 | `40-domain/dream-pipeline.md` | **백엔드 세션에 요청.** 꿈 풀이 6개 미만일 때 `_generate_combo` 의 부족분 채우기를 무작위 대신 `recommend` 의 `ensemble` 점수 기반으로 바꿔 달라. 응답 변경은 필요 없고 급하지 않다. 프론트는 그동안 "무작위로 채웠다" 고 화면에 고지한다
2026-08-27 | 생성 | `00-decisions/0013-dream-meanings-in-frontend.md` | 색인 대상 10개 소재의 민간 해석 풀이를 `frontend/src/lib/dream-meanings.ts` 상수로 둔다. **판정 기준은 개수** — 색인 대상이 수백 개가 되면 이 결정을 뒤집는다
2026-08-27 | 수정 | `20-design/components.md` | ScrollArea 절 신설(신호 셋·sticky 레일·`aria-hidden` 함정·JS 없는 대안이 통하는 조건), DreamStudio 절 신설(입력 상한·gubun 표시 규칙·채움 고지·`sourcePath`)
2026-08-27 | 수정 | `20-design/components.md` | **낡은 서술 정정.** FAQ 가 "데스크톱 2열" 로 적혀 있었으나 2026-08-25 사용자 지시로 1열로 되돌렸다. 코드가 맞고 문서가 틀렸다
2026-08-27 | 발견 | `20-design/components.md` | 배경 스크롤 그림자(`background-attachment: local`)는 **내용이 배경을 가리는 곳에서는 통하지 않는다.** 표·히스토그램에 걸었다가 그림자가 아예 나타나지 않아 되돌렸고, 그쪽은 화살표 방식으로 갈랐다
2026-08-27 | 수정 | `20-design/components.md` | 홈 캐러셀 화살표가 **끝에 닿아도 남아 있던** 문제를 고치고 규칙을 기록. 갈매기를 ScrollArea 와 같은 SVG 로 통일
2026-08-27 | 발견 | `40-domain/dream-pipeline.md` | **`dream_word` 는 사용자가 적은 단어가 아니다.** 형태소 분석 1단계의 유의어 확장 때문에 `집` → `집안`·`건물` 이 딸려 오고, 어간 되돌리기 실패로 `크함`·`꾸음` 같은 비단어가 섞인다. `건물` 은 표제어와 정확히 일치해 `gubun=1` 로 내려온다. 실측 기록
2026-08-27 | 요청 | `40-domain/dream-pipeline.md` | **백엔드 세션에 요청.** `_stem_to_noun` 이 만든 비단어를 후보에서 걸러 달라. 가능하면 `matched_words[].from_text` 로 원문 유래 여부를 표시해 달라(계약 추가라 승인 필요). 프론트는 그동안 문자열 포함으로 갈라 "넓혀 본 단어" 로 접어 둔다
2026-08-27 | 수정 | `00-decisions/0013`, `30-seo/metadata-strategy.md` | 색인 대상 꿈 소재를 10 → **30개**로 늘리고 전부 민간 해석을 썼다. 사이트맵 1,287 URL. `DREAM_INDEXED_KEYWORDS` 를 `DREAM_MEANINGS` 키에서 끌어와 **풀이 없이 색인되는 일이 구조적으로 불가능**하게 했다
2026-08-27 | 수정 | `20-design/components.md` | DreamGuide 절 신설(두 페이지 하단 공통화, FAQPage 단일 출처), DreamStudio 절에 `dream_word` 함정·추천 개수 부족 알림 추가. 라벨 "조합 개수" → **"추천 개수"**(꿈해몽·번호추천 양쪽)
2026-08-27 | 발견 | — | `components.css` 에 `.scroll-area`·`.dream-*` 블록이 **428줄 통째로 두 벌** 들어가 있었다(앞선 삽입 작업의 사고). 뒤 사본이 이겨 `.dream-lore` 수정이 화면에 반영되지 않았다. 제거 후 클래스 중복 전수 검사로 확인
2026-08-27 | 수정 | `20-design/components.md` | 꿈해몽 조합의 번호마다 **출처를 두 글자 라벨**(일치·포함·비슷·관련·채움)로 붙이는 규칙 기록. 색만으로 나누지 않고 선 모양과 글자로 이중 부호화. 320px 에서 볼 6개 + 라벨 실측 통과
2026-08-27 | 수정 | `20-design/components.md` | 번호 목록을 **제외 스위치**로 만든 규칙 기록(44px 타겟, 취소선+`aria-pressed`, 재요청 없이 3배수 선수신 후 필터). "표제어" 같은 어려운 말을 쓰지 않는다는 문구 규칙도 명문화
2026-08-27 | 요청 | `40-domain/dream-pipeline.md` | **백엔드 세션에 요청.** `/recommend` 에 `exclude: number[]` 를 달아 달라. 없어서 프론트가 3배로 받아 거르고 있다. 생기면 그 우회를 걷어낸다
2026-08-27 | 데이터 | `data/chroma_words/` · 신규 `data/custom_words.json` · 신규 `scripts/add_custom_words.py` | **꿈 사전에 `이재명 → [1, 32]` 을 넣었다**(사용자 지시). 4,802 → 4,803 단어. **전체 재임베딩은 하지 않았다** — 새 단어 하나만 같은 모델(`upskyy/kf-deberta-multitask`, 768차원)로 인코딩하면 기존 벡터들과 같은 공간에 놓인다. ⚠ **사전에 직접 쓰지 않고 스크립트로 넣은 이유**: `data/chroma_words/` 는 `.gitignore` 되어 git 에 없고 원본 데이터도 남아 있지 않다 — 직접 넣으면 그 변경이 이 PC 에만 존재하고 배포하는 순간 조용히 사라진다. '무엇을 더할 것인가' 는 git 에 들어가는 JSON 에, '반영' 은 `upsert` 스크립트에 뒀다. ⚠ **적용 전 백엔드 서버를 멈춰야 한다** — 떠 있던 8005 서버의 multiprocessing 자식이 HNSW 인덱스(`data_level0.bin` 등)를 열고 있었다. 동시에 쓰면 복구 불가능한 자산이 상한다. 스크립트가 `lsof` 로 감지해 거부한다. 절차: 36MB 백업 → **사본에서 전 과정 검증** → 서버 정지 → 원본 반영 → 재기동 → 실제 API 200 확인(`이재명` gubun=1 → [1, 32]). kiwipiepy 가 `이재명` 을 `NNP` 로 잡아 `"이재명이 나왔다"` `"이재명 대통령이"` 등 모든 어형에서 추출되는 것을 먼저 확인했다 — 사전에 넣어도 형태소 분석이 못 뽑으면 검색에 도달하지 못한다. 원본에 `문재인 → [1, 32]` 이 이미 있어 값과 관례가 일치한다. ⚠ **`backend/CLAUDE.md` 의 '`data/chroma_words/` 에 쓰기 금지' 를 '직접 쓰지 않는다 — 스크립트로만' 으로 고쳤다**. 금지 자체가 목적이 아니라 추적 불가능한 변경을 막는 것이 목적이었다
2026-08-27 | ⚠ **함정** | `scripts/add_custom_words.py` | **점유 프로세스 검사가 자기 자신을 잡아 항상 실행을 거부했다.** `lsof +D` 를 ChromaDB 클라이언트를 연 *뒤에* 불렀기 때문이다 — 스크립트 자신이 그 디렉토리를 열고 있다. 검사를 **사전을 열기 전으로** 옮기고 `os.getpid()` 를 제외해 고쳤다. 안전장치가 항상 발화하면 사람은 그것을 끄는 법을 먼저 배운다
2026-08-28 | 수정 | `frontend` | 푸터의 `/sitemap.xml` 링크 제거. 크롤러는 `robots.txt` 의 `Sitemap:` 으로 찾고, 사람에게는 XML 원본이 뜰 뿐이다. 사람용 사이트맵 역할은 푸터 메뉴가 한다
2026-08-28 | 발견 | `30-seo/metadata-strategy.md` | **회차 상세가 색인 URL 의 96%인데 본문의 84%가 공통 문구**였다(1238 vs 1237 실측: 고유 낱말 36 / 공통 186). 사이트맵 규모(1,287 URL = 구글 한도의 2.6%)는 문제가 아니었다
2026-08-28 | 수정 | `20-design/components.md`, `30-seo/metadata-strategy.md` | 회차마다 다른 본문을 붙였다(`RoundNumberHistory`) — 그 회차 여섯 번호의 **그때까지** 누적 횟수·순위·직전 출현 이후 쉰 회차. 고유 낱말 36 → **77**, 본문 1,150 → 1,786자
2026-08-28 | 발견 | `10-contracts/api-contract-stats.md` | `/stats/number/{n}` 에 기간을 줘도 **`rounds_since`·`last_seen_round`·`max_gap` 은 최신 회차 기준**이다(계약대로이나 실측으로 재확인). 과거 회차 화면에서 쓰면 거짓이 되므로 프론트는 `recent_appearances` 로 직전 출현을 찾아 뺄셈한다
2026-08-28 | 발견 | — | 빌드 중 `[api] 요청 실패` 374건·212건이 났으나 원인은 프론트 부하가 아니라 **백엔드가 `--reload` 로 떠 있어 다른 세션의 수정마다 재시작된 것**이었다(전부 `fetch failed`, timeout 0건). 백엔드가 안정된 뒤 같은 조건(회차 300장 × 7요청)으로 재빌드하니 **실패 0건**. 사전 생성 상한은 300 그대로 둔다
2026-08-28 | 수정 | `20-design/components.md` | **백엔드 장애를 404 로 굳히지 않는다.** `getJsonOrThrow` 로 없음(404)과 장애를 갈라, 회차 상세는 장애면 던진다 — 그 렌더는 캐시되지 않는다. 목 백엔드를 껐다 켜 실측: 장애 중 500 → 복구 후 같은 회차 200(종전엔 404 가 일주일 유지). 진짜 없는 회차는 404 그대로
2026-08-28 | 발견 | `20-design/components.md` | **같은 데이터라도 화면에 따라 장애 처리가 갈린다.** `/lotto/stat` 은 `getNumberStat` 으로 구현 여부만 떠보므로 삼키고, 회차 상세는 `getNumberStatStrict` 로 던진다. 판정 기준은 "그 데이터가 없을 때 페이지가 성립하는가"
2026-08-28 | 계약 | `10-contracts/*`, `40-domain/forbidden-expressions.md`, `90-external/youtube-data-api.md` | **유튜브 영상 수집 계약 신설.** 워커에 `video_channel`·`video_search`·`video_refresh` 잡 3종과 `lotto_video` 테이블을 더한다. 잡을 셋으로 나눈 이유는 쿼터 버킷(`search.list` 는 하루 100회 별도 버킷)·락·실패의 의미가 각각 다르기 때문이다 — 갱신 실패만 정책 위반이다. 명명 표준에 접미어 둘을 신설했다: `_key`(외부 시스템이 발급한 문자열 식별자, `_id` 는 bigint 대리키 전용이라 재사용 불가)와 `_sec`(초 단위 길이). `_cnt` 가 bigint 일 수 있다는 단서도 추가(조회수는 int4 상한을 넘는다). 백엔드는 `GET /api/videos` 를 만들되 **응답 캐시 24시간 상한**(YouTube 30일 보관 정책)과 **표시 조건 WHERE 절**을 반드시 지킨다. `forbidden-expressions` 에 "외부에서 들어온 텍스트" 절을 신설했다 — 지금까지 우리가 쓰는 문구만 다뤘으나 영상 제목은 남이 쓴 금지 표현이 우리 화면에 뜨는 첫 사례다. 계획 근거는 [`004-유튜브영상수집계획.md`](../raw/004-유튜브영상수집계획.md)
2026-08-28 | 구현 | `worker/**` | **유튜브 영상 수집 워커 구현 완료.** `lotto_video` 테이블(마이그레이션 `0002`, DDL 과 한 글자 일치)과 잡 3종을 추가했다. 신규 8파일(`sources/youtube_data.py`·`sources/llm_judge.py`·`jobs/video_common.py`·`jobs/video_{channel,search,refresh}.py`·DDL·마이그레이션), 수정 6파일. **의존성을 늘리지 않았다** — httpx 로 두 API 를 부르고 테스트는 표준 `unittest` 다. 검증: 단위 46건 통과, DB 경로 8항목(신규/중복/승격/분류/선조회/제목중복/만료삭제/정책감시) 기대대로, 금지 표현 3중 방어 실측 **10/10**(채널 4 · 규칙 1 · LLM 1). 채널 차단을 빼면 13건 중 11건으로 떨어져 세 층이 각각 다른 것을 잡는다는 것이 확인됐다 — `gpt-5-nano` 는 "이번주 로또 1등 예상 조합 5게임 공개" 처럼 제외어 목록에 없는 표현을 잡고, 반대로 "1238회 참고로보는 조합결과" 처럼 제목만으로 애매한 것은 채널 차단이 맡는다. `app_reader` 권한이 `ALTER DEFAULT PRIVILEGES` 로 자동 부여됨을 `pg_default_acl` 로 확인했다(백엔드가 죽지 않는다). **남은 것은 `YOUTUBE_API_KEY` 발급뿐이다** — 키가 없으면 세 잡이 크론에 등록되지 않고 `lotto`·`news` 는 정상 동작한다
2026-08-28 | TBD | `50-ops/deployment.md` | **운영 DB·이미지 무게 조사 기록**(사용자 질문에서 나옴, 결정 아님). 실측: Postgres 컨테이너 **66MB** · DB 전체 **9.5MB** · backend `.venv` **5.2GB**(그중 `nvidia` 2.7GB + `torch` 1.1GB) · worker `.venv` 60MB. ★ **무게는 DB 가 아니라 torch 에 있다** — Postgres 는 전체의 1% 남짓이고, CUDA 2.7GB 는 개발 PC 에 GPU 가 있어 uv 가 집은 것이라 배포 서버에서는 한 번도 안 쓰인 채 실린다. CPU 전용 휠로 바꾸면 3.8GB → 200~300MB. **DB 를 바꾸는 것보다 10배 큰 레버**다(다만 CPU 휠 실측은 아직 안 했다). 이 문서가 종전에 '이미지가 수백 MB' 라고 적은 것은 과소평가였고 정정했다. 관리형 후보: **Supabase 무료 티어는 자동 백업이 없고**(PITR·일 단위 백업은 유료) 7일 미사용 시 프로젝트가 일시정지된다 → 백업 부담이 지금 Docker 와 같아진다. Neon 은 scale-to-zero 가 자동 복귀이고 무료에서도 짧은 복원 창을 준다. ⚠ 무료 티어 조건은 자주 바뀌므로 **정할 때 직접 확인**한다. SQLite/Turso 는 권장하지 않는다 — 확정된 worker·backend 컨테이너 분리가 무의미해진다(파일이라 같은 볼륨을 공유해야 한다). 덧붙여 DB 가 9.5MB 라 `pg_dump | gzip` 이 1MB 안팎이므로, 관리형의 진짜 이점은 백업이 아니라 **호스트를 안 돌봐도 되는 것**이다
2026-08-28 | 구현 | 신규 `app/routers/video.py` · `repository.py` · `schemas.py` · 신규 `tests/test_video.py` | **워커의 유튜브 계약 구현 — `GET /api/videos` · `GET /api/videos/{id}`.** 계약이 이 리소스에만 건 제약 셋을 전부 **코드로 강제**했다. ① **캐시 24시간 상한** — `_MAX_CACHE_SECONDS` 를 두고 모듈 로드 시 `assert` 로 검사한다. 주석으로만 적으면 값을 올리는 사람이 주석을 읽지 않는다. 실제 값은 1시간(`Cache-Control: public, max-age=3600`)으로 상한에 여유를 뒀다 — 바싹 붙여 두면 누군가 '조금만 더' 를 더하는 순간 위반이 된다. 헤더로 내보내는 이유는 프론트뿐 아니라 중간 프록시·CDN 도 규약을 알아야 하기 때문이다. ② **표시 조건 WHERE 절** — `_VIDEO_VISIBLE` 상수 하나를 목록·상세가 함께 쓴다. 상세에도 거는 이유: 목록에서 숨긴 영상을 URL 만 알면 볼 수 있게 두면 거른 의미가 없다(그 경우 404). ③ **`shorts_hint` 를 boolean 으로 바꾸지 않는다** — `unknown` 은 `normal` 에도 `shorts` 에도 넣지 않고 `all` 에서만 보인다. ④ **`game='lotto'` 일 때만 `lotto_draw` 와 조인.** 회차 연결을 프론트에 맡기지 않고 백엔드가 붙이는 쪽으로 정했다(계약이 어느 쪽이든 좋다고 열어 둠) — 맡기면 `game` 검사를 잊을 위험이 프론트로 옮겨갈 뿐 사라지지 않는다. 추첨 전 회차라 조인 결과가 없는 것은 **정상 경로**이고 404 가 아니다. **워커가 그 사이 마이그레이션을 돌려 `lotto_video` 가 실재했다**(0행). 덕분에 통합 테스트가 실제 스키마에 대해 돌았고 `app_reader` 의 SELECT 권한도 정상이었다 — `ALTER DEFAULT PRIVILEGES` 함정에 걸리지 않았다. 테스트 11개 추가
2026-08-28 | **계약 변경** | `10-contracts/api-contract.md` · `app/dream/**` · `app/schemas.py` | **꿈해몽에 `exclude` 와 `from_text` 를 더했다**(프론트 세션의 위키 요청 2건). 둘 다 추가만 하는 하위호환 변경이라 사용자 지시대로 사전 승인 대신 이 기록으로 갈음한다. ① **`exclude: number[]`(최대 39)** — 프론트가 3배수로 받아 걸러 내던 우회를 걷어낸다. 번호는 **풀에서도 채움에서도** 빠지고 응답의 `pool` 에서도 빠진다(그래야 프론트의 `pool`↔`numbers` 대조가 계속 성립한다). 상한이 39인 이유는 6개를 만들려면 6개가 남아야 하기 때문이고(45−39=6), 40개 이상과 범위 밖은 422 다 — 조용히 무시하면 사용자는 뺐다고 믿는데 그 번호가 계속 나온다. ② **`matched_words[].from_text`** — 판정은 원문에 대한 단순 문자열 포함으로, 프론트가 이미 하던 규칙을 그대로 서버로 옮겼다. 바꾸면 화면의 분류가 조용히 달라진다. ③ **비단어 필터** — `_stem_to_noun` 이 만든 문자열은 **정확 일치가 있을 때만** 쓴다. 실측으로 `크함` 이 `큰북`·`큰방`·`대형` 을 데려와 번호 3개를 보태고 있었다(사용자는 '큰' 이라고 썼을 뿐이라 출처를 설명할 방법이 없다). 통째로 막지 않은 이유는 정확 일치가 있으면 사전에 실재하는 표제어라 규칙이 우연히 맞은 것이기 때문이다. **응답 필드는 그대로이고 근거 없는 항목만 사라진다.** 구현은 `analyzer.analyze_detailed()` 가 후보마다 `from_text`·`derived` 를 함께 돌려주는 방식이고, 기존 `analyze()` 는 그 위의 얇은 래퍼로 남겼다 — 두 함수가 다른 규칙으로 갈라지지 않게 한 쪽만 구현한다. 실측: `'큰 돼지가 나왔어요'` → 종전 `크함`·`돼지`·`나오음` 셋이 매칭되던 것이 **`돼지` 하나만** 남았다. `seed` 재현성 유지. 테스트 7개 추가, 전체 **184개 통과**
2026-08-28 | 구현 | `frontend` | **유튜브 영상 계약을 프론트에 반영했다.** `/videos` 목록(갈래 필터·쪽 이동·설명·FAQ), `/videos/{id}` 상세(임베드 → 당첨번호 → 당첨금 → 번호구성 → 광고 → 다른영상 순), 홈 영상 3편, 네비·사이트맵. 백엔드 `/api/videos` 는 아직 404 라 목으로 검증했고, 없으면 화면이 비는 구조다
2026-08-28 | 수정 | `20-design/components.md`, `30-seo/metadata-strategy.md` | 영상 화면의 **정책 제약**을 기록. 출처 표시(III.F.2)는 홈 카드까지, 광고는 플레이어와 떨어진 자리에(III.G.1.c), **회차를 못 붙이는 영상에는 광고 슬롯 자체를 그리지 않는다**(III.G.1.d 독립적 가치). 제목은 고치지 않고 `<title>` 만 우리가 짓는다. `shorts_hint` 의 `unknown` 은 전체에서만 보인다
2026-08-28 | 수정 | `frontend/src/app/privacy/page.tsx` | **개인정보처리방침에 '7. 유튜브 영상의 재생' 절 신설.** `youtube-nocookie.com` 을 써도 제3자 쿠키가 실제로 발생하는 것을 실측했다(Lighthouse `inspector-issues`). 영상을 걸었으면 방침에도 적어야 한다. 뒤 절 번호를 하나씩 밀었다
2026-08-28 | 발견 | — | 영상 상세의 Lighthouse **모범사례 96**(다른 화면은 100). 원인은 유튜브 임베드가 만드는 쿠키 이슈이고 **우리가 고칠 수 없다** — 임베드를 쓰는 이상 불가피하다. 목록·홈은 100 이다
2026-08-28 | 생성 | `50-ops/crawling-defense.md` | **크롤링 방어의 순서를 기록.** 당첨번호는 동행복권 공식 자료라 막아도 원천에서 가져가고, 이 사이트는 유입이 전적으로 검색이라 방어 대부분이 검색 크롤러도 함께 막는다. **진짜 열린 곳은 HTML 이 아니라 공개된 백엔드 API** — 실측 `size=200` 7번 호출 0.2초면 전 회차가 나온다. 그쪽을 닫지 않은 채 HTML 만 막는 것은 의미가 없다
2026-08-28 | 수정 | `frontend/src/app/robots.ts` | AI 학습 수집기 20종·상업 SEO 도구 10종을 선별 차단. **검색·광고 크롤러는 전부 허용 유지**(Googlebot·Yeti·Daum·bingbot·Applebot·PetalBot·AdsBot-Google 통과 실측). `Google-Extended`·`Applebot-Extended` 는 검색 색인과 무관해 안전하게 막을 수 있다 — 본체와 혼동하면 사이트가 검색에서 사라진다. Lighthouse SEO 100 유지
2026-08-28 | 알림 | `10-contracts/api-contract.md` · `40-domain/dream-pipeline.md` · `20-design/components.md` | **프론트 세션에게 — 기다리던 것 셋이 전부 살아 있다.** ① `GET /api/videos` · `GET /api/videos/{id}` 가 **200 을 반환한다**(프론트 로그의 "아직 404 라 목으로 검증" 은 그 시점의 사실이고 지금은 아니다). `lotto_video` 가 비어 있어 `{"total":0,...,"items":[]}` 를 주지만 **404 가 아니다** — 워커가 `YOUTUBE_API_KEY` 를 받으면 그대로 채워진다. `/videos/{id}` 는 `game='lotto'` 이고 그 회차가 있을 때만 `draw` 를 담으므로, **광고 슬롯을 그릴지 판단하는 근거로 `draw` 의 null 여부를 그대로 쓰면 된다**(III.G.1.d 독립적 가치). ② 꿈해몽 `exclude: number[]`(최대 39) — `components.md` 의 `FETCH_MULTIPLIER` 3배수와 프론트 필터링을 걷어낼 수 있다. 다만 서버로 보내면 매번 재요청이 되므로 '다시 요청하지 않는다' 와 어느 쪽을 택할지는 프론트 결정이다. ③ `matched_words[].from_text` — 프론트의 문자열 포함 판정을 걷어낼 수 있다(같은 규칙을 서버로 옮긴 것이라 분류가 달라지지 않는다). `dream-pipeline.md` 의 백엔드 요청 블록 둘을 완료 기록으로 닫았다
2026-08-28 | 검증 | `tests/test_video.py` | **표시 조건이 '들어 있는가' 가 아니라 '거르는가' 를 확인했다.** 문자열 검사만으로는 조건이 뒤집혀도(`=` vs `<>`) 통과한다. `lotto_video` 는 워커 소유라 건드리지 않고 **같은 모양의 임시 테이블**(세션 한정)에 8행을 넣어 repository 가 조립한 SQL 을 그대로 돌렸다. 결과: `private`·`unlisted`·`embeddable=no`·`made_for_kids=yes`·`embeddable=unknown` 이 전부 걸러지고, `shorts_estimate_cd='unknown'` 은 `normal`·`shorts` 어느 쪽에도 안 들어가며, **연금복권 330회가 로또 330회로 새지 않는다.** 상태를 모르는 것(`unknown`)은 보여주지 않는다 — 모르면 안전한 쪽으로 넘어간다. 테스트 12개
2026-08-28 | 계약·구현 | `10-contracts/db-schema.md`, `api-contract.md`, `worker-jobs.md`, `env-vars.md`, `worker/**` | **수집 이력 상세화 + 운영자 전용 로그 화면 계약.** `collect_job_log` 에 `stat_json`(단계별 통과 건수)·`log_list`(그 실행의 WARNING 이상 전부) 두 컬럼을 더했다(마이그레이션 `0003`). 종전에는 `collected_cnt` 와 `error_desc` 뿐이라 **왜 0건인지 알 수 없었고**, 잡을 죽이지 않고 넘어간 문제(채널명 변경·LLM 판정 실패·배치 건너뜀)가 아무 데도 안 남았다. 로그 캡처는 `contextvars` 로 잡별 격리한다 — 워커는 1프로세스지만 잡별 락이라 여러 잡이 동시에 돌고, 전역 버퍼면 두 잡의 로그가 섞인다. 실측(`video_search`): `fetched 119 → on_topic 103 → rule_clean 70 → llm_clean 52 → stored 50` — 한 줄이 "규칙 33건·LLM 18건을 걸렀다"를 말해 준다. 워커에 `/internal/jobs/logs`(루프백+X-Job-Key)를 더했고, **공개 관리자 화면은 백엔드·프론트가 만든다**(워커는 관리자 UI 를 제공하지 않는다) — `POST /api/admin/login`·`GET /api/admin/job-logs` 계약을 [[api-contract]] 에 적었다. 인증은 `ADMIN_TOKEN` 환경변수 하나이고 `secrets.compare_digest` 비교, **빈 값이면 503**(빈 문자열끼리는 비교가 통과해 아무나 들어온다), 쿠키에는 토큰 원문 대신 서명값을 담는다. 화면은 `noindex`·사이트맵 제외·광고 없음·링크 없음
2026-08-28 | 구현 | 신규 `app/routers/admin.py` · `repository.py` · `schemas.py` · `config.py` · `env.sample` · 신규 `tests/test_admin.py` | **운영자 전용 수집 로그 API 구현 — `POST /api/admin/login` · `POST /api/admin/logout` · `GET /api/admin/job-logs`.** 계약을 다시 훑다가 **빠뜨린 것을 발견해** 구현했다(2026-08-28 신설분 중 유튜브만 하고 이건 놓쳤다). ★ **빈 설정이 통과가 되지 않게 하는 것이 이 구현의 핵심**이다 — `secrets.compare_digest("", "")` 는 **True** 라서, 빈 값을 '인증 없음' 으로 두면 빈 토큰을 보낸 누구나 들어온다. 인증이 없는 것보다 나쁘다(있다고 착각하게 만든다). 그래서 `ADMIN_TOKEN`·`ADMIN_SESSION_SECRET` 중 하나라도 비면 이 경로 전체가 503 이고, 그 성질 자체를 테스트로 못 박았다. **기동은 막지 않는다** — 운영자 화면 하나 때문에 공개 API 전체가 안 뜨면 손해가 더 크다. 쿠키에는 토큰 원문이 아니라 `{발급시각}.{HMAC(서명키, 발급시각)}` 을 담는다(XSS 한 번에 영구 토큰이 새지 않게). 서명과 만료를 **둘 다** 본다 — 서명만 보면 한 번 발급된 쿠키가 영원히 유효해지고, 만료만 보면 누구나 새 쿠키를 만든다. 미래 시각 쿠키도 거부한다(서명이 맞아도 만료 계산을 믿을 수 없다). `ADMIN_TOKEN == ADMIN_SESSION_SECRET` 이면 **기동을 거부한다** — 계약이 '다른 값이어야 한다' 고 정한 것을 사람의 주의력에 맡기지 않는다(복붙 한 번이면 같아진다). **결정**: 쿠키 `Secure` 를 설정 키가 아니라 **요청 스킴**에서 정한다. 키를 늘리면 로컬에서 `true` 로 뒀다 쿠키가 안 붙거나 운영에서 `false` 로 두어 평문으로 새는 사고가 난다 — 스킴은 서버가 아는 사실이다. 계약에 기록했다. 요약(7일)에는 **목록 필터를 적용하지 않는다** — 실패만 보려고 필터를 건 사용자에게 '실패 3 / 실행 3' 을 보여주면 전부 실패한 것처럼 읽힌다. `last_success_at` 을 따로 세는 이유는 워커가 33일 멈춘 사고를 그 값 하나로 알아채기 때문이다. 목록은 커서 페이징(`before_id` 배타적)이고 `total` 을 주지 않는다. `job_name` 은 검증하지 않는다 — `job_nm` 에 CHECK 가 없고 잡 목록의 정본은 워커의 위키라, 백엔드가 목록을 박으면 워커가 잡을 추가할 때마다 틀린 답을 한다. ⚠ 브루트포스 카운터는 **프로세스 메모리**라 다중 프로세스에서는 각자 센다(지금은 1 프로세스). 실측(잡 이력 121건, 실패 3건): 로그인 전 401 → 로그인 200 → 요약 5잡 + 목록 커서 → 로그아웃 204 → 401. `stat` 은 있고 `logs` 는 null 인 실행이 실제로 있어 **null 경로가 실데이터로 확인**됐다. 테스트 14개, 전체 **199개 통과**. ⚠ **사용자 조치 필요**: `openssl rand -hex 32` 를 두 번 돌려 `.env_backend` 의 `ADMIN_TOKEN`·`ADMIN_SESSION_SECRET` 에 **서로 다른 값**으로 넣어야 화면이 열린다
2026-08-28 | ⚠ **함정** | `app/routers/admin.py` | **세션 발급에서 `time.time()` 을 두 번 부르면 초 경계에서 방금 만든 세션이 즉시 무효가 된다.** 쿠키에 담는 발급시각과 서명한 발급시각이 1초 어긋나기 때문이고, 재현이 드물어 '가끔 로그인이 안 된다' 로만 나타난다. 한 번 읽어 변수에 담는다
2026-08-28 | 검증 | 신규 `tests/test_contract_conformance.py` | **계약 전수검사를 테스트로 만들어 저장소에 남겼다.** 2026-08-12 에 같은 성격의 검사를 돌려 '위반 0건' 을 기록했으나 **그 검사기가 커밋되지 않아** 다시 돌릴 수 없었고, 그 뒤 추가된 영상·운영자·꿈해몽 필드는 같은 검사를 받은 적이 없었다. 한 번 돌리고 버리는 검사는 다음 사람이 다시 만들지 않는다 — 테스트로 둬야 매번 돈다. 공개 엔드포인트 12개 + 추천 + **꿈해몽**을 한 번씩 불러 응답 트리 전체를 훑는다(꿈해몽을 빼지 않는 이유: 계약이 `score` 를 금지 필드로 지목하며 든 예가 바로 `searcher.py` 의 유사도 점수다 — 샐 가능성이 가장 높은 곳을 빼면 검사의 의미가 없다). **종전 테스트에 있던 구멍 셋을 메웠다** — ① 금지 필드명을 `/recommend` 한 곳이 아니라 **모든 응답**에서 본다 ② **DB 컬럼명 누출**(`title_nm`·`provider_video_key`·`job_nm` 등 41개)을 본다. 계약의 첫 절이 'DB 컬럼명 ≠ API 필드명' 인데 검사가 없었다 ③ **모든 시각이 KST(+09:00)** 인지 본다. 변환 지점이 둘이면 언젠가 아홉 시간이 어긋난다. 금지 필드명 목록은 **계약 문서에서 직접 읽어 대조**한다 — 문서가 늘면 테스트가 먼저 깨져 사람이 옮겨 적기를 잊지 못한다. 결과 **위반 0건**(16개 통과). 계약 선언 19개 엔드포인트 대비 누락 0·계약에 없는 구현 0 도 함께 확인했다
2026-08-28 | 확인 | `10-contracts/api-contract-stats.md` | **`consecutive_counts` 는 이름과 달리 '개수' 가 아니라 '비율' 이다**(계약이 그렇게 정했다). 같은 응답의 `sum_histogram` 은 **회차 수**라 둘의 규칙이 반대이고, 검증식을 짤 때 헷갈려 한 번 오진했다. 계약이 직접 '두 값이 어긋나면 백엔드 버그다' 라고 적은 교차검증(`consecutive_ratio == 1 - consecutive_counts["0"]`)을 전수검사에 넣어 다시 헷갈리지 않게 했다. **코드는 계약대로였고 고칠 것이 없었다**
