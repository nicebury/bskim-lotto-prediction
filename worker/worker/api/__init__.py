"""내부 트리거 API.

**루프백에만 바인딩한다.** nginx 에 /internal/* 을 노출하지 않는다.
원격에서 실행해야 하면 SSH 터널을 쓴다:

    ssh -L 8003:127.0.0.1:8003 user@server

관리자 UI 를 만들지 않는다. 요구된 것은 "특정 키를 붙여 호출하면 수동 동작" 이지
관리 화면이 아니다. 화면을 만들면 인증·세션·CSRF 가 따라오고, 그것들은 이 서비스가
로그인 없이 운영된다는 전제와 충돌한다.
"""
