import type { GameMeta, GameSlug } from './types'

/**
 * 게임 여섯 개의 메타 — **데이터만** 담는다.
 *
 * → docs/wiki/10-contracts/playground-game-contract.md
 * → docs/wiki/20-design/game-g01-shooting.md … game-g06-ringdash.md
 *
 * ── ⚠ 이 파일에 canvas·window 가 한 줄이라도 있으면 빌드가 깨진다 ──
 * 목록 페이지의 SSR 텍스트, `generateMetadata`, `sitemap.ts` 가 **서버 컴포넌트에서**
 * 이 모듈을 import 한다. 브라우저 전용 API 를 참조하는 순간 서버 렌더가 죽는다.
 *
 * ── ⚠ 그리고 여기가 번들 경계다 ────────────────────────────────────
 * `catalog.ts`(데이터)와 `loaders.ts`(코드)를 나눈 이유가 이것이다. 목록 페이지가
 * 게임 구현을 딸고 들어가면 첫 화면이 여섯 배로 무거워진다.
 * **이 파일은 게임 모듈을 import 하지 않는다.**
 *
 * ── 값의 출처 ──────────────────────────────────────────────────────
 * `slug`·`code`·`title` 은 계약의 게임 목록 표, 나머지는 각 게임 사양서의 메타 표와
 * "한 문장" 절에서 옮겼다. **여기서 새로 지어낸 값은 없다.**
 */

/**
 * ⚠ 순서가 곧 목록 페이지의 카드 순서다. 코드 순(G01~G06)으로 둔다 — 사양서·계약과 같은
 *   순서라야 여섯 세션이 같은 그림을 본다.
 */
export const GAMES: readonly GameMeta[] = [
  {
    slug: 'shooting',
    code: 'G01',
    title: '오리 사격장',
    tagline: '12발 실탄으로 오리를 맞혀 숨은 번호를 꺼냅니다.',
    /*
      ⚠ **짧게 유지한다**(2026-09-08 사용자 요청). 이 글이 캔버스 위에 얹히는데, 모바일에서
        네 줄짜리 소개가 게임을 화면 밖으로 밀어내 스크롤하지 않으면 플레이할 수 없었다.
        한 줄로 목표만 말하고 나머지는 아래 "조작 방법" 이 맡는다.
    */
    intro: '12발 실탄으로 오리 여섯 마리를 맞히세요. 맞힌 오리가 번호를 물고 옵니다.',
    /*
      ⚠ 2026-09-17 에 **조준점이 좁혀질수록 정확해지는 규칙**이 생겼다(G01 사양서 참조).
        2026-09-17 에 다시 **조준이 끝나기 전에는 발사 자체가 안 되는** 규칙으로 바꿨다.
        그 전까지 줌은 장식이었고 누르자마자 떼도 불이익이 없었다. 규칙이 안내에 없으면
        사용자는 기다릴 이유를 모른 채 연타하게 되므로 둘째 줄에 명시한다.
      ⚠ 계약이 정한 3~4줄을 넘기지 않는다. 상세 페이지에 <ol> 로 그대로 렌더된다.
    */
    howTo: [
      '화면을 누른 채 움직여 오리에 조준점을 맞춥니다. 오리는 가끔 뛰어서 피합니다.',
      '누르고 있으면 조준점이 좁혀집니다. 다 좁혀져야 발사할 수 있어요.',
      '좁혀진 뒤 손을 떼면 발사됩니다. 맞힌 오리는 선반으로 올라가 번호가 공개됩니다(가끔 꽝).',
      '12발을 다 쓰기 전에 번호 6개를 모으면 성공입니다.',
    ],
    /*
      ⚠ `↑ ↓` 를 반드시 적는다. 컨베이어가 **세 줄**이라 좌우만으로는 다른 두 줄에 닿을 수
        없고, 안내만 따르는 키보드 사용자는 완주할 수 없다(2026-09-08 수정).
    */
    keyGuide: '키보드: ← → ↑ ↓ 로 조준, 스페이스로 발사',
    stage: { width: 360, height: 440 },
    paceSeconds: 60,
    accent: 'reco',
  },
  {
    slug: 'crane',
    code: 'G02',
    title: '크레인 뽑기',
    tagline: '자석을 멈춰 동물 볼을 붙입니다.',
    /*
      ⚠ **짧게 유지한다**(2026-09-08 사용자 요청). 이 글이 캔버스 바로 위에 얹히는데,
        모바일에서 세 줄짜리 소개가 게임기를 화면 밖으로 밀어내 스크롤하지 않으면
        플레이할 수 없었다. 한 줄로 목표만 말하고 나머지는 아래 "조작 방법" 이 맡는다.
      ⚠ **번호를 미리 말하지 않는다.** 볼에는 번호가 없고 깨져야 나온다(은닉형).
    */
    intro: '자석을 멈춰 원하는 동물 볼에 붙이세요. 볼이 깨지면 번호가 나옵니다.',
    howTo: [
      '자석이 좌우로 왕복합니다.',
      '원하는 볼 위에서 빨간 버튼(멈춤)을 눌러 세웁니다.',
      '한 번 더 눌러(내림) 자석을 내립니다. 볼 가운데에 맞아야 붙습니다.',
      '9번 안에 볼 여섯 개를 모으면 성공입니다. 가끔 꽝도 나옵니다.',
    ],
    keyGuide: '키보드: 스페이스를 두 번 (멈춤 → 내림)',
    stage: { width: 360, height: 430 },
    /*
      ⚠ 실측값이다. 기회가 9번뿐이라 **한 판의 길이에 상한이 있다** — 헤드리스로 재면
        잘 조준해서 21초, 대충 눌러 9번을 다 써도 32초다. 60 으로 두면 카드의 "약 60초" 가
        거짓말이 되고, 계약의 "목표 시간 ±30%" 검증도 통과할 수 없다.
    */
    paceSeconds: 35,
    accent: 'play',
  },
  {
    slug: 'flyball',
    code: 'G03',
    title: '로또볼 홈런',
    tagline: '눌러서 공을 부르고, 타이밍 맞춰 때려 멀리 보냅니다.',
    /*
      ⚠ **짧게 유지한다**(2026-09-10 사용자 요청). 이 글이 캔버스 바로 위에 얹히는데,
        모바일에서 세 줄짜리 소개가 캔버스를 밀어내 **그 아래 "모은 번호 6칸" 이 화면 밖으로
        나갔다.** 한 줄로 목표만 말하고 나머지는 아래 "조작 방법" 이 맡는다.
      ⚠ **번호를 미리 말하지 않는다.** 공에는 구간 색만 보이고 번호는 깨져야 나온다(색힌트형).
    */
    intro: '눌러서 공을 부르고, 타격 존에 올 때 다시 눌러 때리세요. 100m 를 넘기면 볼이 깨지며 번호가 나옵니다.',
    howTo: [
      '한 번 누르면 투수가 공을 던집니다. 공에는 번호 대신 구간 색만 보입니다.',
      '공이 타격 존에 들어올 때 다시 눌러 스윙합니다. 존 한가운데일수록 멀리 날아갑니다.',
      '100m 를 넘기면 볼이 깨지며 그 색 구간의 번호가 나옵니다. 200m 를 넘으면 장타입니다.',
      '기회는 9번입니다. 그 안에 번호 6개를 모으면 성공입니다.',
    ],
    keyGuide: '키보드: 스페이스로 공을 부르고, 다시 스페이스로 스윙',
    /*
      ⚠ 520 → **430**(2026-09-10). 캔버스가 길면 그 아래 진행 볼 여섯 칸이 모바일 화면
        밖으로 밀린다. 세로가 더 필요하면 늘리지 말고 카메라를 쓴다(계약).
    */
    stage: { width: 360, height: 430 },
    /*
      ⚠ 80 → 50 → 35 → **60**(2026-09-17). 35 는 "공이 알아서 오고 타구가 1~2초에 끝나던"
        시절의 실측이다. 2026-09-17 재설계로 **내가 눌러야 공이 오고**(기다리는 시간이
        사람마다 다르다) **장타 체공이 최대 4.2초**가 되어 한 타석이 6~8초로 늘었다.
        기회 9번이므로 한 판은 대략 60초다.
      ⚠ 이 파일을 전역 `sed` 로 고친 세션이 있어(`paceSeconds: 40` 값 치환) 이 줄이 남의
        값으로 바뀐 적이 있다(log.md). **공유 파일은 값이 아니라 slug 블록을 찾아 그 안에서만
        고친다.**
    */
    paceSeconds: 60,
    accent: 'lotto',
  },
  {
    slug: 'curling',
    code: 'G04',
    title: '얼음판 컬링',
    tagline: '스톤을 밀어 뒤집힌 칸을 맞히면 번호가 열립니다.',
    /*
      ⚠ **한두 문장으로 끝낸다**(계약: 모바일에서 소개글을 길게 쓰지 않는다). 이 글이 캔버스
        바로 위에 얹히므로, 한 문장이 늘 때마다 캔버스가 아래로 밀리고 그 아래 "모은 번호
        6칸" 이 화면 밖으로 나간다. 규칙 설명은 아래 "조작 방법" 이 맡는다.
      ⚠ **번호를 미리 말하지 않는다**(2026-09-17 은닉형 전환). 예전 소개글은 "1번부터 45번까지
        칸이 펼쳐져 있습니다" 였는데, 이제 칸은 전부 뒷면이라 그 문장이 화면과 어긋난다.
    */
    intro:
      '얼음판 끝에 마흔다섯 칸이 뒤집힌 채 놓여 있습니다. 스톤을 당겼다 놓아 칸 가운데에 세우면 그 칸이 열립니다.',
    howTo: [
      '스톤을 아래로 당겼다 놓으면 위로 미끄러집니다. 많이 당길수록 멀리 갑니다.',
      '칸 가운데에 세워야 열립니다. 선에 걸치면 아무것도 열리지 않습니다.',
      '열린 칸은 번호일 수도 꽝일 수도 있습니다. 얻은 번호 칸은 장애물이 됩니다.',
      '좌우 벽에 맞고 돌아 들어갈 수 있습니다. 기회는 아홉 번입니다.',
    ],
    keyGuide: '키보드: ← → 로 방향, ↑ ↓ 로 세기, 스페이스로 발사',
    /*
      ⚠ 620 → **430**(2026-09-16). 계약의 "420~450 안에서 끝낸다" 를 따른 것이자, 620 에서는
        **모바일에서 스톤이 화면 밖이라 게임이 성립하지 않았기** 때문이다. 무대 높이가
        `clamp(260, 100svh - --pg-chrome, 660)` 로 먼저 정해지고 캔버스가 그 박스를 채우므로,
        세로로 긴 무대일수록 아래가 잘린다 — 390×760 에서 논리 471, 375×667 에서 394 까지만
        보였다. 보드 칸을 30→23px 로 줄이고 물리를 다시 맞춰 430 에 담았다.
        → docs/wiki/20-design/game-g04-curling.md
    */
    stage: { width: 360, height: 430 },
    paceSeconds: 90,
    accent: 'stats',
  },
  {
    slug: 'plinko',
    code: 'G05',
    title: '플린코 낙하',
    tagline: '아홉 칸 중 셋은 꽝. 아홉 번 안에 번호 6개를 모아 보세요.',
    /*
      ⚠ **짧게 유지한다**(계약: 모바일에서 소개글을 길게 쓰지 않는다). 이 글이 캔버스 바로
        위에 얹히는데, 세 줄짜리 소개가 캔버스를 밀어내 그 아래 "모은 번호 6칸" 이 화면 밖으로
        나간다. 한두 문장으로 목표만 말하고 나머지는 아래 "조작 방법" 이 맡는다.
      ⚠ **번호를 미리 말하지 않는다.** 빈에는 구간 색만 보이고 번호는 깨져야 나온다(색 힌트형).
    */
    /*
      ⚠ **기회 아홉 번과 꽝 셋을 여기서 말한다**(2026-09-17 · 사용자 지시). 이 글은 JS 를
        끈 상태에서도 보이는 SSR 본문이라, 규칙이 바뀌었는데 여기가 옛 글이면 화면과 설명이
        갈린다. → docs/wiki/20-design/game-g05-plinko.md "2026-09-17 개정 요약"
    */
    intro:
      '떨어뜨릴 자리를 정해 볼을 놓으세요. 아홉 칸 중 세 칸은 꽝이고, 기회는 아홉 번입니다.',
    howTo: [
      '떨어뜨릴 자리를 좌우로 옮깁니다. 아홉 칸 중 셋은 꽝이고, 나머지에는 번호 대신 구간 색만 보입니다.',
      '놓으면 볼이 못을 튕기며 내려갑니다. 자리는 내가 정하지만 결과는 튕김이 정합니다.',
      '번호 칸에 들어가면 볼이 깨지며 그 색 구간의 번호가 나옵니다.',
      '꽝 칸에 들어가면 기회만 줄어듭니다. 아홉 번 안에 번호 6개를 모으세요.',
    ],
    keyGuide: '키보드: ← → 로 자리, 스페이스로 놓기',
    /*
      ⚠ 640 → **430**(2026-09-16). 계약이 `stage.height` 를 **420~450 안에서 끝내라**고
        정했다. 640 이면 모바일에서 높이가 먼저 걸리고 `aspect-ratio` 가 폭을 줄여 **그려지는
        폭이 263px** 까지 좁아지는데, 이 게임은 그 폭을 아홉으로 나눠 쓰는 구조라 빈이 29px 이
        된다. 430 이면 358px 폭이 살아 빈이 40px 을 지킨다. 못 행도 11 → 9 로 줄였다.
    */
    stage: { width: 360, height: 430 },
    /*
      ⚠ 40 → **55**(2026-09-17). 기회가 아홉 번으로 제한되고 아홉 칸 중 셋이 꽝이 되면서
        여섯 개를 모으는 데 필요한 낙하 수가 여섯에서 **최대 아홉**으로 늘었다. 한 번에
        조준까지 약 6초이므로 아홉 번이면 55초다. 꽝 연출(0.75초)이 획득(1.1초)보다 짧아
        늘어나는 폭이 낙하 수에 비례하지는 않는다.
    */
    paceSeconds: 55,
    accent: 'dream',
  },
  {
    slug: 'ringdash',
    code: 'G06',
    title: '링 통과 비행',
    tagline: '날갯짓으로 높이를 맞춰 링을 통과하며 번호를 모으세요.',
    /*
      ⚠ **짧게 유지한다**(계약 2026-09-16). 이 글이 캔버스 바로 위에 얹히는데, 세 줄짜리
        소개가 게임을 화면 밖으로 밀어낸다. 한두 문장으로 목표만 말하고 나머지는 아래
        "조작 방법" 이 맡는다.
      ⚠ **번호를 미리 말하지 않는다**(계약 색 힌트형, 2026-09-10). 링에는 구간 색만 보이고
        번호는 통과해야 나온다. 옛 문구("번호가 걸린 링")는 이제 거짓말이 된다.
      ⚠ **"링 여섯 개" 라고 쓰지 않는다**(2026-09-17 개편). 이제 링 스무 개 남짓을 지나야
        번호 6개가 찬다 — 여섯 번만 지나면 된다고 읽히면 그 자체가 거짓말이 된다.
    */
    intro:
      '눌러서 날갯짓하며 링을 통과하세요. 색이 있는 링을 지나면 볼이 깨지며 번호가 나옵니다.',
    howTo: [
      '눌러서 날갯짓하면 위로 오르고, 놓으면 천천히 내려갑니다.',
      '색이 있는 링에만 번호가 들어 있습니다. 회색 링은 지나가도 번호가 나오지 않습니다.',
      '높이를 맞춰 링 가운데로 지나가면 볼이 깨지며 번호가 나옵니다.',
      '부딪혀도 모은 번호는 그대로이고 기회만 줄어듭니다. 기회 9번 안에 6개를 모으면 성공입니다.',
    ],
    /*
      ⚠ `←` `→` 를 반드시 적는다. 번호 6개를 모은 뒤 "계속 날기 / 번호 확인" 을 고르는
        화면이 캔버스 안에 있어, 좌우키가 없으면 **키보드 사용자는 고를 수가 없다**
        (2026-09-17 개편).
    */
    keyGuide: '키보드: 스페이스로 날갯짓, 완주 후 ← → 로 선택',
    /*
      ⚠ 560 → **430**(2026-09-16). 계약이 무대 세로를 420~450 으로 못 박았다 — 캔버스가
        세로로 길면 그 아래 "모은 번호 n/6" 볼 여섯 칸이 모바일 화면 밖으로 밀리고, 이제는
        높이가 먼저 걸려 `aspect-ratio` 가 폭까지 줄인다(560 이면 390px 화면에서 301px).
        G02 가 430 으로 줄였고 G03 이 520 → 430 으로 따라간 것과 같은 조정이다.
      ⚠ 세로가 줄어도 물리는 그대로다 — 간극 중심 범위를 고정값이 아니라 **간극 크기에서
        유도**하도록 게임 쪽을 함께 고쳤다(`MIN_PIPE_LEN`). 무대 560 에서 사양서의
        [130, 390] 과 사실상 같은 값이 나온다.
    */
    stage: { width: 360, height: 430 },
    /*
      ⚠ 60 → **90**(2026-09-17 개편). 옛 값은 "링 여섯 개를 지나면 끝" 이던 시절의 실측이다.
        이제 번호 6개를 채우려면 링을 **20개** 지나야 하고, 링 하나에 2.0초(간격 260px ÷
        속도 130px/s)이므로 무실수 코스만 3 + 2 × 20 = **43초**다. 여기에 충돌·이어날기
        대기가 붙으므로 90 이 맞다.
      ⚠ 이 값은 아직 **실측이 아니다.** 개편 후 측정은 사용자가 직접 한다(2026-09-17).
    */
    paceSeconds: 90,
    accent: 'news',
  },
] as const

/**
 * slug 로 찾는다. 없으면 `undefined` — 호출부(상세 페이지)가 `notFound()` 를 부른다.
 *
 * ⚠ **사이트맵에 없는 URL 이 200 을 내면 안 된다.** 그래서 상세 페이지는 반드시 이 함수의
 *   결과를 확인하고, 없으면 404 로 보낸다.
 */
export function findGame(slug: string): GameMeta | undefined {
  return GAMES.find((g) => g.slug === slug)
}

/** 정적 경로 생성·사이트맵용. */
export const GAME_SLUGS: readonly GameSlug[] = GAMES.map((g) => g.slug)
