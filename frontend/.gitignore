/node_modules
/.next/
# 실측용 프로덕션 산출물 (NEXT_DIST_DIR). dev 서버가 켜진 채로 빌드할 때 쓴다.
/.next-prod/
/out/
/build

# 환경변수 실제 파일 — 커밋하지 않는다. 템플릿은 env.sample.
.env
.env_frontend
.env.local
.env*.local

*.tsbuildinfo
next-env.d.ts.bak
.DS_Store
npm-debug.log*
